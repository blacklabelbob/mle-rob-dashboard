// One-line DEMO disclaimer, shared across every rep-facing surface (Critic
// Rob punch #9: /rep/accounts and the workspaces relied only on "(fabricated)"
// role text + the timeline's DEMO-HISTORY badge — which Dale's workspace
// never gets, since his timeline is an honest empty shell, not fake history).
// A single shared component means every current and future rep page carries
// the same disclaimer without hand-copying text.
export default function DemoFooter() {
  return (
    <p className="text-[11px] text-slate-600">
      DEMO rep + fabricated demo leads (marked in every record) — built 2026-07-18 per Rob to show
      the rep experience. Real reps + real routing land with Phases 4–5.
    </p>
  );
}
