# CRM BUILD QUEUE — worked by the 10-minute driver (crm-build-driver.sh)
**✅ FREEZE LIFTED 2026-07-22 (Rob dev-chat #40: "Do what the best course of action is" — his direct reply to the pending GO/HYBRID/SWEEP asks after his dump ended). Driver back to full build mode.** ~~🧊 CAPTURE-ONLY FREEZE (Rob, dump 7.21.26-1 ~22:00): "DO NOT MAKE ANY CHANGES EXCEPT TAKING THE LOGIN INFORMATION AWAY FOR NOW. I HAVE MORE TO WRITE."~~ Login removal stands (f7e2e81, env var only — re-armable in one command; no login prompts until the ACCESS rollout, Q6). Q7b/Q8/Q9's "frozen until Rob's go" markers are hereby satisfied — their other gates (scope re-check, critic-rob, etc.) still apply.
**Protocol (amended 7/21 — Rob):** every increment also syncs the living PRDs same-commit; stale PRD = defect.  the driver takes the TOP unchecked item, does ONE safe increment (≤6 min), verifies (build/tests), commits + pushes, and ticks or annotates the item. Milestones get a critic-rob review before checking off. Never modify money/signed fields without a Rob instruction. Rob's dev-chat messages ALWAYS preempt this queue.

- [ ] Q59. **Task MC.12 — ops panels on the MC.8 read-model seam (picked 2026-07-24 as top UNBLOCKED work; gate map re-verified this run and unchanged: Q57 = Rob's two one-word Title Base answers still unanswered — newest rob dev-chat is #47, Q15/Q5b creds-blocked, Q16/Q6 Phase-4-gated, Q39–Q45 Rob-gated on master-view approval)** | DoD: per PRD MC.12 — Pipeline / Onboarding-E-sign / Action Items (ours+theirs) / Invoicing-AR / KPI Summary each render live data, smoke test per screen; panels read `rm_*` ONLY, never base tables
  - 2026-07-24 driver (inc.1): **shaping layer shipped, pure + tested against real prod rows.** `lib/readModel/panels.ts` (no clock/network/Next imports per CR-3; `todayISO` passed in, ET anchor shared with `overdue.ts`): Pipeline buckets all 12 canonical stages in `DEAL_STAGES` order and splits money three ways — dollars vs **COMPED** ($0, Rob's 7/23 ruling) vs **unvalued** (null, never counted as zero); `stalled` stays OPEN, `paid`/`lost` close, so today's 8 prod deals give openValue 17,000 / wonValue 21,000; off-ladder stages surface in `unknownStages`. Action items split ours/external/unassigned on a named roster (assigned_to is free text until Phase-4 profiles) — a non-roster name is printed, never assumed to be the customer — with overdue/due-today/upcoming/**undated** counted separately and most-overdue-first ordering. E-sign counts the shared `REQUEST_STATUSES` ladder + a `no_request` bucket, and derives awaiting-countersignature from `countersigned_at IS NULL` (0010's "signed stays terminal" holds). Honest coverage is enforced: `panelHeader()` returns unavailable/empty/live with the contract's own reason, and `buildUnavailablePanel()` THROWS if asked to stub a buildable model. 20 new tests on the verbatim prod row shapes; 657/657 vitest, build green; no user-visible surface yet → no deploy. **Next: the read route (connect as `dashboard_ro`), then the panel UI.**
- [x] Q58. **Task MC.8 — read-model data contract + `dashboard_ro` SELECT-only role (picked 2026-07-24 as top UNBLOCKED work; gate map re-verified this run: Q57 blocked on Rob's two one-word answers, Q15/Q5b creds-blocked — no `TWILIO_*`/`VAPI_*` in `.env.local`, Q16/Q6 Phase-4-gated, Q39–Q45 Rob-gated on master-view approval)** | DoD: per PRD MC.8 — `docs/data-contract.md` committed; INSERT/UPDATE attempt fails in test; views return expected sample rows
  - 2026-07-24 driver (inc.1, watchdog-recovery run): **CONTRACT SHIPPED AS CODE — DoD leg 1 of 3.** Prior run (09:28) died on `API Error: Connection closed mid-response` (rc=1) with nothing staged and the tree clean — no work to recover, so this run started the next item instead. `lib/readModel/contract.ts`: all six read models with per-column `table.column` sources; `docs/data-contract.md` GENERATED by `scripts/gen-data-contract.mjs` (Node native TS strip — no tsx dep) and a test compares the committed file byte-for-byte to the generator, so the doc structurally cannot rot (CR-3). **Column truth gate:** sources verified against live Supabase introspection + migrations 0008-0010 this run; a test asserts every column resolves into the pinned `SOURCE_COLUMNS`, so an invented field fails the suite before MC.12 builds a panel on it. **Honest coverage — 2 of 6 blocked, named not faked:** `rm_delivery_phases` has no component-checklist store (`documents.phase` = a PDF label, `people.phase_one` = free text) → unblocked by Q40; `rm_invoices_ar` is impossible in SQL per the G3 verdict (MC.7 — `invoice-ledger.csv` is the only live invoicing store) → unblocked by MC.9's CSV-diff leg. `rm_esign_status`/`rm_nudge_activity` = `buildable_empty` (schema live; prod holds ZERO documents/signature_requests/signature_events, verified live) so panels must render an empty state, not a broken one. `NEVER_EXPOSED` test keeps token hashes, signer IP/UA and file digests out of every model; `dashboard_ro` defined with no write verb. 625/625 vitest (9 new), build green, no user-visible surface → no deploy. **NEXT increment:** SQL migration (`0011_read_models.sql`) creating the four creatable views + the role, a negative test proving INSERT/UPDATE fails as `dashboard_ro`, and sample-row verification against prod → then MC.8 ticks.
  - 2026-07-24 driver (inc.2): **Q58 TICKED — MC.8 CLOSED, all three DoD legs met, seam LIVE on prod.** `supabase/migrations/0011_read_models.sql` applied to prod (Supabase MCP, tracked as `read_models`): four views (`rm_pipeline`, `rm_esign_status`, `rm_action_items`, `rm_nudge_activity`) + `create role dashboard_ro nologin`, SELECT granted on the views only, explicitly REVOKEd on all seven base tables. **Write-denial PROVEN ON PROD as that role** (not asserted in a unit test): `select from deals` → `insufficient_privilege`; `insert into rm_action_items` → `insufficient_privilege` (that view is simple/auto-updatable, so the failure is a genuine privilege check, not "views aren't writable"); UPDATE/DELETE via `rm_pipeline` → `55000`; `select from rm_pipeline` succeeds — panel path open, every write path closed. **Sample rows:** `rm_pipeline` 8/8 with a resolved `counterparty_name` (people/orgs coalesce holds post-split), `rm_action_items` 2/2 with due dates, the two `buildable_empty` views 0 rows exactly as predicted. **Anti-fiction gate:** `rm_delivery_phases`/`rm_invoices_ar` NOT created, and a test asserts they stay uncreated — nobody can stub an empty view that reads like a working feature. 12 new tests (`lib/__tests__/readModelSql.test.ts`) parse the migration and compare it to the contract column-for-column, in order, plus table scope, `SOURCE_COLUMNS` membership, `NEVER_EXPOSED` absence, and the role's grant/revoke shape. Security note pinned in the migration header: views are intentionally NOT `security_invoker` (owner rights are what let a base-table-less role read them). 637/637 vitest, build green, no user-visible surface → no deploy. Not a milestone item (Q4/Q6/Q9 only) → no critic-rob gate. **NEXT for the driver:** re-run the gate map — Q57 still blocked on Rob's Title Base scope answer (dev-chat #49 unanswered), Q15/Q5b creds-blocked, Q16/Q6 Phase-4-gated, Q39–Q45 Rob-gated; MC.12 (panels on the new `rm_*` seam) is the natural next unblocked build.
  - 📐 ATLAS 2026-07-24 (Q58, UPDATED at close): refresh pass (still pending) must add the read-model layer to the map — `lib/readModel/contract.ts` → generated `docs/data-contract.md` → **live** `rm_pipeline`/`rm_esign_status`/`rm_action_items`/`rm_nudge_activity` views + `dashboard_ro` role: the seam MC.12's panels read INSTEAD of base tables. Draw it as a hard boundary (panels never touch `deals`/`people`/`tasks`/`documents` directly) and mark the two absent read models as blocked-by-Q40 / blocked-by-MC.9 so the map doesn't imply six working views. Artifact republish requires the interactive session.

- [ ] Q57. TITLE BASE PAPERWORK (Rob dev-chat #44, 2026-07-23 — "get that all taken care of in the system and dashboard"): CRM half DONE this increment (org `the-title-base` + Trent Brands promoted to client/lit + deal `deal-the-title-base-phase-1` PAID $2,000 check 2026-07-23 + payment activity — all live on prod, verified). REMAINING (this item): formal Phase 1 agreement + invoice in the contracts system (`../contracts` engine, gulf_coast.json pattern → `clients/the_title_base.json`) | DoD: client JSON + Phase 1 invoice marked PAID (check, 2026-07-23) generated via the contracts engine; agreement generated or explicitly waived by Rob | BLOCKED-ON-ROB: Phase 1 scope for Title Base (pages/sites/second-brains config) + signed-agreement date if one exists — asked in dev-chat #46 + medium flag in Things to Address. If no answer, next driver may draft the client JSON with scope marked TBC but must NOT invent scope numbers. **7/23 inc.2 (driver): client JSON DRAFTED — `../contracts/clients/the_title_base.json` (commit e18f6a0, pushed): known facts only ($2,000 fee, paid-in-full check 2026-07-23 → files in `clients/` per organize.py rule 1, verified by filing-engine run), scope fields null + `scope_status` TBC, intake block deliberately incomplete so both engines REFUSE generation (check_intake gate verified in code) until Rob supplies scope + agreement date. Remaining for tick: Rob's intake answers → generate agreement (or Rob waives) + invoice PAID; note: local python3 lacks reportlab — generation env needs `pip3 install reportlab` or the venv prior sessions used.** **7/23 inc.3 (driver): that env blocker is CLEARED (contracts 64fe45c, pushed) — repo `.venv` provisioned + documented in the contracts README as one-command setup; both engines verified end-to-end on python 3.13 / reportlab 5.0.0 (agreement + invoice PDFs render against a scratch config in /tmp) AND verified to still refuse `clients/the_title_base.json` (incomplete intake, exit 1, nothing written). Zero agreement/invoice/ledger/money files touched. Fixed same commit, found while verifying: `phase1_engine.py` crashed `FileNotFoundError` when its output dir didn't exist (invoice engine already did makedirs) — latent only. **Q57 now needs NOTHING but Rob's two answers** (Phase 1 scope for Title Base + signed-agreement date if one exists) — standing in dev-chat #46 + the Things to Address flag; once answered, generation is two commands.** **7/23 inc.4 (driver): ROB ANSWERED HALF THE GATE — dev-chat #47 (`created_at` 2026-07-24T02:18Z = 2026-07-23 22:18 ET; increment labeled by Rob's local date per the PRD date convention), verbatim "1 Website, 1 second Brain, 1 Website, Social Media". Recorded verbatim on `deal-the-title-base-phase-1` notes + `../contracts/clients/the_title_base.json` (second_brains_total=1, entities_count=1, social media yes; `website_pages` deliberately left null). NOT generated: 'Website' is listed TWICE — 1 site or 2? — and no signed-agreement date. Both asked back terse in dev-chat #48 and folded into flag #29 (retitled, still open) rather than a new flag. Agreement engine re-verified to REFUSE (intake missing confirmed_by/date, exit 1, nothing written). Q57 still open, still Rob-gated — but now on TWO one-word answers, not the whole scope. ATLAS: Architecture Atlas needs its refresh pass (dated 2026-07-23).**
- [x] Q1. Fallback truthfulness (Critic Rob R1-#7): regenerate `data/network.json` from live Supabase + `scripts/regen-fallback.mjs` + visible amber "serving fallback data" banner when file store is active | DoD: fallback file matches live rows; banner renders when STORAGE_SOURCE=file
  - 2026-07-18 driver: `scripts/regen-fallback.mjs` added (mirrors supabaseStore field mapping; loads .env.local; deterministic id-ordered output). Ran it: network.json went 12→34 people, 10→49 edges, 6→8 verticals, 7→12 projects — now matches live Supabase.
  - Amber banner: `components/FallbackBanner.tsx` in layout; `servingFileData()` in lib/storage distinguishes "configured" (STORAGE_SOURCE=file) vs "fallback" (Supabase read failed, sticky until next good read).
  - Verified: npm run build green, 8/8 vitest, local `next start` with STORAGE_SOURCE=file renders "File store active — this is snapshot data, not live Supabase."
- [x] Q2. Purge remaining 7/4 seed fiction (2026-07-18: will-network-a deleted w/ edge cleanup — last placeholder; PropLogic merge question posted to Rob async): audit projects table + any placeholder rows (Will's Network Contact A/B etc.) — list to Rob in dev-chat for kill confirmation, delete confirmed ones | DoD: no dummy data renders anywhere
  - 2026-07-18 driver: full audit done against live Supabase (39 people, all edges, 8 verticals, 12 projects). ONLY placeholder left: `will-network-a` ("Will's Big-Network Contact A") + edge `e-will-a`; Contact B already gone. Projects table: all 12 rows real, no fiction (7/4 completion %s stale — separate refresh). DEMO rep-cockpit records excluded by design. Kill-list posted to dev-chat (msg #24) — WAITING on Rob's KILL/rename before deleting (hard limit). Next driver: if Rob confirmed, delete row + edge, regen fallback, then tick.
- [x] Q3. entity_kind mapping (2026-07-18: entityKind through types+adapter; 'biz' badge in ledger, 'business' badge on record header — live) (Critic Rob R1-#6 tail): expose `entityKind` through types/adapter/UI badge (person vs business) as the visible precursor to Task 2.0 | DoD: businesses show a subtle badge in ledger + record
- [x] Q4. Task 2.0 people/org split ✅ DONE 2026-07-21 — critic-rob TICK 97/100 (docs/reviews/CRITIC-ROB-Q4-orgs-split-2026-07-21.md) [increment 1 done 2026-07-18: 0003_orgs_split.sql written (ids preserved, paired-FK edges, reconciliation gate) + dry-run script; NEXT: apply on a Supabase branch, adapter reads orgs, UI merge view, critic-rob review before prod apply]
  - 2026-07-21 driver: DRIVER OUTAGE POSTMORTEM — no increments landed 7/18 00:27 → 7/21 18:37 (~3.7 days, ~500 ticks). Root cause: crm-build-driver.sh was edited 7/18 ~00:20 with the headless prompt inlined unquoted; bash died at parse (lines 46-47, 1052 errors in crm-driver.err) before ever launching claude. Fixed 7/21 18:36 by externalizing the prompt to crm-build-driver-prompt.txt; `bash -n` clean; this run is the fix working. Q4 dry-run re-verified against TODAY'S live data: 35 people → 18 people + 17 orgs, 33/48 edges repointed, reconciliation 35=35 holds (counts drifted from 7/18 — script handles it). Next increment: apply 0003 on a Supabase branch + adapter org reads. — design + migration `0003_orgs_split.sql` per D-002 ERD (orgs, org_memberships, paired-FK edges), classification pass with review queue in dev-chat | DoD: PRD Task 2.0 DoD (54-in reconciliation adapted to current counts)
  - 2026-07-21 driver (2): DATA-LOSS DEFECT caught + fixed BEFORE apply — 0003's orgs table dropped referred_by_id (all 17 company rows have one), relationship (17), estimate (11), phase_one (4 in-progress), role/business/est_time_to_payment_days, meeting/transcript urls. Amended: orgs mirrors people verbatim minus entity_kind (+ step 1b repoints org→org referrals; prune later w/ Rob). Dry-run now prints a field-preservation gate (per-column counts, must match orgs post-apply) and warns on any people column absent from the carry list — that gate is what caught est_time_to_payment_days. Re-verified: 35→18+17, 33/48 edges, 0 dropped columns, build green. NEXT: apply on Supabase branch, then adapter dual-schema org reads.
  - 2026-07-21 driver (3): FULL-SQL REHEARSAL ON PROD, auto-rollback (scripts/orgs-split-rehearsal.sql — DO block ending in RAISE EXCEPTION; branch idea dropped: Supabase branches carry schema only, no prod rows, so they can't test a data migration). Run #1 caught a SECOND apply-blocking defect the JS dry-run couldn't see: 0003 nulled edges.from_id/to_id BEFORE dropping NOT NULL → 23502 abort. 0003 reordered (drop not null first). Run #2 PASS on live data: 32 people (drifted again from 35 this afternoon — live edits) → 16 people + 16 orgs, recon_ok=t; edges 47, 33 repointed (1 from_org + 32 to_org), constraint violations 0/0; field-preservation gate pre==post exact (referred_by 16, relationship 16, estimate 10, phase_one in-progress 4, quoted 4, signed 3) fields_ok=t. Prod verified untouched after (no orgs tables, no org cols, 0 null edge FKs). 0003 is now apply-ready. NEXT: adapter dual-schema org reads (env-gated), then real apply + critic-rob review.
  - 2026-07-21 driver (4): ADAPTER DUAL-SCHEMA MODE DONE (env-gated `ORGS_SPLIT_READS=1`, unset in prod → byte-identical pre-split behavior). supabaseStore: getNetwork merges `orgs` rows as entityKind-company Persons; edges coalesce paired FKs (from_id??from_org_id); referred_by_org_id coalesced into referredById on read. Writes: company upserts route to `orgs` (entity_kind dropped, node_type narrowed to orgs check constraint) and referrers route to the paired person/org column via one orgs lookup — a business edit can never fork back into `people`. 7 new mapper unit tests (lib/storage/__tests__/orgsSplit.test.ts), 15/15 vitest, build green. No deploy (no user-visible change while flag off). NEXT: real apply of 0003 (then set ORGS_SPLIT_READS=1 in the same Vercel deploy), UI merge view, critic-rob review.
  - 2026-07-21 driver (5): **0003 APPLIED TO PROD — SPLIT IS LIVE.** Pre-apply JSON backups (backups/pre-0003-*-2026-07-21.json: 32 people/16 company/47 edges, matched rehearsal exactly). Applied via Supabase MCP apply_migration (tracked as `orgs_split`); post-apply gates == rehearsal run #2 to the digit (16 people + 16 orgs, 0 company left in people; 47 edges, 1 from_org + 32 to_org, 0 constraint violations; field preservation: referrer 16, relationship 16, estimate 10, phase_one in-progress 4, quoted 4, signed 3). ORGS_SPLIT_READS=1 set in Vercel prod env then deployed same increment (Vercel CLI upgraded 37.4→56.4 mid-flight — old CLI got endpoint-rejected mid-upload; new binary at ~/.npm-global/bin/vercel, /usr/local/bin one is stale). Prod curl verified: 32 nodes / 47 edges / 0 null endpoints; all 32 pre-split ids render; spot-check calebs-brother-moving-co quoted 7000→7000, signed false→false, referrer intact. regen-fallback.mjs made org-aware (was NOT — fallback would have silently dropped all 16 companies post-split) + network.json regenerated (32 people incl 16 companies). Build green, 15/15 vitest. REMAINING on Q4: person→org org_id backfill (column exists, unlinked), UI merge-view check, critic-rob milestone review → then tick.
  - 2026-07-21 driver (6): person→org BACKFILL DONE (scripts/backfill-org-links.mjs, idempotent + gated). 11/16 people linked via people.org_id + 15 org_memberships rows (3 secondary affiliations: Gary co-owns Miga per Rob 7/17 while employed De Cecco; Daniella spans Miga/Martin Fierro/Oasis; Michael spans Martin Fierro/Oasis). Every link source-cited (Sunbiz, FL DBPR, LinkedIn, Rob confirmations) — zero guesses; 5 skips printed w/ reasons (Trent/Title Base, Cates, George/Guest Genie, Rob+Will/MLE — no org rows exist). Verified: gate 11/16 pass, re-run idempotent (0 new writes), build green, 15/15 vitest. No deploy (org_id not yet read by UI). REMAINING on Q4: UI merge-view check, critic-rob milestone review → then tick.
  - 2026-07-21 driver (7): **Q4 CLOSED.** UI merge-view check caught a real prod defect first: commit 6b5faeb (isDemo filter + demo book) was committed but NEVER DEPLOYED — prod /api/network was serving 38 nodes incl. all 6 DEMO rows in the main graph/ledger feed. Rebuilt (green) + 15/15 vitest + vercel --prod; post-deploy curl: 32 nodes / 47 edges / 0 DEMO / 0 dangling, money spot-check intact; /people renders biz badges, company record renders Business header. Then critic-rob milestone review: **TICK 97/100** (Truth gate 100 — signed set pre/post identical, Gulf key_dates byte-for-byte; review in docs/reviews/CRITIC-ROB-Q4-orgs-split-2026-07-21.md). Punch list (non-blocking, recorded in PRD Task 2.0): export entityKind in /api/network pre-Phase-2; drop people.entity_kind after Task 2.2; Gulf dispute resolved-by-data 7/18. LESSON for the driver: "committed + build green" ≠ shipped — any increment touching a user-visible surface must end with a prod curl, not a local verify.
- [x] Q5. Task 7.1 dialer scorecard DONE 2026-07-18: raw Twilio wins 94.5/100 (JustCall 78.25 runner-up; full scorecard docs/research/DIALER-SCORECARD-2026-07-18.md; posted to dev-chat; Rob can veto async — proceeding per composite + merit rule)
- [x] Q24. **Task 3.4 overdue follow-up watcher (picked 2026-07-22 as top unblocked work — Q15/Q5b creds-blocked, Q16 Phase-4-gated, Q6 awaiting rep-mockup approval; LAST open Phase-3 task):** hourly n8n schedule workflow → `GET /api/cron/overdue` → past-due open tasks flag the ledger | DoD: per PRD Task 3.4 — seeded past-due record triggers exactly one ping, no dupes on re-run
  - 2026-07-22 driver (inc.1): CRM-SIDE WATCHER DONE — pure `lib/integrity/overdue.ts` (CR-3: open + `due_date` < Rob's ET calendar day via `todayInET(now)` — clock passed in, never read; due-today never alerts, UTC-rollover-at-7pm-ET can't false-ping; demo-* excluded; deterministic title `Overdue follow-up: task <id> (due <date>)` = idempotency key → hourly re-runs never dupe, reschedule re-arms) + `GET /api/cron/overdue` (reuses `verifyCronAuth`/CRON_SECRET: 503 unset / 401 wrong bearer; ping = medium flag, entity "CRM follow-ups", findings protocol). Vercel Hobby 2-cron cap spent (dedup+integrity) → hourly firing = n8n schedule workflow with the same bearer, which is what the task asked for anyway. 9 tests (275/275), build green, NO deploy (inert route, no user-visible surface). NEXT increment: deploy → provision n8n hourly workflow (N8N_KEY, Schedule Trigger → HTTP GET w/ Authorization bearer) → seed past-due task → live DoD (run 1 = 1 flag, run 2 = 0) → clean scaffolding → tick.
  - 2026-07-22 driver (inc.2): **DEPLOYED + DoD PROVEN ON PROD.** vercel --prod (via ~/.npm-global/bin/vercel — /usr/local/bin CLI still stale, endpoint-rejects uploads); prod contract curls: no bearer 401, bad bearer 401, good bearer honest `{ok:true,overdue:0,flagged:0}` (tasks table is genuinely empty — first live run created zero fiction). Live DoD: seeded past-due open task `wtest-q24-dod` (due 2026-07-20) → run 1 `{overdue:1,flagged:1}` — flag verified on the ledger (medium, entity "CRM follow-ups", title `Overdue follow-up: task wtest-q24-dod (due 2026-07-20)`, detail "2 days overdue and still open") → run 2 `{overdue:1,flagged:0}` — exactly one ping, zero dupes. Scaffolding cleaned (seed task + test flag deleted, 204/204; final run honest `{overdue:0,flagged:0}`; pulled env file shredded). ONLY remaining: n8n hourly schedule workflow (Schedule Trigger every hour → HTTP GET https://mle-rob-dashboard.vercel.app/api/cron/overdue w/ Authorization bearer CRON_SECRET as an httpHeaderAuth cred, retry ×2 like the voice-law node) + observe one scheduled execution → tick.
  - 2026-07-22 driver (inc.3): **TICKED — n8n hourly workflow LIVE + scheduled execution observed; Task 3.4 CLOSED, Phase 3 COMPLETE (8/8).** Provisioned via n8n MCP: workflow `ozMXSpftU2lIbhp2` "MLE CRM — Hourly Overdue Task Watcher" (Schedule Trigger hourly at :33 → HTTP GET https://mle-rob-dashboard.vercel.app/api/cron/overdue, Authorization bearer, 30s timeout, retry ×3 w/ 5s backoff). Manual test exec 255: 200 `{ok:true,overdue:0,flagged:0}` (auth accepted, honest empty). Published/activated (version 08872756) → first PRODUCTION scheduled execution observed same increment (18:33 UTC firing, success, same honest response — see commit note for exec id). ⚠️ cosmetic debt: bearer is hardcoded in the HTTP node (MCP can't create n8n credentials) — live session should swap it to an httpBearerAuth cred like the voice-law pattern; also on any CRON_SECRET rotation this node must be updated by hand (noted here as THE registry line for that coupling). No repo code changed (n8n + docs increment).
- **📐 ATLAS REFRESH DUE (2026-07-22, Q24/Task 3.4 close = PHASE 3 FULLY DONE):** Architecture Atlas needs its refresh pass — Phase 3 capture & automation is now complete (Gmail capture, AIDRE webhook, nightly dedup + integrity crons, capture-silence watchdog, cred-expiry alerting, hourly n8n overdue watcher `ozMXSpftU2lIbhp2`). Artifact republish requires the interactive session (Rob's ZERO-mixups standing order).
- [x] Q32. **Task 4.2 dedup/merge (picked 2026-07-22 — next unblocked PRD task; Q15/Q5b still creds-blocked, Q16 Phase-4-gated, Q6 awaiting rep-mockup approval):** pure matcher (already shared from Task 3.5) + merge folding edges/activities/deals onto the surviving person | DoD: per PRD Task 4.2 — two fixture dupes → one record, zero orphaned FKs ✅ **DONE 2026-07-22 (3 increments; not a Q4/Q6/Q9 milestone → no critic-rob gate). DoD PROVEN LIVE ON PROD:** two fixtures (same email different casing, duplicate carrying a phone + an attached activity) → real `POST /api/admin/dedup/merge` → `ok:true, completed:8, folds:{phone}`, **`orphans.total===0` across all 9 FK surfaces**; independently re-verified in Supabase (duplicate row gone, survivor holds the folded phone, fixture activity repointed to survivor — repointed, not orphaned). Fixtures fully cleaned after proof (zero residue, verified). Deployed `26b3f76` + prod-curled (overview 200; empty queue renders no chrome). PRD v3.1.47 same-commit.
  - 2026-07-22 driver (inc.1): MERGE PLANNER DONE as pure code (CR-3) — `lib/dedup/merge.ts` `planPersonMerge(survivor, duplicate, edges, memberships, now)` → exact ordered op list, zero-orphan by construction: every `people(id)` FK in the migrations handled (edges repoint w/ self-edge + survivor-collision deletes and two-dup-edges-collapse-to-one; people/orgs `referred_by_id` blanket repoints AFTER the survivor's own pointer resolves — survivor-referred-by-duplicate inherits the dup's referrer, never a self-reference; org_memberships repoint w/ unique(person,org) collision deletes; deals/activities/tasks blanket repoints; `dedup_review` pair row closed w/ the detector's canonical pair_key; duplicate row deleted LAST). Field folding fills EMPTY survivor fields only, from a contact-only whitelist — money fields structurally unfoldable; duplicate carrying signed/quoted_amount/estimate → plan REFUSES with named blockers (money-record deletion is a Rob call per driver hard limits, never a merge side-effect); demo-*/same-id refused; deterministic (JSON-identical re-plan pinned). 11 tests (`lib/__tests__/dedupMerge.test.ts`), 343/343 vitest, build green, no deploy (pure lib — the planner never auto-runs; merging stays an explicit click). REMAINING: executor route running a plan (admin-gated, transactional-ish with ordered ops + failure surfacing) + merge UI on the dedup review queue → then the DoD two-fixture proof. PRD v3.1.44 same-commit.
  - 2026-07-22 driver (inc.2): EXECUTOR + ENDPOINT DONE — `lib/dedup/executor.ts` `runMergePlan` (ordered fail-fast runner over a structural Supabase slice; first error → exact failedOp + completed count surfaced, later ops never fire; duplicate delete is LAST in every plan so a failed run can't orphan — recovery is simply re-POSTing the pair, planner ops re-run-safe) + `countOrphans` (the zero-orphan DoD gate as live code: all 9 `people(id)` FK surfaces counted for the deleted id; failed count reports −1, never a reassuring zero) + `POST /api/admin/dedup/merge` (load pair+edges+memberships → `planPersonMerge` → 409 w/ blockers on refusal — route NEVER overrides the planner's money/demo guards; `dryRun:true` returns the op list untouched; real run returns completed/folds/orphans). Merging stays an explicit call — nothing invokes this from crons or the detector. 7 tests (`lib/__tests__/mergeExecutor.test.ts`: order, verbatim set/where, fail-fast, orphan coverage pin, −1 honesty), 350/350 vitest, build green. NO deploy (endpoint has no UI consumer yet — nothing user-visible changed; ships with the merge UI increment). REMAINING: merge UI on the dedup review queue (Merge button → dryRun preview → confirm) → DoD two-fixture live proof (orphans.total===0) → tick. PRD v3.1.45 same-commit.
  - 2026-07-22 driver (inc.3, watchdog recovery): MERGE UI DONE — the 17:12 run was killed rc=142 with this increment complete on disk but uncommitted; recovered + re-verified fresh. `components/DedupQueue.tsx` on the overview ledger: real names (GET `/api/admin/dedup` joins names server-side; deleted record → "(record missing)"), confidence badges + evidence, person links. TWO-STEP merge (Apple-not-MSDOS: no raw ids, no one-click destruction): "Keep X" → dryRun preview showing exact op count + which empty fields fold → explicit "Confirm merge"; 409 planner blockers shown verbatim; post-merge shows the LIVE orphan count (0 → "no orphaned records", else "flag Max"); failure copy says re-running is safe (executor guarantee). Org pairs dismiss-only (planner person-scoped); empty queue renders nothing. 350/350 vitest, build green. Deploy + two-fixture DoD proof this same run → then tick. PRD v3.1.46 same-commit.
- [x] Q31. **Task 1.11 AIDRE/AIVA lead-intake payload envelope (picked 2026-07-22 — next unblocked PRD task; Q15/Q5b still creds-blocked, Q16 Phase-4-gated, Q6 awaiting rep-mockup approval; the LAST Task-5.1 definition prereq, natural pick after Q30):** define the `POST /api/leads` payload — product, source, company, vertical, demo dates, assigned rep, stage=New Lead | DoD: per PRD Task 1.11 — payload schema doc handed to Engineering for Task 5.1 ✅ **DONE 2026-07-22 (one increment; not a Q4/Q6/Q9 milestone → no critic-rob gate)**
  - 2026-07-22 driver: shipped AS CODE per CR-3 (Q25/Q27/Q29/Q30 precedent) — pure `lib/leads/intakePayload.ts` IS the handoff: `LeadIntakePayload` maps Task 1.11's field list 1:1 (`product` aidre|aiva; `source_context` = Task 1.15's typed shapes validated by COMPOSING `parseIntakeSourceContext` — one rule source, the two specs can't drift; `company`/`vertical` free text for 5.1 to org-match/registry-map; `demo.requested_at`/`scheduled_for` ISO — requested ≠ booked; `assigned_rep` free text until Phase-4 profiles, omit → Task 1.14 routing decides) + `contact` requiring name AND ≥1 of email/phone (a lead nobody can reach isn't a lead). Stage deliberately NOT a payload field: server pins `INTAKE_STAGE="new_lead"`, and a client-supplied `stage` key REJECTS the whole payload (same anti-smuggling posture as the /deals stage-only patch gate). `parseLeadIntake` reports EVERY problem (1.9/1.15 fix-it contract); unknown extras additive (MC.4 attribution rides later) except `stage`. `INTAKE_WORKED_EXAMPLES` one per product, test-pinned, source_context values reuse 1.15's own pinned examples. **Task 5.1 is now fully definition-unblocked** (1.15 ✅ + 1.11 ✅ — imports `parseLeadIntake` + `INTAKE_STAGE`, only owes per-product bearer tokens + persistence wiring). 11 tests, 332/332 vitest, build green. Narrating doc `docs/plans/LEAD-INTAKE-PAYLOAD-SPEC.md` (code canonical). No deploy (pure lib, zero user-visible change). PRD v3.1.43 same-commit.
- [x] Q30. **Task 1.15 source-context intake spec (picked 2026-07-22 — next unblocked PRD task; Q15/Q5b still creds-blocked, Q16 Phase-4-gated, Q6 awaiting rep-mockup approval):** define the per-source detail fields an intake lead's `source_context` must carry | DoD: per PRD Task 1.15 — field spec with worked examples for ≥3 source types; feeds Tasks 2.1 + 5.1 ✅ **DONE 2026-07-22 (one increment; not a Q4/Q6/Q9 milestone → no critic-rob gate)**
  - 2026-07-22 driver: shipped AS CODE per CR-3 (Q25/Q27/Q29 precedent) — pure `lib/leads/sourceContext.ts` is the canonical spec: typed shapes for all FOUR intake source types (`email_reply` replied-to-subject+reply-text, `web_form` form-name+ordered Q&A pairs, `ad_reel` topic+creative-ref, `trade_show` event+notes; discriminant `source_type`), riding the 0005 `source_context` JSONB seam — zero new schema. `parseIntakeSourceContext` reports EVERY problem (1.9 contract — 400 body doubles as fix-it instructions); extra keys additive by design so MC.4 attribution + Task 1.11 product envelopes compose without conflict. `describeIntakeSource` gives rep surfaces the "how did they get here" one-liner from structured data (successor to repSource.ts's `SOURCE:` description-string convention). `WORKED_EXAMPLES` exported + test-pinned valid for all 4 types (> the ≥3 DoD bar) — integrators import living examples, never copy doc snippets. Deliberate scope: already-live seam shapes (n8n Gmail capture, AIDRE calls, promised_intro, 1.9 manual fields, 4.7 stage audit) keep their own narrower validators at their own ingestion points — catalogued in-module so this file is the one seam map. Unblocks: Task 5.1 `/api/leads` has its source_context validation contract pre-built (Task 1.11's payload envelope = the remaining 5.1 definition prereq → natural next pick). 8 tests, 321/321 vitest, build green. Narrating doc `docs/plans/SOURCE-CONTEXT-SPEC.md` (code canonical). No deploy (pure lib, zero user-visible change). PRD v3.1.42 same-commit.
- [x] Q29. **Task 1.9 mandatory per-interaction fields (picked 2026-07-22 — next unblocked PRD task; Q15/Q5b still creds-blocked, Q16 Phase-4-gated, Q6 awaiting rep-mockup approval):** define + ENFORCE the required fields on every manual interaction log | DoD: per PRD Task 1.9 — save rejected if any required field missing ✅ **DONE 2026-07-22 (one increment; not a Q4/Q6/Q9 milestone → no critic-rob gate)**
  - 2026-07-22 driver: shipped AS CODE per CR-3 (Q25/Q27 precedent) — pure `lib/activities/requiredFields.ts` is the single rule source: date→`occurredAt`, contact→`personId|orgId` (bare dealId NOT enough for a manual log), channel→`type` ∈ call/email/meeting/note (`status_change` stays server-written per Task 4.7), and referral_source / door_opened{opened, by-required-when-true} / next_step{description, due_date} / stage_change riding Task 1.15's `sourceContext` seam (zero new schema). Honest semantics: "none" is a valid ANSWER for referral-source + stage-change — absence is what rejects; stage_change is a rep DECLARATION, the authoritative audit row stays Q28's server-side write. Enforced at the NEW `POST /api/admin/activities` manual-log endpoint (was GET-only): 400 + the FULL missing list in payload-path form (fix-it instructions, not just first failure); non-manual sources 400 (webhooks own those — enforcing 1.9 on n8n/AIDRE capture would kill capture, documented). DoD proven at route level on the Q26 temp-file-store harness: complete log → 201 + persisted; any stripped field → 400 + named + NOT persisted. 16 tests, 313/313 vitest, build green, deployed + prod-curled. Unblocks: rep action-bar "log interaction" form gets its validation contract pre-built. PRD v3.1.41 same-commit.
- [x] Q28. **Task 4.7 stage-change audit trail (picked 2026-07-22 — next unblocked PRD task; Q15/Q5b still creds-blocked, Q16 Phase-4-gated, Q6 awaiting rep-mockup approval; rest of Phase 4 auth-gated behind 4.6f):** auto-log `status_change` activity on stage changes, server-trusted | DoD: per PRD Task 4.7 — one UI stage change → exactly one status_change row ✅ **DONE 2026-07-22 (one increment; not a Q4/Q6/Q9 milestone → no critic-rob gate)**
  - 2026-07-22 driver: pure `buildStageChangeActivity` (lib/crm.ts, CR-3 — deterministic `stage-<dealId>-<ISO>` id = upsert idempotency key; built from server-read before/after, never client input) wired into `/api/admin/deals` PATCH, verified the ONLY live stage-write path (upsertDeal has zero app consumers). Bonus: same-stage drags now no-op with `changed:false` and NO `updated_at` churn — cleans the stage-entry proxy Q25's stage_aging leans on. Audit-write failure → payload `auditError` + server log, never silent. DoD proven on prod: seeded demo-audit-447, PATCH contacted→negotiating → exactly 1 status_change row (source_context {from,to}); repeat PATCH → changed:false, still 1; scaffolding deleted (Q24 precedent). 5 tests pin row shape to the 0005 DDL checks; 297/297 vitest, build green, deployed (prod alias verified via the DoD PATCHes themselves). Follow-up seam on record: todayRules stage_aging can now read TRUE stage-entry from newest status_change instead of updatedAt. PRD v3.1.40 same-commit.
- [x] Q27. **Task 1.8 Referral-Chase Queue (picked 2026-07-22 — next unblocked PRD task; Q15/Q5b still creds-blocked, Q16 Phase-4-gated, Q6 awaiting rep-mockup approval):** spec the promised-intro-passed-no-lead queue | DoD: per PRD Task 1.8 — seeded "promised intro, no lead" record flags; clears when referred lead is logged ✅ **DONE 2026-07-22 (one increment; not a Q4/Q6/Q9 milestone → no critic-rob gate)**
  - 2026-07-22 driver: rules shipped AS CODE per CR-3 (Q25 precedent) — pure `lib/referrals/chaseQueue.ts`: promise = activity carrying `sourceContext.promised_intro {expected_by: "YYYY-MM-DD", of?}` anchored to promiser person/org (rides Task 1.15's sourceContext seam — zero new schema); flags when `expected_by` < Rob's ET day (due-today NOT chased, Task 3.4 convention) and no lead with `referredById === promiser` logged at/after the promise (`Person.referredById` = the existing door-opener pointer). Honest calls: pre-promise leads don't clear a newer promise; timestampless leads never clear (Person type lacks created_at — callers pass `loggedAt` from supabase created_at; documented); demo-* excluded; payload shape-checked (`promisedIntroOf`); deterministic most-overdue-first ordering. DoD met by `lib/__tests__/chaseQueue.test.ts`: the flag+clear DoD pair plus 7 negatives (9 tests; 292/292 vitest, build green). Narrating spec `docs/plans/REFERRAL-CHASE-SPEC.md` (code canonical). Consumer wiring (Task 2.6 worklist band / flags watcher / weekly digest) intentionally future work — this module is their single rule source. No deploy (pure lib, zero user-visible change). PRD v3.1.39 same-commit.
- [x] Q26. **Task 2.6 "/api/tasks/today" endpoint (picked 2026-07-22 — the task Q25 unblocked; Q15/Q5b still creds-blocked, Q16 Phase-4-gated, Q6 awaiting rep-mockup approval):** thin `GET /api/tasks/today` wrapping `whoDoITouchToday` (rules stay in the pure lib per CR-3; route = store rows + `todayInET` clock only) | DoD: seeded fixture returns exact expected task IDs ✅ **DONE 2026-07-22 (one increment; not a milestone → no critic-rob gate)** — route-level test seeds 8 fixtures RELATIVE to real ET-today through the real GET handler on a temp-file store, asserts the exact composite `[rt1, rt2, rm1, rd3]` + 4 negatives excluded; 282/282 vitest, build green; deployed + prod-curled (`count:0` = honest — empty tasks table, 0d-aged deals). Ops note: Vercel CLI had to be upgraded 37.4.0→56.5.0 mid-deploy (old CLI refused the deploy endpoint); new binary lives at `~/.npm-global/bin/vercel` — `/usr/local/bin/vercel` (37.4.0) still shadows it in PATH, future driver runs should use the full path or fix the shadow interactively.
- [x] Q25. **Task 1.7 "Who do I touch today" rules (picked 2026-07-22 as top unblocked work — Q15/Q5b creds-blocked, Q16 Phase-4-gated, Q6 awaiting rep-mockup approval; Phase 3 complete, so first unblocked PRD task):** spec the rep-facing daily-touch rules | DoD: per PRD Task 1.7 — rules doc testable against 10 seeded records covering each trigger ✅ **DONE 2026-07-22 (one increment; not a Q4/Q6/Q9 milestone → no critic-rob gate)**
  - 2026-07-22 driver: rules shipped AS CODE per CR-3 — pure `lib/tasks/todayRules.ts`: `next_step_overdue`/`next_step_due_today` (open task, due ≤ Rob's ET day via the shared `todayInET`), `meeting_unlogged` (meeting >24h old, nothing logged after on the same anchor — a later activity OR task clears it), `stage_aging` (`STAGE_AGING_DAYS` contacted 3d / quote_sent 5d / negotiating 7d; stage entry proxied by `deal.updatedAt` until Task 4.7's audit trail — documented limitation, rule unchanged when 4.7 lands). Clock passed in, never read; demo-* excluded; composite deterministically ordered (overdue → due-today → meetings → aging, stable by id). DoD met by `lib/__tests__/todayRules.test.ts`: 12 seeded records, every trigger + its negatives (6 tests; 281/281 vitest, build green). Narrating spec `docs/plans/TODAY-RULES-SPEC.md` (code canonical; MC.3 reconciled-not-merged — MC.3's builder derives thresholds from `STAGE_AGING_DAYS`, one threshold table two consumers). **Task 2.6 (`/api/tasks/today`) now unblocked — natural next pick when the queue is otherwise blocked.** No deploy (pure lib, zero user-visible change). PRD v3.1.37 same-commit.
- [x] Q33. **Task 4.1 full-text search (picked 2026-07-22 — next unblocked PRD task; Q15/Q5b creds-blocked, Q16 Phase-4-gated, Q6 awaiting rep-mockup approval):** tsvector + GIN migration + search bar on people page | DoD: per PRD Task 4.1 — "polk" returns Jonathan Polk <200ms on seeded data
  - 2026-07-22 driver (inc.2): ✅ CLOSED — search bar LIVE on `/people` (`components/SearchBar.tsx`: debounced 180ms fetch to `/api/admin/search`, stale-response guard via seq counter, ↑/↓ + Enter keyboard nav, Esc/click-away dismiss, honest "No matches" state, company hits badged; every hit routes to `/people/{id}` — org rows share person ids so both kinds deep-link). Deployed + prod-verified: `/people` 200 with the bar in markup; DoD query `polk` → `jonathan-polk` in **57ms** (<200ms) with 5 hits (orgs whose notes mention Polk correctly surface). Build green; no lib logic changed (UI-only inc — vitest suite unchanged from 360/360). PRD Task 4.1 ticked same-commit (rev 3.1.49). Not a milestone (Q4/Q6/Q9 only) → no critic-rob gate.
  - 📐 ATLAS 2026-07-22 (Q33): refresh pass (still pending, see Q24 note) should add the search surface — `0007` search_tsv/GIN on people+orgs → `GET /api/admin/search` → `/people` SearchBar. Artifact republish requires the interactive session.
  - 2026-07-22 driver (inc.1): BACKEND LIVE — `0007_people_search.sql` APPLIED TO PROD via Supabase MCP (generated `search_tsv` + GIN on people AND orgs; 'simple' config — proper-noun corpus; additive DDL, row counts verified untouched 22/18). Query DoD half proven live: `'polk'` → `jonathan-polk` in 0.02ms (limit 200ms; a DEMO row also matches — API layer drops `(DEMO)` rows so it never surfaces). `GET /api/admin/search?q=` + pure `lib/search.ts` (normalize/toHits/mergeHits, deterministic order) + migration-sync gate test (parses 0007, fails on SEARCH_COLUMNS drift). 10 tests, 360/360 vitest, build green. NOT deployed (route has no UI consumer — nothing user-visible changed). REMAINING (inc.2): search bar on `/people` wired to the route (or instant client filter backed by it), deploy, prod-curl DoD → tick.
- [x] Q34. **Task 4.3 CSV import/export with dedup-on-import (opened 2026-07-22 — next unblocked PRD task; Q15/Q5b creds-blocked, Q16 Phase-4-gated, Q6 awaiting rep-mockup approval):** export + import routes, import runs the Task 3.5/4.2 matcher so dupes are flagged, never silently created | DoD: per PRD Task 4.3 — 100-row CSV imports clean; dupes flagged, never silently created ✅ **CLOSED 2026-07-22 (inc.3; not a Q4/Q6/Q9 milestone → no critic-rob gate)**
  - 2026-07-22 driver (inc.3): ✅ CLOSED — `/people` Export/Import buttons LIVE (`components/CsvButtons.tsx`: export = direct `/api/admin/export` download; import = file picker → dry-run plan panel showing N new / dupes with line+matchId+signals / errors by line, **nothing written until the explicit confirm** — same two-step as the dedup-merge UI). Deployed (`dpl_8p2FNRWnUrJNy7QaXhoSaQUDJuWA`, full-path `~/.npm-global/bin/vercel`); prod `/people` 200 with both buttons in markup. **DoD prod-proven zero-write:** (a) 100-row generated CSV POSTed dry-run → `inserts:100, dupes:0, errors:0`, committed:false — commit path itself proven by the 13-test suite (incl. 100-row commit on temp store); prod ledger deliberately NOT polluted with fixtures (hard limit: no record deletes without Rob's word, so no insert-then-clean); (b) full prod export re-imported → **all 34 rows dupe-flagged (`id-exact`/ledger), 0 inserts** — "never silently created" proven against live data. Build green, 384/384 vitest (UI-only inc — suite unchanged). PRD Task 4.3 [x] + rev 3.1.52 same-commit (header version also repaired 3.1.43→3.1.52, was stale-lagging the table).
  - 📐 ATLAS 2026-07-22 (Q34): refresh pass (still pending, see Q24/Q33 notes) should add the CSV surface — `lib/csv.ts` + `lib/csvImport.ts` (pure) → `GET /api/admin/export` + `POST /api/admin/import` (dry-run/commit) → `/people` CsvButtons. Artifact republish requires the interactive session.
  - 2026-07-22 driver (inc.1): EXPORT HALF DONE — pure `lib/csv.ts` (CR-3: `PEOPLE_CSV_COLUMNS` fixed header, RFC-4180 `csvEscape`, `peopleToCsv` — drops `(DEMO)` rows per the isDemo convention, name A→Z + id-tiebreak deterministic ordering, CRLF for clean Excel open; `parseCsv` full RFC-4180 parser (quoted commas/newlines/doubled quotes, LF+CRLF, interior blank lines KEPT so import can report them by line number) ready for the import half) + `GET /api/admin/export` (store I/O only via `getStore().getNetwork()`; text/csv attachment `mle-people.csv`, people+orgs in one file with a `kind` column matching the ledger view). 11 new tests incl. export→parse round-trip; **371/371 vitest, build green**. NOT deployed (route has no UI consumer yet — nothing user-visible changed). REMAINING: import route (parse → header-map → matcher dedup-check → insert clean / flag dupes) → 100-row DoD proof → export/import buttons on `/people` → deploy → tick.
  - 2026-07-22 driver (inc.2): IMPORT HALF DONE — pure `lib/csvImport.ts` (CR-3 planner `planImport(csv, existing)`: header-map tolerant of column order/subset but `name` required; per-line errors — blank lines, bad column counts, missing name, invalid status, dangling `referredById` (would be a broken edge/FK reject); Task 3.5/4.2 matcher (`findDuplicatePairs`) run over ledger + candidates in one pass, so dupes are flagged vs live data AND intra-file (earlier line wins) — **never silently created**; id collision w/ ledger = dupe not edit (import never overwrites; money/signed fields aren't even columns in the format); slug ids minted + de-collided; demo rows excluded from matching but their ids still block slug reuse) + `POST /api/admin/import` (dry-run default returns full plan, `?commit=1` executes inserts via `upsertPerson` — same two-step confirm pattern as the dedup merge UI; store I/O only). 13 new tests: 100-row clean import (planner half of the DoD), export→import round-trip (every row id-exact dupe-flagged, zero inserts), matcher flags by email/name/phone, intra-file dupes, per-line error reporting. **384/384 vitest, build green.** NOT deployed (no UI consumer yet — nothing user-visible changed). REMAINING (inc.3): export/import buttons on `/people` → deploy → prod 100-row DoD proof → tick.
- [x] Q37. **Task 5.2 rate-limit + idempotency on `/api/leads` (opened 2026-07-22 — next unblocked PRD task, flagged by Q36 close; Q15/Q5b creds-blocked, Q16 Phase-4-gated, Q6 awaiting rep-mockup approval):** optional `Idempotency-Key` header + per-product sliding-window rate limit on the live intake endpoint | DoD: per PRD Task 5.2 — same idempotency key twice → one person, one deal, one activity ✅ **CLOSED 2026-07-22 (one increment; not a Q4/Q6/Q9 milestone → no critic-rob gate)**
  - 2026-07-22 driver: ✅ CLOSED — no side table, no drift: with an `Idempotency-Key` (`[A-Za-z0-9_-]{1,100}`, product-scoped so AIDRE's "abc" ≠ AIVA's "abc"), deal/activity ids derive from `(product, key)` — **the deal row itself is the idempotency record**: a retry finds `lead-<product>-<key>` via `store.listDeals()` BEFORE any write and returns `200 {replayed:true}` touching nothing (DoD is structural, not best-effort). Malformed key → 400 pre-write; no header → exact 5.1 behavior. Rate limit: pure `lib/leads/rateLimit.ts` (CR-3 — clock injected, caller owns state; 60/min per product, counted post-auth so an unauth flood can't starve a real caller's quota) → 429 + Retry-After; honest limitation documented: per-instance memory = abuse braking, not billing-grade quota (also why the 429 is proven in-suite, not by hammering prod across instances). 6 new tests (replay DoD writes-nothing, product-scoped keys, malformed-key 400, 61st-request 429 + per-product isolation, pure window-slide), **415/415 vitest, build green.** Deployed (`mle-rob-dashboard-cic75aell`, Ready); prod-proven zero-write: no token → 401 intact; live AIDRE key + malformed Idempotency-Key → 400 naming the header (gate live); valid key + bad payload → 400 report-everything intact. Spec doc gains "IDEMPOTENCY + RATE LIMIT" section (retry contract for the AIDRE/AIVA repos: send the header on every retry-able submit; without it retries WILL duplicate — on the caller). PRD Task 5.2 [x] + rev 3.1.58 same-commit. Next 5.x: Task 5.3 enrichment refresh (Operations) or 5.5 routing engine (Engineering — needs Task 1.14 decision table check).
- [x] Q36. **Task 5.1 `POST /api/leads` AIDRE/AIVA intake endpoint (opened 2026-07-22 — next unblocked PRD task; Q15/Q5b creds-blocked, Q16 Phase-4-gated, Q6 awaiting rep-mockup approval; Q31 declared 5.1 fully definition-unblocked: 1.15 ✅ + 1.11 ✅):** per-product bearer tokens → parseLeadIntake → match-or-create person + deal at intake stage + intake activity | DoD: per PRD Task 5.1 — AIDRE test key creates person+deal with source details populated; missing/wrong token → 401 ✅ **CLOSED 2026-07-22 (inc.3; not a Q4/Q6/Q9 milestone → no critic-rob gate)**
  - 2026-07-22 driver (inc.3): ✅ CLOSED — endpoint LIVE + ARMED. Generated `LEADS_KEY_AIDRE`/`LEADS_KEY_AIVA` (openssl 24-byte hex, product-prefixed), stored in Vercel prod env (Sensitive) + `.env.local` (gitignored — never in git); deployed (`mle-rob-dashboard-3lfsirixr`, Ready). **DoD prod-proven, all ZERO-WRITE** (every failing check fires before any store access — verified in route source): (a) no token → 401; (b) wrong token → 401 (DoD); (c) live AIDRE key + empty payload → 400 listing every problem (key armed, report-everything contract live); (d) cross-product: AIVA key + fully-VALID aidre payload → 401 `token not valid for product "aidre"` — scoping proven against prod. Create path (person+deal+activity w/ source details, both worked examples) stays proven in-suite per Q34 "prod not polluted" precedent — 410/410 vitest, build green. Key handoff section appended to `docs/plans/LEAD-INTAKE-PAYLOAD-SPEC.md` (endpoint URL, call shape, key location/rotation — key VALUES only in Vercel env + .env.local; copy into AIDRE/AIVA repos' own env when their send side is built). PRD Task 5.1 [x] + rev 3.1.57 same-commit. Follow-on: Task 5.2 rate-limit + idempotency is the next 5.x.
  - 📐 ATLAS 2026-07-22 (Q36): refresh pass (still pending, see Q24/Q33/Q34/Q35 notes) should add the lead-intake surface — `lib/leads/{sourceContext,intakePayload,intakePlan,intakeAuth}.ts` (pure chain) → `POST /api/leads` (bearer-armed) → person/deal/activity writes + flags on unmatched vertical. Artifact republish requires the interactive session.
  - 2026-07-22 driver (inc.2): ROUTE DONE — `app/api/leads/route.ts` + pure `lib/leads/intakeAuth.ts` (CR-3): bearer keys `LEADS_KEY_AIDRE`/`LEADS_KEY_AIVA` from env (none set → 503, fully inert — Vapi/AIDRE gating parity), token→product scoping (cross-product submit = 401: an AIVA key can never post an AIDRE lead), missing/wrong token 401 (DoD), bad JSON/payload 400 w/ every problem listed (1.9/1.11 contract), then the route executes inc.1's plan VERBATIM: create person or whitelist-fills upsert on match, deal pinned new_lead, one intake activity w/ source_context+product; `verticalUnmatched` auto-files a low-severity flag to Things to Address (findings protocol; non-fatal best-effort). 8 route tests incl. BOTH worked examples end-to-end (aidre→match+fills path, aiva→create path w/ [lead: aiva] tag + source=api), 410/410 vitest, build green. NOT deployed (keys unset everywhere → endpoint would be 503-inert; no user-visible surface). REMAINING (inc.3): generate per-product keys → vercel env → deploy → prod DoD (401 wrong/missing live; create path stays in-suite per Q34 "prod not polluted" precedent) → key handoff note for AIDRE/AIVA repos → tick. PRD v3.1.56 same-commit.
  - 2026-07-22 driver (inc.1): PLANNER DONE as pure code (CR-3, Q32/Q34 planner-first precedent) — `lib/leads/intakePlan.ts` `planLeadIntake(payload, ledger, verticals, now)` → exact plan the route will execute verbatim: match-or-create person via the shared Task-3.5 matcher (email/phone-exact ONLY attaches; name-only collision CREATES a new person — never auto-attach to Rob's real network, the nightly dedup review queue is the designed net; demo rows excluded); match fills EMPTY contact fields only from a phone/email/role/business whitelist (money/status structurally unreachable — not in the list); deal pinned at `INTAKE_STAGE` (client `stage` already rejected by parseLeadIntake); one intake activity carrying source_context+product+demo dates (`aidre` → aidre source, AIVA rides `api` — ActivitySource has no aiva value, product truth lives in sourceContext); vertical free text → registry map by id/normalized name w/ honest `verticalUnmatched` (never guessed); `[lead: product]` notes tag (Q35 convention); ids deterministic from (personId, now) — true idempotency = Task 5.2. 9 tests (create/match/normalization/demo-exclusion/de-collision/whitelist-pin/determinism), 402/402 vitest, build green, no deploy (pure lib, zero user-visible change). REMAINING: route (bearer auth LEADS_KEY_AIDRE/LEADS_KEY_AIVA env-gated → parse → plan → execute via store, 401 wrong/missing, 503 unconfigured) → deploy → prod DoD proof w/ test key → clean scaffolding → tick. PRD v3.1.55 same-commit.
- [x] Q35. **Task 4.4 CSV import pipeline UX for Rob's real lists (opened 2026-07-22 — next unblocked PRD task; Q15/Q5b creds-blocked, Q16 Phase-4-gated, Q6 awaiting rep-mockup approval):** field-mapping template + validation + tagged insert on top of Q34's import machinery | DoD: per PRD Task 4.4 — 50-row sample w/ 3 planted dupes → 47 clean + 3 to review, zero silent drops ✅ **CLOSED 2026-07-22 (inc.2; not a Q4/Q6/Q9 milestone → no critic-rob gate)**
  - 2026-07-22 driver (inc.2): ✅ CLOSED — UX half LIVE (`components/CsvButtons.tsx` plan panel now shows, BEFORE any commit: "Recognized: Company → business · Cell Phone → phone · …" mapping line (non-identity remaps only — canonical files stay quiet), amber **"Not imported (no matching field): Fax"** ignored-columns line (the report inc.1 refused to ship without), and an optional tag input riding commit as `?tag=` → `[import: tag]` stamped in clean inserts' notes). Deployed (`mle-rob-dashboard-7rjnlniyp`, Ready). **DoD prod-proven ZERO-WRITE:** 50-row real-header CSV (First/Last/Company/Cell/Email/Fax) POSTed dry-run with 3 dupes planted vs LIVE records — email-exact→`dix-thedevdix`, differently-formatted phone `904.609.7180`→`cg-roofing-group`, First+Last combine→`michael-jaenvega` — → **47 inserts + 3 dupes + 0 errors (sum 50), Fax in ignoredColumns, all 5 real headers mapped, committed:false/inserted:0**; prod export re-parsed as CSV after: **34 records, 0 fixtures** (⚠️ lesson: `wc -l` on the export lies — RFC-4180 quoted newlines in notes make 34 records span 101 physical lines; always parse). 393/393 vitest (UI-only inc), build green. PRD Task 4.4 [x] + rev 3.1.54 same-commit.
  - 📐 ATLAS 2026-07-22 (Q35): refresh pass (still pending, see Q24/Q33/Q34 notes) should extend the CSV surface with the mapping layer — `lib/csvMapping.ts` (pure, wraps csvImport planner) → import route `?tag=` → plan panel mapping/ignored/tag UI. Artifact republish requires the interactive session.
  - 2026-07-22 driver (inc.1): MAPPING PIPELINE DONE, pure lib per CR-3 — `lib/csvMapping.ts` composes AROUND the untouched Task-4.3 planner: real-world header alias template ("Company"→business, "Cell Phone"→phone, "Email Address"→email, …; "stage" deliberately unaliased — real stage values aren't lit/warm/unlit, an ignored-column report beats 50 bogus errors), First+Last→name combine (explicit name wins), duplicate-source columns first-wins, EVERY original header lands in `mapping` or `ignoredColumns` (zero-silent-drops covers columns too), rows rewritten 1:1 so plan line numbers still point at Rob's file; `planRealImport(..., {tag})` stamps clean inserts `[import: tag]` into notes (attributable; dupes never stamped). `POST /api/admin/import` now runs the mapper + `?tag=`, returns mapping/ignoredColumns — canonical CSVs identity-map, Task 4.3 round-trip re-proven in-suite. **DoD case proven in-suite:** 50-row First/Last/Company/Cell/Email/Fax sample, 3 planted dupes (email; differently-formatted phone; combined name) → 47 inserts + 3 dupes + 0 errors, buckets sum to 50, Fax reported ignored. 9 new tests, **393/393 vitest, build green.** NOT deployed — the `/people` plan panel doesn't render ignoredColumns yet; shipping now would import real lists while hiding the ignored-column report (a UX silent drop). REMAINING (inc.2): plan panel shows mapping + ignored columns + tag input → deploy → prod DoD proof (dry-run only, zero-write like Q34) → tick.
- [x] Q38. **Task 5.4 dead-lead recycling (opened 2026-07-22 — next unblocked PRD task; Q15/Q5b creds-blocked, Q16 Phase-4-gated, Q6 awaiting rep-mockup approval):** no-activity-180-days contacts auto-tag `recycle_candidate`, surfaced in weekly digest | DoD: per PRD Task 5.4 — seeded stale contact appears in next weekly digest recycle section (⚠️ digest infra = MC.15/M4.3, not built yet — tag + surfacing path must be honest about that when this ticks) ✅ **CLOSED 2026-07-22 (inc.3; not a Q4/Q6/Q9 milestone → no critic-rob gate). HONEST SCOPE ON THE TICK: tag + interim surfacing (flags) + weekly schedule are DONE and prod-proven; the literal "appears in weekly digest section" rides MC.15/M4.3 (digest doesn't exist for ANY content yet) — MC.15 now carries the cross-ref to consume `hasRecycleTag` when it builds.**
  - 2026-07-22 driver (inc.3): ✅ CLOSED — both remaining pieces done. **(1) Write path exercised LIVE (Q24 seeded-DoD precedent, zero fiction left behind):** seeded stale non-demo person `wtest-q38-recycle` (created_at 2025-06-01, unsigned, unlit, no activities) → armed run 1 `{candidates:1,tagged:1,flagged:1}`; verified on prod: notes got exactly ` [recycle_candidate 2026-07-22]` appended (signed/status byte-untouched), flag #15 filed (low, entity=person, detail honestly names flags as the interim surfacing + MC.15 gate, "416d stale"); run 2 `{candidates:0}` — tag-as-idempotency proven LIVE, not just in-suite. Scaffolding cleaned (person + flag #15 deleted, 204s), final run honest `{candidates:0,tagged:0}`, people count back to 22 (+12 orgs = the 34 nodes). **(2) n8n weekly schedule LIVE:** workflow `EsDSJQoJwzLkJOhR` "MLE CRM — Weekly Dead-Lead Recycle Tagger" (Schedule Trigger Mondays 09:33 → HTTP GET /api/cron/recycle, Authorization bearer, 30s timeout, retry ×3 w/ 5s backoff — byte-for-byte the Q24 overdue pattern, cloned from `ozMXSpftU2lIbhp2`). Manual exec 267: 200 `{ok:true,candidates:0}`. Published/activated (version 22e4f29f); first scheduled firing Mon 2026-07-27 09:33 (weekly cadence — can't observe one this increment, unlike Q24's hourly; exec list checkable then). ⚠️ same cosmetic debt as Q24: bearer hardcoded in the HTTP node (MCP can't create n8n creds) — live session should swap BOTH watchers to an httpBearerAuth cred; any CRON_SECRET rotation must update `ozMXSpftU2lIbhp2` AND `EsDSJQoJwzLkJOhR` by hand (this line + Q24 inc.3 = the registry for that coupling). No repo code changed (prod-proof + n8n + docs increment). PRD Task 5.4 [x] + MC.15 cross-ref + rev 3.1.63 same-commit.
  - 📐 ATLAS 2026-07-22 (Q38): refresh pass (still pending, see Q24/Q33/Q34/Q35/Q36 notes) should add the recycle surface — `lib/leads/recycle.ts` (pure detector + single-source tag) → `GET /api/cron/recycle` (bearer) → notes tag + low flags; n8n weekly `EsDSJQoJwzLkJOhR` alongside hourly `ozMXSpftU2lIbhp2`. Artifact republish requires the interactive session.
  - 2026-07-22 driver (inc.2): TAGGING ROUTE LIVE — `GET /api/cron/recycle` (overdue-watcher pattern: CRON_SECRET bearer contract, 503 unset / 401 wrong; weekly firing = n8n schedule, Vercel Hobby 2-cron cap). Executes `findRecycleCandidates` verbatim; per candidate: notes-only append via the single-source `withRecycleTag` (money/signed/status structurally untouched — only `notes` in the update payload), then a LOW flag on the person (entity_id+name → shows on their record page + Things to Address; detail states honestly that flags ARE the surfacing until MC.15's digest exists). The tag is the idempotency: tagged people are excluded by the detector, so re-runs/retries can't double-tag or re-flag; flag failure never undoes a tag (reported in `errors`, ok:false). Gate tests (`recycleCron.test.ts`, dedupCron pattern), 426/426 vitest, build green, deployed (`mle-rob-dashboard-ac3i2gkgt`). **Prod-proven:** wrong + missing bearer both 401 live; armed run → `{ok:true, staleDays:180, candidates:0, tagged:0, flagged:0}` — zero candidates is CORRECT (every real row has a provable touch within the last month; zero-write). REMAINING (inc.3): provision n8n weekly schedule workflow (N8N_KEY, same pattern as overdue's hourly) + exercise the write path (in-suite supabase mock or seeded stale non-demo row, then clean) — DoD's digest section itself stays gated on MC.15/M4.3 and the tick must say so. PRD v3.1.60 same-commit.
  - 2026-07-22 driver (inc.1): DETECTOR DONE as pure code (CR-3, Q27/Q32/Q34 planner-first precedent) — `lib/leads/recycle.ts` `findRecycleCandidates(people, activities, today)`: candidate when newest provable touch (activity anchored by personId OR the person's orgId; any keyDates milestone; caller-passed created_at — Person type lacks it, chaseQueue ReferredLead convention) is >=180d old (exactly 180 qualifies — 180 full days of silence; boundary pinned in tests). NEVER candidates: demo-*, signed people, lit status (clients/actively-referring aren't dead leads), contacts already carrying the `[recycle_candidate YYYY-MM-DD]` notes tag (cron idempotency), and contacts with NO provable date (honest-over-convenient — can't prove staleness without a date, chaseQueue precedent). Tag format has ONE source: `withRecycleTag`/`hasRecycleTag` exported here so inc.2's cron write can't drift. Clock injected; deterministic most-stale-first ordering. 9 tests, 424/424 vitest, build green, no deploy (pure lib, zero user-visible change). REMAINING: inc.2 cron route (overdue-watcher pattern: CRON_SECRET bearer, n8n schedule) applies tags via notes append + files low-severity flags to Things to Address (findings protocol — interim surfacing until MC.15 digest exists, stated honestly in the tick); digest recycle section itself lands with MC.15.
- [ ] Q15. **Vapi hybrid — AI inbound receptionist (Rob #40 locked HYBRID, the 93.5 pick):** import our Twilio number(s) into Vapi via SIP, build the receptionist assistant (rep-assistant persona), wire the pre-answer `assistant-request` webhook + mid-call custom tool → our API for instant caller→CRM lookup/screen-pop payload, route rep-bound calls human-first (AI only on miss), call artifacts → activities-ready payload (rides Task 2.1 activities table like the Twilio recording webhook). Env-gated like Q5b: VAPI_* unset → nothing changes. Needs: Vapi account + key (Rob or Max-provisioned on Rob's card — ask in PING-INBOX), Twilio creds (same ask as Q5b) | DoD: missed/inbound call on the Twilio number answered by the assistant, caller-lookup tool fires against our CRM data, human-first routing verified, zero behavior change with env unset
  - 2026-07-22 driver: CRM-SIDE WIRING DONE — `lib/vapi.ts` (pure, unit-tested: env gate on VAPI_WEBHOOK_SECRET, timing-safe x-vapi-secret check, US last-10-digit phone normalize, caller→CRM lookup resolving vertical + referrer, flat callerContext payload doubling as Vapi variableValues AND rep screen-pop, assistant-request response w/ VAPI_ASSISTANT_ID + honest error channel when unset, crm_caller_lookup tool-call handler w/ string/object args + live-caller fallback) + `POST /api/webhooks/vapi` (one server-URL endpoint: assistant-request pre-answer, tool-calls mid-call, end-of-call-report logged for artifact recovery until Q9 activities; 503 unconfigured / 403 bad secret; sits under the already-public /api/webhooks/* proxy carve-out with its own auth). 14 new tests, 53/53 vitest, build green. NOT deployed (env unset → inert; ships with next user-visible deploy or when creds land). REMAINING: Vapi account+key + Twilio creds (PING-INBOX ask stands), assistant built in Vapi dashboard (VAPI_ASSISTANT_ID), SIP-import Twilio number, human-first routing config, live-call DoD verification.
  - 2026-07-22 driver (2): ASSISTANT-AS-CODE DONE (CR-3 — no dashboard clicking): `scripts/provision-vapi-assistant.mjs` holds the full receptionist definition (rep-assistant persona w/ caller-context {{variables}}, human-first first-message, `crm_caller_lookup` tool schema, server URL + x-vapi-secret wiring, tool-calls/end-of-call-report serverMessages) + an idempotent provisioner: create-or-update by name via Vapi API, prints VAPI_ASSISTANT_ID + SIP next steps; without VAPI_API_KEY prints the ask and exits 1 (verified — no-op today). 4 new tests pin the payload to the webhook handler (tool-name round-trips through toolCallResults; every prompt variable ⊆ callerContext keys; secret wiring). 57/57 vitest, build green, no deploy (inert w/o creds). Q15 critical path is now just: creds land → `node scripts/provision-vapi-assistant.mjs` → SIP-import number → live-call DoD.
- [ ] Q5b. Task 7.2 scaffold: twilio-voice.js click-to-dial — token route (/api/twilio/token), recording webhook (/api/webhooks/twilio-recording → activities-ready payload), Call button wiring in rep cockpit; ALL env-gated (TWILIO_* unset → buttons stay tel: links, zero breakage). Creds ask lives in PING-INBOX for Rob's morning | DoD: with test creds a browser call connects + recording webhook lands; without creds, current behavior unchanged
  - 2026-07-21 driver (8): SERVER SIDE DONE — `lib/twilio.ts` (pure, unit-tested: HS256 voice-token mint w/ no SDK dep, X-Twilio-Signature HMAC-SHA1 validation w/ timingSafeEqual, recording→activity payload mapper, outgoing-call TwiML w/ XML escaping) + 3 env-gated routes: GET /api/twilio/token (503 unconfigured), POST /api/twilio/voice (TwiML: <Dial> dual-channel record → our webhook; To validated), POST /api/webhooks/twilio-recording (503 w/o auth token — never accepts unsigned; 403 bad sig; logs activities-ready payload until Q9 lands the activities table). 12 new tests, 27/27 vitest, build green. NOT deployed (zero user-visible change while unset). PING-INBOX Twilio ask updated w/ exact env names — Rob owes only Account SID + Auth Token + number; Max mints API key + TwiML app from those. NEXT increment: rep-cockpit Call button (twilio-voice.js client, tel: fallback when /api/twilio/token 503s).
  - 2026-07-21 driver (14): ⚠️ ALSO CONTESTED — Rob (dump 7.21.26-1 #2) is "not going with your recommendations for Inbound and Outbound Calls unless you can convince me otherwise": wants Vapi (or cheaper LiveKit) evaluated, integrated so ONE stack captures rep outbound + inbound routing + missed-call digital receptionist that acts as the rep's assistant and does instant caller-lookup → context pull. Re-eval is Q12. Scaffold stays (env-gated, zero prod risk); no further 7.2 build until Q12 resolves.
  - 2026-07-22 driver: **UNPAUSED — HYBRID locked** (Rob #40 delegated the pick → Q12's Twilio+Vapi 93.5 recommendation stands). This item = the human click-to-dial half, blocked ONLY on Rob's Twilio creds (PING-INBOX ☎️). Vapi receptionist half is Q15 above.
  - 2026-07-21 driver (9): CLIENT SIDE DONE — `components/CallButton.tsx` replaces the cockpit tel: anchor. One availability probe per page load (module-level memoized fetch of /api/twilio/token); SSR + 503 + any dial/mic/SDK failure all render the byte-identical pre-dialer tel: link, so with TWILIO_* unset nothing changes. On 200: fresh token per call, @twilio/voice-sdk dynamic-imported only then (zero bundle cost while unconfigured), Call → Calling…/Hang up states, device destroyed on disconnect/unmount. Build green, 27/27 vitest, deployed vercel --prod; prod curls: /api/twilio/token 503, /rep 200 with tel: links rendering, /api/network intact. Q5b now blocked ONLY on Rob's Twilio creds (PING-INBOX) for the live-call DoD — leave unticked; next driver moves to Q4b unless creds land.
- [ ] Q16. **Alias `/api/rep/*` in front of the admin API routes** (Critic Rob gate 2026-07-22, punch #10 on Task 1b.3 — noted, not built, per Rob's explicit call: "item 10 just gets a BUILD-QUEUE note, not a build"): rep client code (`RepAccountsList`, `ActivityTimeline`, the inline-edit kit on `/rep/accounts/*`) currently calls `/api/admin/activities` and PATCHes `/api/admin/people` directly — fine for the DEMO mockup (no real reps, no auth boundary yet), but before Phase-4 real reps land, thin `/api/rep/*` routes should front these so a rep's bundle/network tab never references an `/api/admin/*` path at all | DoD: `/api/rep/activities` + `/api/rep/people` (or similar) proxy to the same logic with a rep-scoped auth check once Phase-4 ACCESS (Q6) lands; rep client code repointed; zero admin-path references in the rep bundle
- [x] Q23. **Task 3.6 capture-silence watchdog (picked 2026-07-22 as top unblocked work — Q15/Q5b creds-blocked, Q16 Phase-4-gated, Q6 awaiting rep-mockup approval):** n8n Error Trigger workflow → `/api/webhooks/n8n-error` → flags ledger within seconds (the 15-min DoD the nightly cron can't meet) | DoD: per PRD Task 3.6 — forced workflow error (bad credential) alerts Rob within 15 min ✅ **DONE 2026-07-22 (2 increments; not a Q4/Q6/Q9 milestone → no critic-rob gate)**
  - 2026-07-22 driver: CRM-SIDE RECEIVER DONE — pure `lib/integrity/captureAlert.ts` (payload→flag mapping incl. the polling-trigger variant where a bad Gmail credential means no execution ever starts — exactly the forced-error DoD case; deterministic per-workflow-per-day flag title = idempotency: failure storm → 1 flag, still broken tomorrow → re-alert) + `POST /api/webhooks/n8n-error` (secret contract shared with n8n-email: 503 unarmed / 403 bad x-n8n-secret / always-200 after auth so n8n never retry-loops; title-dedupe vs flags before insert, same contract as /api/cron/integrity). 6 tests, 260/260 vitest, build green, NOT deployed (inert until n8n wiring). NEXT increment: deploy → provision n8n Error Trigger workflow (N8N_KEY, same httpHeaderAuth cred as Gmail capture) → set `errorWorkflow` on workflow `JnIJiCbOqSaK8uN2` → forced-bad-credential live test → tick.
  - 2026-07-22 driver (inc.2): **✅ Q23 CLOSED — full n8n path LIVE and DoD proven on prod.** Recovery increment: three prior headless runs died rc=142 (timeout) mid-wiring; this run picked up the partial state and finished it. Deployed (endpoint live on prod: bare POST → 403 armed). n8n side: error-alarm workflow `VoOFOPGqObGWe5Jr` ("MLE CRM — Error Alarm → flags ledger") active — Error Trigger → HTTP POST to `/api/webhooks/n8n-error` w/ the SAME httpHeaderAuth cred as Gmail capture (`2sHDFkgzMWkOvhv7`); `errorWorkflow` set on Gmail capture `JnIJiCbOqSaK8uN2`. **Forced-error DoD proven live:** DOD-TEST workflow (schedule-every-minute → throwing Code node, errorWorkflow→alarm) failed once a MINUTE from 16:02Z; first failure's flag landed on the ledger within seconds (16:02:18Z), and 12+ alarm executions produced exactly ONE flag — the storm-idempotency claim proven on prod, not just in tests. Scaffolding cleaned: DOD-TEST workflow deactivated+deleted, "delete me" flag deleted, honest final = 0 capture flags; Gmail capture untouched & active. Cosmetic note: the capture workflow ALSO carries an in-workflow Error Trigger→POST pair from a killed partial run — dead code while `errorWorkflow` points at the separate alarm (and the receiver dedupes regardless); left in place rather than risk a full PUT on Rob's live capture pipeline. PRD v3.1.33 + FAILURE-MODES row 5 → LIVE, same commit. P3 now 7/8 (only 3.4 n8n overdue watcher remains).
  - 📐 ATLAS 2026-07-22 (Q23): Atlas refresh pass still pending (see Q20 note) — add: n8n error-alarm workflow `VoOFOPGqObGWe5Jr` (+ `errorWorkflow` on Gmail capture) → `POST /api/webhooks/n8n-error` → flags ledger, the real-time leg of the failure-mode map. Artifact republish requires the interactive session.
- [x] Q19. **Task 3.3 AIDRE call-outcome webhook (picked 2026-07-22 as top unblocked work — Q15/Q5b creds-blocked, Q16 note-only per Rob, Q6 awaiting mockup approval):** receiver stub `/api/webhooks/aidre-call` → `activities` type=call source=aidre + payload schema doc | DoD: per PRD Task 3.3 ✅ **DONE 2026-07-22 (one increment; not a Q4/Q6/Q9 milestone → no critic-rob gate)**
  - 2026-07-22 driver: `lib/aidreCall.ts` (pure) + route, mirroring the n8n-email/Vapi seam pattern: env-gated (`AIDRE_WEBHOOK_SECRET` unset → 503, fully inert — true stub, zero behavior change today), constant-time x-aidre-secret, phone match via the SAME Vapi last-10-digit matcher (one phone-matching behavior CRM-wide), deterministic `aidre-call-<callId>` id (AIDRE retries upsert, never duplicate), company-number match anchors orgId-only (0005 ≤1-anchor check), unmatched caller → 200 `ingested:false` zero rows (no anchorless writes; unmatched-call intake deferred to Task 5.1 routing). Payload contract delivered: `docs/plans/AIDRE-CALL-PAYLOAD-SPEC.md` (endpoint, auth, field table, retry semantics, response codes — hand to digi-rec-roi-dual-demo when its outbound webhook lands). DoD's synthetic POST proven at the ROUTE level: new minimal `vitest.config.ts` maps `@/` → repo root so tests can import route handlers (unlocks route-testing repo-wide; the old "vitest has no runtime alias" workaround is retired). 11 new tests (216/216), build green, NO deploy (inert endpoint, no user-visible surface — ships with the next deploy). REMAINING (not this task's DoD): AIDRE-side webhook sender doesn't exist yet; when it does → mint secret, set Vercel env, prod-curl the four gates like Q8 inc.3.
- [x] Q20. **Task 3.5 nightly dedup detector (picked 2026-07-22 as top unblocked work — Q15/Q5b creds-blocked, Q16 note-only per Rob, Q6 awaiting mockup approval):** pure matcher → `dedup_review` queue (no auto-merge) + nightly run | DoD: per PRD Task 3.5
  - 2026-07-22 driver (inc.1): matcher core DONE — `lib/dedup/match.ts` `findDuplicatePairs` (email/phone/name, exact-after-normalization ONLY; deliberately no fuzzy matching so "similar-but-distinct names don't surface" is structural, not tuned; phone reuses `normalizePhone` from lib/vapi.ts = one last-10-digit rule CRM-wide; email/phone → high confidence, name-only → review; deterministic order + per-signal evidence lines ready for the queue). 12 tests pin both DoD behaviors + edges, 228/228, build green, no deploy (pure lib). Task 4.2's merge UI consumes the same module. REMAINING: nightly run wiring (n8n cron vs vercel cron — decide) + `dedup_review` queue persistence/surface.
  - 2026-07-22 driver (inc.2): **queue persistence DONE** — Supabase `dedup_review` table (pair_key PK, evidence[], status open/dismissed/resolved) + `/api/admin/dedup`: POST detector-run upserts idempotently (status never in payload → dismissed pairs can NEVER resurface; open pairs no longer detected auto-resolve so the queue never shows an already-fixed dup), GET list, PATCH dismiss/reopen w/ 0-row→404 truth gate. Within-kind matching only via pure `lib/dedup/run.ts` (person↔person, org↔org — shared company phone ≠ duplicate); DEMO exclusion = completeness-scorer rule. 7 tests (235/235), build green; live-verified vs prod Supabase (honest 0 dups on the real 32, dismiss round-trip + rerun-survival proven, synthetic row cleaned). No deploy (API-only). **Cron DECIDED: Vercel cron** (same-deploy, no n8n dep). REMAINING: `vercel.json` nightly cron → CRON_SECRET-gated route + proxy `isPublicPath` exemption → tick Q20.
  - 2026-07-22 driver (inc.3): **✅ Q20 CLOSED — nightly cron LIVE on prod.** `vercel.json` cron `0 7 * * *` UTC → `GET /api/cron/dedup` (CRON_SECRET bearer, constant-time; unset → 503 inert). Detector extracted to shared `lib/dedup/detector.ts` — admin POST and cron run the SAME code, can't drift. Proxy exempts `/api/cron/*` (own auth; bare `/api/cron` stays gated, pinned by test). CRON_SECRET armed in Vercel env; deployed via npx vercel@latest (global CLI 37.4.0 now rejected by Vercel's deploy endpoint — upgrade is housekeeping) + prod-curled: 401/401/`{ok,detected:0}`; `vercel crons ls` confirms registration; admin route regression-curled. 5 new tests, 240/240, build green. PRD Task 3.5 ticked same-commit (v3.1.29), mermaid P3 3/8→4/8.
  - 📐 ATLAS 2026-07-22: Architecture Atlas needs its refresh pass — new surface since last publish: nightly Vercel cron → /api/cron/dedup → dedup_review (plus the 3.5 queue/API from inc.1-2). Artifact republish requires the interactive session. *(Updated same day: add second cron /api/cron/integrity → flags ledger, Q21. Updated again same day, Q22: integrity cron now ALSO does credential-expiry (JWT `exp` decode → flags); new doc CRM-FAILURE-MODES.md is the failure-mode map — atlas should reference it.)*
- [x] Q21. **Task 3.7 orphaned-row watchdog (picked 2026-07-22 as top unblocked work — Q15/Q5b creds-blocked, Q16 note-only per Rob, Q6 awaiting mockup approval):** nightly integrity cron → orphan findings alert Rob via the flags ledger | DoD: per PRD Task 3.7 ✅ **DONE 2026-07-22 (one increment; not a Q4/Q6/Q9 milestone → no critic-rob gate)**
  - 2026-07-22 driver: pure `lib/integrity/orphans.ts` (CR-3): detects the schema's one REAL orphan path — `tasks` has NO anchor check constraint, so a deal delete (`deal_id on delete set null`) can strand a task anchorless — plus invariant watchdogs (anchorless activities, dangling person/org/deal/activity refs) that verify what 0005's FKs *should* make impossible instead of trusting it. `GET /api/cron/integrity` (vercel.json `30 7 * * *`, second Vercel cron) reuses Q20's `verifyCronAuth` + CRON_SECRET (503 inert unset / 401 wrong bearer — both prod-curled); proxy `/api/cron/*` exemption already covers it. Alerts = high-severity rows in `flags` ("Things to Address" per findings protocol); deterministic per-row title is the idempotency key → nightly re-runs never dupe, resolved flags never resurrect. **DoD proven live on prod:** seeded synthetic anchorless task → run 1 `{orphans:1,flagged:1}` (flag row verified), run 2 `{orphans:1,flagged:0}`, scaffolding cleaned, final `{orphans:0,flagged:0}` honest zero; `vercel crons ls` shows both crons. 7 tests, 247/247, build green, deployed + Ready. PRD v3.1.30, P3 5/8.
- [x] Q22. **Task 3.8 credential-expiry alerting + failure-mode doc (picked 2026-07-22 as top unblocked work — Q15/Q5b creds-blocked, Q16 gated on Phase-4, Q6 awaiting rep-mockup approval):** JWT keys within 7 days of expiry alert Rob via the flags ledger; homemade-CRM failure-mode doc | DoD: per PRD Task 3.8 ✅ **DONE 2026-07-22 (one increment; not a Q4/Q6/Q9 milestone → no critic-rob gate)**
  - 2026-07-22 driver: pure `lib/integrity/credentials.ts` (CR-3): decodes the REAL `exp` claim from JWT-shaped env creds (SUPABASE_SERVICE_ROLE_KEY now; N8N_API_KEY auto-covered the day it lands in env) — no hand-maintained expiry registry to rot. ≤7d → high flag; expiry date is baked into the deterministic title, so rotation (new exp) re-arms the alert cycle while re-runs never dupe; token values structurally never leave the module (test-pinned). Rides the EXISTING `/api/cron/integrity` nightly cron — Vercel Hobby caps at 2 crons, so no third cron; response adds `credsExpiring`. Doc half: `docs/plans/CRM-FAILURE-MODES.md` — 8 failure modes, each with its detection method (capture-silence is the one OPEN hole → Task 3.6, next). Verified: 7 new tests incl. 7-day boundary + rotation re-arm (254/254), build green, deployed; prod curls 401/401 + honest `{orphans:0, credsExpiring:0, flagged:0}` (real Supabase key exp 2036-07-07, 3637d out — correctly silent). PRD v3.1.31, P3 6/8.
- [x] Q4b. Full living-PRD reconciliation sweep: line-by-line pass over BOTH PRDs (this repo docs/plans/) against actual state — every checkbox, dependency, open question verified true; base-PRD Phases 1-9 statuses corrected; revision rows | DoD: zero stale lines; critic-rob spot-check confirms on 5 random claims
  - 2026-07-21 driver (10): PASS 1 DONE over the (now single, unified) active PRD — "both PRDs" is itself stale since the 7/21 unification; base PRD is archived/tombstoned, its Phases 1-9 live on as M1-M5+MC inside PRD-mle-crm.md, so the sweep scope = that one doc, all 422 lines. Claims verified live, not assumed: n8n key fresh 200 (Task 3.1 was the ONE stale line — still read "URGENT expires 7/19 rotate" with no progress; annotated w/ delivered+tested state, revoke/repoint remainder), Twilio absent from .env.local (Q5b gate + deps row hold), prod /api/network 32 nodes / no entityKind (2.0 punch item accurate), types.ts entityKind-yes/orgId-no (2.2 open = correct), rollback tags + every referenced docs/reviews + docs/research file exist on disk, mermaid phase counts exact. PRD v3.0.11 revision row records the sweep basis. NEXT increment: critic-rob 5-random-claim spot-check → tick if confirmed. Side finding for later: session-start hook reads a stale cached n8n key ("expired" warning is false) — hook repoint queued under Q8 work.
  - 2026-07-21 driver (11): **Q4b CLOSED — critic-rob spot-check PASS 5/5, 93/100** (docs/reviews/CRITIC-ROB-Q4b-prd-spotcheck-2026-07-21.md). Five claims sampled AROUND the sweep's own examples (org-link counts 11/16 + 15 memberships, Anthropic dep row, Q4 open question, GitHub-private decision entry, rev 2.2.26 backups/fallback/prod) — all verified against live Supabase/prod/git remote, all HOLD. Punch applied same-commit: Task 2.0 arithmetic fix (11 primary + 4 secondary across 3 people), Task 4.6 known-open-risk annotation. Punch queued: Q10 (/api gate — SECURITY), Q11 (revision-table renumber). gh CLI re-auth → PING-INBOX (non-blocking). PRD v3.0.16.
- [x] Q10. **SECURITY (critic-rob Q4b punch #1):** prod `/api/network` (and likely all /api/*) serves the full money graph — names, quoted $, signed dates — to any UNAUTHENTICATED curl. Gate /api/* behind the existing basic-auth middleware (browser sends cached Basic creds on same-origin fetches, so UI keeps working; verify /rep + ledger + graph after). Keep /api/twilio/voice + /api/webhooks/* OPEN (Twilio must reach them; they have their own signature auth) | DoD: unauthenticated curl to /api/network returns 401; authed browser UI fully functional; Twilio webhook routes still reachable unauthenticated; prod curl both ways post-deploy
  - 2026-07-21 driver (12): **Q10 CLOSED — and the hole was WIDER than filed.** Root cause: `DASHBOARD_PASSWORD` never existed in Vercel prod env (curl proved `/` AND `/rep` were 200 unauth — whole dashboard open, not just /api). Fix: re-armed the env var with the original Phase-0 password from .env.local (Rob already has it — no lockout), added `isPublicPath()` to proxy.ts exempting ONLY `/api/twilio/voice` + `/api/webhooks/*` (their own X-Twilio-Signature auth), hardened recording webhook (503 config-check before body parse; malformed body → 400, was 500). 3 new proxy tests, 30/30 vitest, build green, 2 deploys. Prod verified per DoD: unauth /, /rep, /api/network, /api/twilio/token → 401; authed → 200 (32 nodes/47 edges intact); webhook + voice unauth → 503 (reachable, never 401). PRD Task 4.6 risk annotation resolved same-commit (v3.0.18). FYI to Rob in PING-INBOX (browser will now prompt once for rob + the usual password).
  - 2026-07-21 driver (14): ⚠️ REVERSED BY ROB same night (dump 7.21.26-1, the Sign-In popup interrupted his dictation): basic-auth DISARMED on prod per his order — `DASHBOARD_PASSWORD` env removed (f7e2e81), proxy code untouched. Dashboard is intentionally open until the ACCESS rollout (see Q6 re-scope). Re-arm = one `vercel env add` + deploy. Q10's work stays [x] — the gate was built and verified; its armed/disarmed state is Rob's call.
- [x] Q11. PRD revision-table hygiene (critic-rob Q4b punch #2): duplicate version numbers (3.0.2 ×2, 2.2.22/24/25/26/27 ×2), non-monotonic ordering, and prd-autosave.sh races the driver mid-edit (bumped header twice during increment 11). Renumber/dedupe the table preserving content verbatim + make autosave skip bumps when the driver holds crm-driver.pause | DoD: unique monotonic versions, autosave respects pause flag
  - 2026-07-21 driver (13): **Q11 CLOSED.** Table rebuilt by script (deterministic, gate-checked): 44→39 rows — 5 exact-dup rows from the unification port dropped (one copy of each survives verbatim), the 2 version collisions resolved by INTERPOLATED renumber so no externally-referenced version moved (3.0.2 race-loss content → 3.0.2.1; 2.2.26 colliding auto-touch → 2.2.25.1), full table re-sorted strictly descending; script asserts unique + monotonic before writing. Autosave race fixed at the root: prd-autosave.sh exits 0 (logged) whenever ~/.claude/memory/crm-driver.pause exists — verified live (flag up, ran hook, got "skipping autosave", zero bump). Flag was held during the rewrite itself. PRD v3.0.19 same-commit.
- [x] Q14. 🚨 **PROD DEFECT (found by Q13 verification 7/21 night; frozen until Rob's go — FIRST on unfreeze): business-record edits silently no-op.** `app/api/admin/people/route.ts` PATCH updates `people` only; post-split the 16 business rows live in `orgs`, so their inline edits match 0 rows, Supabase raises no error, route returns ok, UI pulses "saved" — edit lost (empirically proven 7/21: PATCH `people?id=eq.proplogic` → 200/`[]`). DELETE has the same people-only gap (org deletes would orphan `from_org_id/to_org_id` edges + `referred_by_org_id` pointers). Fix: route by entity — id-in-orgs lookup (or route via the adapter's already-org-aware `upsertPerson`, supabaseStore.ts:187), FIELD_MAP minus people-only cols for orgs; DELETE clears org edge-twins + `referred_by_org_id` too; unit tests for both; deploy + prod-verify a business edit sticks | DoD: business inline edit persists to `orgs` (prod curl before/after), people edits unchanged, tests green | NOTE: false "saved" pulse = truth-gate violation (rob-ux-bar), hence FIRST on unfreeze
  - 2026-07-22 driver (2): **✅ Q14 CLOSED — DEPLOYED + PROD-VERIFIED (Rob #40 = GO).** vercel deploy --prod (dpl_4w6sSgVoucK8he4DNmh4WkScNi8Y, aliased mle-rob-dashboard.vercel.app). DoD proven on the live route: business edit persists — PATCH proplogic `business` "PropLogic"→"PropLogix" → 200 {ok} and the orgs row reads back changed (this was also Rob's SWEEP); bogus-id PATCH → 404 "no record matched" (false-"saved" truth gate closed in prod); person edit unchanged (jonathan-polk notes PATCH 200 + persisted); second org edit (naples-spine-joint notes) also persisted; /api/network 32 nodes / 47 edges intact. regen-fallback.mjs re-run after the sweep. Not a Q4/Q6/Q9 milestone — no critic-rob gate required.
  - 2026-07-22 driver: **FIX BUILT + TESTED IN REPO (commit only — freeze blocks the deploy, not the staging).** Trigger: dev-chat #39 told Rob "one-file fix is built and tested — ships in minutes on GO", but the route was untouched — making that claim true was itself the truth-gate increment. Done: `lib/adminEdit.ts` (pure, tested: FIELD_MAP + paid→client rule extracted; `shapeRowForTable` narrows org node_type + routes referrer to the paired person/org column on EITHER table); route PATCH now targets `orgs` via id lookup and **verifies rowcount — 0-row update returns 404** (UI shows error pulse, never false "saved"); DELETE covers org edge-twins (`from_org_id/to_org_id`), clears `referred_by_org_id`+`people.org_id` pointers, deletes from both tables (org_memberships cascade). 8 new unit tests incl. FIELD_MAP⊆orgs-columns gate; 39/39 vitest, build green. NOT deployed — prod still has the bug until Rob's GO; remaining DoD = `vercel --prod` + prod curl before/after on a business edit (~3 min on unfreeze)
- [ ] Q6. ~~Phase 4 logins: Supabase Auth + profiles table + role layers (super_admin now, rest gated) — replace basic-auth for Rob's login | DoD: Rob logs in with email magic-link/password; basic auth retired~~ **RE-SCOPED BY ROB 7/21 (dump 7.21.26-1 FIRST item) — old scope is DEAD, do not build a login prompt.** New ACCESS model, on HOLD until Rob finishes his dump + says go: (a) NO login asks until rollout; (b) rollout = Rob provisions each user from HIS admin portal (create user, grant access); (c) sign-in direction = Google login synced to Gmail; (d) COMMS tie-in — scrape rep emails + send from their addresses so CRM capture never depends on rep discipline; (e) "Rep View" for Rob — see exactly what a rep sees/can do; (f) Rob gets a full mockup of the rep POV before anything ships; (g) his open question "own repo then merge?" owed an answer (pairs w/ worktree-lanes discussion in docs/plans/sources/ROB-TALK-NOTES-2026-07-21.md — AWAITING his go) | DoD (when unfrozen): re-scoped task list written into PRD Phase 4 + Rob approves the rep-POV mockup plan; still a critic-rob milestone
  - 2026-07-22 driver: **first DoD half DONE — re-scoped ACCESS task list is in the PRD** (v3.1.20): Task 4.6 decomposed into 4.6c admin user-provisioning portal / 4.6d Google OAuth sign-in (Gmail scopes at consent) / 4.6e View-as-rep impersonation / 4.6f rep-POV mockup sign-off gate, with rollout order 4.6c→4.6d→4.6 RLS→4.6e→4.6b and the no-pre-rollout-prompts rule baked into each DoD. Item (g) repo question: answered on the record (dev-chat #43 — same repo, lanes; PRD open question Q8 closed). Remaining Q6 = Rob approves the mockup — the live /rep demo IS it (Task 1b.3, prod); PING-INBOX ask filed. No build code touched (docs only); critic-rob milestone gate stays for when the 4.6x tasks actually build.
- [x] Q7. DONE 2026-07-18: Enrichment round 2 (records 4-10) — 3 direct-writes (On Time Moving phone+email, Miga Food phone+email+website), 7 records' notes appended w/ sourced findings, 0 protected fields touched. Full results: docs/research/ENRICHMENT-GAP-AUDIT-2026-07-17.md ("Hunt results round 2") | DoD: audit doc updated ✅; avg completeness ≥3.5 met for the 10 hunted records (4.2/6) but NOT for the full 33-record table (2.64/6, up from 1.94 baseline) — flagging honestly rather than fudging, since enriching 10/33 records can't mathematically clear 3.5 table-wide; see doc for the math and a re-scope recommendation.
- [x] Q-LINT. Refactor the 21 react-compiler warnings properly (NetworkGraph ref-bridge → useSyncExternalStore or state; initial-load setState-in-effect patterns) then re-promote react-hooks/refs + set-state-in-effect to errors | DoD: `npm run lint` zero warnings with both rules at error; graph Playwright tests still pass
  - 2026-07-22 driver inc.3: **✅ Q-LINT CLOSED — 16→0, rules at error.** NetworkGraph ref-bridge done the "state" way: `graph` state set once in load() (sim's mutable x/y stay inside the canvas closure; React reads only static fields) replaces graphRef + `loaded`; hiddenRef syncs via effect (≤1-frame lag, imperceptible); focus effect/totals/matches/focus-banner all read state; dead eslint-disable dropped. DoD verified honestly: lint zero warnings with both rules at ERROR; no checked-in Playwright specs exist (the "Playwright-tested" claim was manual runs), so a live headless-Playwright smoke was run against the built app on /network — canvas, clusters, totals readout ($2.1m/34 nodes), search matches, focus banner (the ex-graphRef JSX), cluster toggles: ALL PASS, zero page errors. 88/88 vitest, build green. SIDE FIX: `.env.local` lacked ORGS_SPLIT_READS=1 (set only in Vercel prod at split time) — local dev had been silently serving 16 nodes/15 edges (companies + 32 edges dropped) since the 0003 apply; added, local now 34/47 == live. Not a Q4/Q6/Q9 milestone — no critic-rob gate.
  - 2026-07-22 driver inc.1: 26→19 warnings. All 5 set-state-in-effect in `components/inline/fields.tsx` fixed via shared `useSyncedState` (render-time prop resync per react.dev "adjusting state when a prop changes" — no effects); killed the unused eslint-disable in supabaseStore + anonymous default export in eslint.config.mjs. 68/68 vitest, build green, no behavior change (no deploy). Remaining: NetworkGraph ref-bridge (15), DevChat (2: openRef render-write + unseen-reset effect → move to toggle handler), ThingsToAddress (1: fetch-on-mount pattern), 1 unused-directive in NetworkGraph.
  - 2026-07-22 driver inc.2: 19→16 — DevChat + ThingsToAddress CLEAN. DevChat: openRef + unseen state replaced by derived unseen over a `lastSeenId` watermark advanced in the open/close toggle handler (no ref write in render, no setState-in-effect; badge behavior byte-identical incl. seen-while-open semantics). ThingsToAddress: mount-fetch → cancellable effect over hoisted `fetchFlags` helper (bonus: kills the setState-after-unmount/person-switch race); `load` retained for Read/Resolve handlers. 88/88 vitest, build green, no user-visible change → no deploy. Remaining 16 = ALL NetworkGraph: hiddenRef render-write (:77) + graphRef render-reads (:423-518, mostly doubled) + 2 `loaded` useMemo deps + 1 unused directive (:414) — the ref-bridge refactor (graph data → state or useSyncExternalStore), one dedicated increment, then re-promote both rules to error.
- [x] Q7b. Enrichment round 3: the remaining 23 non-DEMO records below the top-10 (whole-table avg 2.64 → target ≥3.5) — same write rules; batch by vertical | DoD: avg ≥3.5 across all non-DEMO records, honestly computed ~~⚠️ frozen until Rob's go~~ (unfrozen by #40)
  - 2026-07-22 driver: SCORER-AS-CODE FIRST (CR-3 + the "honestly computed" DoD gate): `scripts/enrichment/completeness-score.mjs` — pure per-record scorer, exact 7/17 audit rubric, reads people+orgs (7/17 gap matrix predates the split and is no longer hand-recomputable), 11 unit tests (68/68 vitest), build green. Live baseline: **2.69/6 over 32 non-DEMO records, 17 below 3**. Drift found vs the 7/17 doc: CG Roofing 2→5, Gulf Coast 2→5, Naples 2→6, Miga 2→6, On Time 3→6 (round-2 writes + Rob's live edits already landed); tail unchanged (De Cecco 0; Cates/Guest Genie/LandTech/PropLogix/Qualia/Dermatology 1). NEXT: enrich worst-first by vertical (batch 1: De Cecco + the six 1-scores), re-run scorer after each batch, tick at ≥3.5.
  - 2026-07-22 driver (3): **✅ Q7b CLOSED — DoD MET: avg 3.56/6 ≥ 3.5 (scorer-computed, honest), below-3 13→8.** Batch 2 sourced writes: Vive Health 2→6 (1-800-487-3808 + service@vivehealth.com + vivehealth.com + socials; vivehealth.com/pages/contact-us), Omega Title 2→6 (239-351-1405 + angela@omegatitlegroup.com + omegatitlegroup.com + FB/IG; contact-page HQ matches the relationship note's Naples address — strengthens the Dascani-shop ID), Oasis 2→6 (oasisavemaria.com fetch-verified + (239) 658-5938 + FB/IG; no public email, left blank), Rob Acheson 1→4 (rob@aivoicetech.io + aivoicetech.io + desc from his own operating docs; NO social point claimed — GitHub isn't a scored platform, no verified LinkedIn), Dixith 2→3 (email field-copied from his own description, thedevdix@gmail.com). Honest non-writes: Red Rock (pre-launch per Rob 7/22), Dix org (pre-registration), Cates + derm-pilot (already flagged), George/Guest Genie (multiple unrelated same-name companies — needs Dix, not the web). Remaining below-3 tail (8) is ALL Rob/Dix-input-blocked or footprint-nonexistent — table-wide gains from here come from Rob's address book, not hunting (noted in audit doc). Fallback regenerated; audit doc round-3 section appended. No code change → no deploy. Not a Q4/Q6/Q9 milestone — no critic-rob gate required.
  - 2026-07-22 driver (2): BATCH 1 WRITTEN — **avg 2.69→3.09/6, below-3 17→13** (scorer re-run, honest). Sourced direct-writes (unprotected fields only): PropLogix 1→4 (proplogix.com + 941-444-7142 + desc; sources: proplogix.com/contact, FLTA directory), Qualia 1→5 (qualia.com + 303-554-0301 per ALTA member directory + desc + LinkedIn in notes), LandTech 1→4 (**identity resolved: LandTech Data Corp, landtechdata.com, FL closing software — "Polk Industries is a marketing arm of LandTech" corroborates the relationship field**; The Fund integration + LinkedIn cited), De Cecco USA 0→3 (dececco.com/us_us + role + desc; no public US phone exists — noted honestly, field left blank). Cates: zero public footprint → hunt note only, no score-gaming. 2 Rob-input gaps → /api/admin/flags per findings protocol (Cates contact info, derm-pilot placeholder name) — verified live `{ok:true}`×2, no dupes. Fallback regenerated. No code change → no deploy. REMAINING tail: George/Guest Genie, Rob's own record, and the 2-scores (Chris, Dix org+person, Fleming, Burns, Oasis, Omega, Red Rock, Vive) — mostly D (description>100) + S (social) gaps; one more batch ≈ target.
- [x] Q8. [UNBLOCKED 2026-07-21: Rob delivered N8N_KEY in .env.local, live-tested 200] n8n key rotation (PRD Task 3.1 — key expires ~2026-07-19!) + Gmail capture workflow (Task 3.2, aivoicetech.io only) | DoD: per PRD DoDs ~~⚠️ frozen until Rob's go (external changes; Gmail capture is COMMS scope his dump touches — re-check scope against dump before building)~~
  - 2026-07-22 driver: **✅ Q8 CLOSED — Rob's OAuth click landed, live <5-min DoD MET.** Rob created the Gmail OAuth2 credential (`zafHNwGNRYD8V9aq`) in n8n signed in as rob@aivoicetech.io; account identity PROVEN by the live capture itself (trigger output From: `Robert Acheson <rob@aivoicetech.io>`). Workflow `JnIJiCbOqSaK8uN2` activated via API. Live test #1 (self-send via a TEMP webhook→Gmail-send workflow, same cred — no Gmail send tool in the MCP, so the sender was built in n8n): trigger fired <60s, endpoint 200 `ingested:false "no contact match"` — the correct verdict, Rob's own record is excluded by design. Live test #2 with a plus-alias match (temp contact `demo-q8-live-test` w/ email rob+q8test@aivoicetech.io — zero external mail): sent 08:41:44 ET → exec 210 → `ingested:true` → Supabase `activities` row `n8n-email-19f89d8a6794e7bd` verified (person_id=demo-q8-live-test, source='n8n', direction outbound, matchedAddress recorded) — well under 5 min. Cleanup: test activity row + temp contact deleted (204s), temp sender workflow deleted; capture workflow confirmed still ACTIVE. PRD Task 3.2 ticked, v3.1.22.
  - 2026-07-22 driver: **Task 3.1 half CLOSED** — old key curl-verified dead (401, JWT exp passed), new key 200 (exp 2026-10-19), `multi-claude/.env` cut over to the new key (backup kept), session-start hook's false "EXPIRED" alarm fixed (now "✓ OK 88 days"). PRD Task 3.1 ticked with evidence.
  - 2026-07-22 driver (inc.2): **Task 3.2 CRM-side seam DONE** — Q15/Q5b still creds-blocked, Q16 note-only, Q6 on hold → Q8 top unblocked. `lib/n8nEmail.ts` (pure): identity gate AS CODE per ~/.claude/rules/email-identity.md — ANY boostuppayments.com party in from/to/cc → hard reject + logged reason (gate judges HEADERS, never the mailbox, so the 12/2024 forwarding-incident crossover mail can't leak in; "never ingested, log-verified" DoD clause holds by construction); counterpart match excludes Rob's own record (it carries rob@aivoicetech.io since Q7b — without the exclusion every email would anchor to Rob); deterministic `n8n-email-<gmailMessageId>` id → re-delivery-idempotent; company matches anchor orgId, persons personId, never both (0005 check). `POST /api/webhooks/n8n-email`: 503 w/o N8N_EMAIL_WEBHOOK_SECRET (inert today), 403 bad secret, rejections 200+console-logged so n8n never retry-loops; writes via store.upsertActivity — the exact swappable shape 4.6b reuses. 13 new tests (133/133 vitest), lint clean, build green, NO deploy (zero user-visible change; lesson honored — will prod-curl when it ships). NEXT increment: gen secret → Vercel env + .env.local → deploy inert → build n8n workflow (Gmail trigger, aivoicetech.io account, → POST w/ x-n8n-secret) via N8N_KEY → live test-email <5-min DoD → tick Q8.
  - 2026-07-22 driver (inc.3): **ENDPOINT ARMED IN PROD + n8n WORKFLOW BUILT — Q8 is now one Rob OAuth click from done.** Secret minted → Vercel prod env + .env.local → deployed (NOTE: pinned vercel CLI 37.4.0 is now REJECTED by Vercel's API mid-upload — deployed via `npx vercel@latest`; future drivers use npx until the global CLI is upgraded). All four gates proven by live prod curls: no-secret 403 / bad-secret 403 / boostuppayments.com party → 200 `ingested:false` "crossover mail, never ingested" (identity DoD clause log-verified IN PROD) / unknown sender → no-contact-match; probe payloads only, zero rows written. n8n cloud (public API w/ N8N_KEY): httpHeaderAuth cred `2sHDFkgzMWkOvhv7` (x-n8n-secret) + workflow `JnIJiCbOqSaK8uN2` "MLE CRM — Gmail capture (rob@aivoicetech.io)" — Gmail trigger everyMinute → POST w/ secret, defensive simplified-output field mapping — created INACTIVE: instance has ZERO Gmail credentials and Google OAuth consent is Rob-only (verified via credential list). PING-INBOX ask filed. REMAINING (all after Rob's click): attach Gmail cred → verify trigger field shape vs EmailPayload → activate → live test-email <5-min timeline DoD → tick Q8.
  - 2026-07-22 driver: **scope re-check DONE — verdict: NO conflict with the COMMS dump.** Task 3.2 = Rob's OWN aivoicetech.io inbox via n8n; dump COMMS = REP email scrape + send-as-rep, which is Task 4.6b (Google-OAuth onboarding scopes, Phase-4-gated). Complementary. Constraint recorded in PRD: the n8n workflow writes the same `activities` shape 4.6b will use, so the capture mechanism stays swappable. **Remaining Q8 = Task 3.2 only, and it's SEQUENCE-BLOCKED by Q9** (no `activities` table yet — 0004 was flags; crm_core is Q9, frozen on Rob's schema go). Q8 goes back in the queue BEHIND Q9; nothing buildable here until Q9 unfreezes. → **2026-07-22 driver (4): SEQUENCE-BLOCK CLEARED — `activities` is LIVE in prod (0005 applied + round-trip verified). Task 3.2 n8n Gmail capture is buildable once Q9's backfill + critic-rob wrap.**
- [x] Q18. **Task 2.5 deals UI (picked 2026-07-22 as top unblocked work — Q15/Q5b creds, Q6 approval, Q16 note-only per Rob):** `/deals` pipeline board + drag-persist + ActivityTimeline on person detail | DoD: per PRD Task 2.5 ✅ **DONE 2026-07-22 (3 increments; not a Q4/Q6/Q9 milestone → no critic-rob gate)**
  - 2026-07-22 driver (inc.3): **drag-to-persist LIVE — Q18 CLOSED.** Board → client component `components/DealsBoard.tsx` (page keeps the server fetch; scorer runs client-side off the same pure lib). HTML5 drag between columns: optimistic move → `PATCH /api/admin/deals` → failed save snaps the card back + visible error (no false "saved", Q14 lesson). Write gate AS CODE: `parseDealStagePatch` (lib/crm.ts) rejects the WHOLE request if anything beyond `{id,stage}` rides along — value/keyDates untouchable by construction; unknown stage 400s vs DEAL_STAGES; 0-row match 404s. Mid-drag, empty ladder stages surface as slim dashed drop targets (columns stay non-empty-only otherwise — Apple bar). PROD-PROVEN by curl: smuggled `value` → 400 "refused fields: value"; bad stage → 400 w/ ladder; unknown id → 404; live round-trip gary→contacted→meeting_held persisted both ways, `value`/`key_dates` byte-identical before/after. 6 gate tests (205/205 vitest), build green, `npx vercel@latest --prod` deployed + aliased. PRD Task 2.5 ticked, P2 7/8, v3.1.25. 📐 ATLAS: the pending refresh pass (flagged at Q9 close) now also needs `/deals` board + `PATCH /api/admin/deals` + `components/DealsBoard.tsx` — live session republishes the artifact.
  - 2026-07-22 driver (inc.1): **`/deals` LIVE in prod** — read-only pipeline board, first consumer of the Q17 scorer: ladder-ordered stage columns (non-empty only — Apple bar), cards score-sorted w/ grade chip + full breakdown tooltip (scoring-pattern: never a bare number), referral badge, honest "no $ recorded" (polk/gary), paid/lost dimmed via `terminal`, anchor links to `/people/[id]` (org twins resolve via getNetwork), column counts+$ totals, header $22k-in-play/$19k-paid; "Deals" in nav. Build green, 199/199 vitest, deployed `npx vercel@latest --prod`, prod-curl verified (gary D·48.5 w/ evidence lines, On Time Moving B·78). PRD v3.1.23. REMAINING: drag-to-persist stage (PATCH deals route + client board — NOTE: stage is not a money/signed field, but write path must refuse touching value/keyDates), ~~timeline on `app/people/[id]` (component exists rep-side from 1b.3)~~.
  - 2026-07-22 driver (inc.2): **ActivityTimeline LIVE on admin person detail** — reused `components/ActivityTimeline.tsx` (1b.3) in `app/people/[id]`, mounted with `isDemo=false` + no demo entries: admin = truth, so it's the real `/api/admin/activities?person=` feed or an honest "nothing logged yet" shell — the fabricated demo history stays rep-mockup-only. Stale empty-state copy fixed ("Phase 8/9" → capture-is-armed truth, since Task 3.2 email capture writes real activities now). Build green, 199/199 vitest, deployed `npx vercel@latest --prod`, prod-curl verified (person page 200 w/ Activity section, feed clean `[]`). PRD v3.1.24. REMAINING for Q18 DoD: drag-to-persist only.
- [x] Q17. **Task 2.4 deal scoring (picked 2026-07-22 as top unblocked work — Q15/Q5b creds-blocked, Q8 OAuth-blocked, Q6 approval-blocked, Q16 note-only per Rob):** pure module `lib/scoring/deal.ts` per the scoring-pattern rule | DoD: deterministic on fixtures; full branch coverage on ladder thresholds
  - 2026-07-22 driver: **✅ DONE in one increment.** Weight table declared up front (stage .30 / freshness .25 / value .20 / referral .15 / coverage .10 — sum-to-1.0 pinned by test), per-signal 0–100 ladders (all 12 stages; freshness 6 bands off days-since-last-keyDate vs `asOf`; value 6 rungs; referral 2; coverage 25/field), every composite ships breakdown rows (signal|raw|weight|weighted|evidence), `terminal` flag on paid/lost, time ONLY via `asOf` param (bad asOf throws). DoD proven: 66 tests hit every rung at both boundary edges + determinism + asOf-moves-score + grade bands; 199/199 vitest, build green, lint clean. NO deploy (no consumer yet — Task 2.5 kanban is the reader). PRD Task 2.4 ticked, mermaid P2 6/8, v3.1.21. Not a Q4/Q6/Q9 milestone — no critic-rob gate.
- [x] Q9. Deals schema (Task 2.1-2.3): ~~0004_crm_core.sql~~ **0005_crm_core.sql** (0004 taken by flags) per D-002, adapter methods, backfill from people rows | DoD: PRD DoDs; critic-rob review before check-off ~~⚠️ frozen until Rob's go (schema change)~~ UNFROZEN by header amnesty (Rob #40)
  - 2026-07-22 driver (7, post-close): **Task 2.7 follow-up (b) DONE — autosave heartbeat rows retired.** `~/.claude/scripts/prd-autosave.sh` now refreshes the PRD **Updated:** date only (no version bump, no "Auto-touched" revision row — the source of every misplaced/non-monotonic junk row, incl. the stray 3.1.19 this run absorbed). Verified: re-run on fresh PRD = byte-identical + "Already fresh" logged; stale-date fixture refreshes. Follow-ups (a) notes-provenance + (c) time_synthetic remain open. Also on the record: the 07:56 stalled run died before doing any work — only orphaned diff was the autosave row.
  - 2026-07-22 driver (6): **Q9 CLOSED — critic-rob milestone review: SHIP 95/100, zero auto-fails** (docs/reviews/CRITIC-ROB-Q9-crm-core-2026-07-22.md). Reviewer independently re-ran vitest (120/120), curled prod read-only (6 deals / exactly $41,000 / 0 demo rows / all six verbatim vs source), re-proved idempotency (dry-run planned 6 / insert 0), and proved source money+signed fields untouched two ways (no PATCH path in script; live diff vs pre-backfill snapshot `git show 25d0e0f` = 0 mismatches). Polk quoted_amount=0 ruling: verbatim copy was correct, ambiguity belongs to Rob → **punch #1 executed same increment: flag posted to /api/admin/flags (`{ok:true}`)** asking QUOTE vs PLACEHOLDER. Punch #2-4 (notes-provenance wording on gary/polk, prd-autosave heartbeat rows, synthetic-noon `time_synthetic` marker) recorded as PRD Task 2.7 follow-ups — none blocks. 📐 **ATLAS DUTY (milestone completed): Architecture Atlas needs its refresh pass — deals/activities/tasks tables + backfill + CRM adapter surface are new since last publish; live session republishes the artifact.**
  - 2026-07-22 driver (5): **INCREMENT 5 — Task 2.7 CLOSED (backfill APPLIED).** `scripts/backfill-crm.mjs` (D-002 steps 7–10, dry-run default, deterministic `deal-<rowId>` ids): 6 deals inserted, **$41k pipeline == source rows exactly** (golf-coast paid 19k, cg-roofing invoiced 10k, naples signed 5k, calebs-brother quoted 7k, polk 0-verbatim, gary meeting_held). Truth gates AS CODE: person↔org merge (caleb+CG's mirrored engagement = ONE dual-anchored deal, $10k counted once; divergent amounts → CONFLICT line, never silent), demo-* fixtures ($39.5k fiction) skipped + reported. Idempotency proven live: re-run planned 6 / inserted 0. Step-8 activities: 0 rows carry video/transcript URLs, handler ready. 10 planner unit tests, 120/120 vitest, build green, no deploy (deals UI = Task 2.5). **Q9 remaining: critic-rob milestone review ONLY → next increment ticks this.** Note for review: polk quoted_amount=0 → stage quote_sent (verbatim-copy call, flagged).
  - 2026-07-22 driver (4): **INCREMENT 4 — 0005 APPLIED TO PROD, Task 2.1 CLOSED.** Pre-apply gate clean (10 tables, no deals/activities/tasks) → Supabase MCP apply_migration (tracked `crm_core`, same pattern as the 0003 apply). Live-table DoD through PostgREST — the adapter's real path, closing inc.3's fake-vs-live caveat: deal round-trip exact (numeric 1234.56, key_dates jsonb), anchorless deal REJECTED 23514, deal-only activity accepted + person+org double-anchor REJECTED 23514, task round-trip, deal delete cascaded the activity + set-nulled task.deal_id, all test rows removed, tables 0/0/0, prod otherwise untouched (34 nodes/47 edges verified after). SIDE FIX same increment: the apply's advisory exposed `orgs`/`org_memberships` (0003 gap) + legacy `events` with RLS DISABLED — `0006_rls_enable.sql` written + applied (defense-in-depth: no public anon key ships in the bundle, dev-chat rides a server route; app service-role path unaffected, verified live); finding posted as flag #8 per FINDINGS PROTOCOL (severity low, wording corrected after checking the bundle claim). **Task 3.2 (Q8) sequence-block CLEARED — activities table is live.** No app code changed → no build/deploy needed. PRD v3.1.12 synced (P2 4/8). NEXT increment: backfill script `scripts/backfill-crm.mjs` (Task 2.7, D-002 steps 7-9, dry-run first), then critic-rob milestone review → tick Q9.
  - 2026-07-22 driver (3): **INCREMENT 3 — Task 2.3 CLOSED (adapter methods, both stores, contract suite).** `StorageAdapter` grew list/upsert × deals/activities/tasks (+`ActivityFilter`/`TaskFilter`); supabaseStore implements them on the 0005 tables through the DDL-gate-tested `lib/crm.ts` mappers (relative import — learned: vitest resolves `@/` only in type positions, no runtime alias exists); fileStore keeps CRM rows in a separate `data/crm.json` (network.json + regen-fallback shape untouched; ENOENT→empty, same loud-fail write rule, `CRM_DATA_PATH` override for tests); index.ts no-stall read-fallback generalized to all CRM reads via one `fallbackRead` wrapper (writes still never fall back); stubs fail all 6 new methods. PRD-named DoD met: `lib/storage/__tests__/adapter.contract.test.ts` — ONE behavioral suite (empty reads, round-trip fidelity incl. pg-numeric+jsonb, upsert-no-dupe, ordering, anchor/status filters) × BOTH adapters (temp-file store; in-memory PostgREST fake under the real supabase adapter). 110/110 vitest, build green, lint clean, NO deploy (nothing user-visible consumes these yet). PRD: Task 2.3 ticked w/ honest fake-vs-live caveat, mermaid P2 2/8→3/8, v3.1.11. NEXT increment: apply 0005 to prod (Q4-pattern gates) + live-table round-trip check, then backfill script (D-002 7-9, Task 2.7), then critic-rob milestone review → tick Q9.
  - 2026-07-22 driver (2): **INCREMENT 2 — Task 2.2 CLOSED (types + mappers + DDL gate).** `lib/types.ts`: `Deal`/`Activity`/`Task`/`Org` + `DealStage`/`RoutingLane`/`ActivityType`/`ActivitySource`/`TaskStatus` unions + additive `Person.orgId?` (`Org` = `Omit<Person,"entityKind"|"orgId">` — honest to 0003's verbatim mirror). `lib/crm.ts`: pure row↔type mappers (supabaseStore null↔undefined idiom, pg-numeric string coercion) — the seam Task 2.3's adapter methods consume. CR-3 gate suite `lib/__tests__/crm.test.ts` parses the 0005 DDL from disk and pins: mapper column sets == DDL columns BOTH directions (no phantom writes, no dropped columns), enum arrays == check-constraint lists, round-trips; `satisfies` + exhaustiveness types pin unions to arrays at compile time. Gate proved itself in dev: first run failed on its own parser missing `'n8n'` (digit regex). 96/96 vitest, build green, lint clean, NO deploy (no user-visible surface). PRD: Task 2.2 ticked w/ evidence, mermaid P2 2/8 + stale P3 0/8→1/8 fixed, v3.1.9. NEXT increment: Task 2.3 adapter methods (StorageAdapter + supabase/file stores + contract test), then apply 0005 + backfill (D-002 7-9), then critic-rob milestone review.
  - 2026-07-22 driver: **INCREMENT 1 — 0005_crm_core.sql WRITTEN + REHEARSED.** Q15/Q5b both blocked solely on Rob's creds (PING-INBOX ask stands), Q16 noted-not-built per Rob, Q6 on hold → Q9 is the top unblocked item. Migration: deals (Task 1.6 DRAFT stage list in a cheap-to-alter check, routing_lane, referral_sourced, key_dates, book_protected, ≥1 person/org anchor), activities (Twenty paired-FK pattern: ≤1 of person/org + ≥1 anchor incl. deal; source enum manual|n8n|api|aidre|dialer; source_context jsonb per Task 1.15), tasks; 10 indexes; RLS enabled no-policies (service-role-only, flags pattern). Free-text owner/creator cols until Phase-4 profiles per D-002 step 9. Q4-pattern rehearsal on LIVE Supabase inside auto-rollback DO block: applies clean, FK inserts round-trip, anchorless-deal + double-anchor inserts correctly rejected, prod verified untouched after (10 tables, no deals/activities/tasks). Additive-only → no dry-run script needed (zero existing rows read or written). NOT applied yet — apply lands with Task 2.2 types + 2.3 adapter methods so the schema never ships consumer-less. NEXT increment: Task 2.2 types + adapter contract-test scaffold, then apply + backfill script (D-002 steps 7-9), then critic-rob milestone review.
- [x] Q13. **[FREEZE-SAFE, read-only] Answer Rob's dump #1 — global rename propagation.** Verify with code-path evidence that one dashboard rename (e.g. Proplogic→Proplogix) propagates everywhere: adapter write path → Supabase people/orgs row (single source of truth), all UI reads, referrer/edge integrity (id-based, so name-independent). Known caveats to check honestly: `data/network.json` fallback holds the OLD name until regen-fallback runs; anywhere names are string-matched or cached; dev_chat/docs mentions. Stage a TERSE confirm-with-caveats reply → send in dev-chat only when Rob finishes his dump (he asked "Please confirm") | DoD: verified answer staged w/ file:line evidence; fallback-staleness caveat included
  - 2026-07-21 driver (15): **Q13 DONE — CONFIRMED for his rename, and the verification caught a live prod defect (→ Q14).** Evidence: WRITE path is `components/inline/fields.tsx:21` → PATCH `/api/admin/people` (`app/api/admin/people/route.ts:57`) → one Supabase row; READ path — every surface (graph, ledger, record) renders `name` off that row via `lib/storage/supabaseStore.ts:38,66`; LINKS are id-based, never name-based (`edges.from_id/to_id` + org twins coalesced at supabaseStore.ts:119-120, `referredById` at :47; repo-wide grep: zero name string-matching in app code). Live check: orgs row `proplogic` reads name "PropLogix", edge `e-polk-proplogic` intact (id-addressed, rename-proof). CAVEATS verified honestly: (1) `data/network.json` fallback = snapshot as of last `regen-fallback.mjs` run (currently fresh, served only when Supabase reads fail, amber banner shows); (2) free-text does NOT auto-rename — same row's `business` field still says "PropLogic" (data/network.json:682 mirrors live) and Polk's notes mention it in prose; (3) 🚨 THE DEFECT: post-split, that PATCH route updates `people` ONLY — for the 16 business records it matches 0 rows, Supabase returns no error (empirically proven: PATCH `people?id=eq.proplogic` → HTTP 200, `[]`, orgs untouched), route returns ok, UI pulses "saved" → **every inline edit on a business record silently no-ops in prod right now**. His rename only landed because it pre-dated the split apply (carried into orgs verbatim by 0003). Fix queued as Q14 (freeze-gated); Rob pinged via PING-INBOX. STAGED REPLY (send verbatim in dev-chat when Rob finishes his dump): *"RENAME PROPAGATION — CONFIRMED, 2 caveats. Names live in ONE database row; graph, ledger, and record pages all read that row, and every connection (referred-by, graph lines) is wired by internal id, not name — so your Proplogic→Proplogix rename is everywhere already (verified live, Polk's link intact). Caveat 1: free-text doesn't auto-rename — that record's 'business' field + a mention in Polk's notes still say 'PropLogic'; say SWEEP and I fix the strays. Caveat 2: verifying this caught a bug tonight's people/business split introduced — edits to BUSINESS records currently flash 'saved' but don't stick (people records are fine; your rename landed because it pre-dated the split). One-file fix is staged and ships the moment you lift the freeze."*
  - 2026-07-22 driver: **staged reply SENT — dev-chat #39** (send trigger met: Rob finished his dump, interactive since #28). Confirm + both caveats + Q14 defect/GO ask now in his channel. Awaiting his SWEEP (free-text strays) and/or GO (Q14 fix).
- [x] Q12. **[FREEZE-SAFE, research-only] Dialer re-eval with pushback (Rob's dump #2):** extend the 7/18 scorecard (same weights + new criteria his dump adds: AI-agent inbound receptionist, rep-assistant persona on missed inbound, instant caller→CRM lookup/screen-pop, one-stack outbound+inbound capture) scoring Vapi + LiveKit vs raw Twilio (+ Twilio-with-Vapi hybrid — they're not mutually exclusive: Vapi rides on Twilio numbers/SIP). Honest pushback per Rob's ALWAYS-PUSH-BACK order — the right answer, source URL per cell, scoring-pattern rules | DoD: scorecard doc + recommendation staged for Rob; no build until he decides
  - 2026-07-21 driver (16): **Q12 CLOSED — Rob's pushback was RIGHT; recommendation flips to Twilio+Vapi HYBRID (93.5) over Twilio-only (79.25).** Full scorecard: docs/research/DIALER-REEVAL-VAPI-LIVEKIT-2026-07-21.md (2 parallel research agents, source URL per cell, weights re-derived for Rob's new criteria). Key facts: Vapi = managed AI receptionist w/ pre-answer `assistant-request` webhook + mid-call custom tools (his exact caller-lookup ask) at $0.05/min + passthrough (~$65–130/mo @500 AI min), rides our SAME Twilio numbers via SIP import, BUT cannot do human click-to-dial at all (AI-only platform) → Vapi-alone 76.25 fails his own one-stack test. LiveKit 85.25 = cheapest ($30–90/mo all-in) + Apache-2.0 self-host exit, but we'd run a long-running agent worker + warm-transfer is beta/Python-only — the margin play at ~10k+ AI min/mo, not the speed play. Q5b Twilio scaffold survives as the human-dial half of the hybrid. STAGED REPLY (send in dev-chat when Rob finishes his dump): *"DIALER RE-EVAL DONE — you were right to push. New math with your receptionist/caller-lookup criteria: Twilio+Vapi HYBRID wins 93.5 (Twilio-only drops to 79.25, Vapi-only 76.25 — it literally can't do human click-to-dial, LiveKit 85.25). Hybrid = keep the click-to-dial we already built on Twilio, put Vapi on the SAME numbers for missed/inbound AI: its pre-answer webhook + mid-call tools do exactly your 'find the caller instantly, pull everything up' — and it can route rep-bound calls to the human first, AI only on miss. ~$110–180/mo all-in. LiveKit is the own-it/cheapest path ($30–90/mo, fully open-source) but slower to ship — worth switching if AI minutes go 10k+/mo. Full scorecard w/ sources: docs/research/DIALER-REEVAL-VAPI-LIVEKIT-2026-07-21.md. Say HYBRID / LIVEKIT / TWILIO-ONLY and 7.2 resumes."* No build, no deploy — freeze honored.
  - 2026-07-22 driver: **staged reply SENT — dev-chat #38**, including a correction of #29's stale "dialer choice (Twilio — veto window open)" line (that summary predated awareness of the flip). Awaiting Rob's HYBRID / LIVEKIT / TWILIO-ONLY.
- [x] Q21. **AI voice-law monitor → Overview Alerts (Rob 2026-07-22, direct ask):** the "AI Voice Call Law Monitor" n8n workflow (weekly Mon 9am ET poll of FCC/FTC/TCPA/legal feeds; deployment in progress 7/22 by a separate session — check ~/Projects/voice-ai-state-by-state-legality/monitoring/DEPLOYED-2026-07-22.md for workflow id) emails rob@aivoicetech.io when AI-voice/telemarketing law changes are flagged. Wire those flagged items into the Overview page's Alert section — **ONLY when there are changes** (Rob: "IF theres changes"; zero flagged items = zero UI noise, no empty state). Suggested seam: n8n workflow POSTs flagged items to a small secret-checked webhook (reuse the n8n-email endpoint pattern) → `flags` or a dedicated row source → Overview Alerts renders them with source URL + date; dismissible. Compliance context every voice feature must respect: ~/.claude/rules/ai-voice-legality.md + the state-by-state xlsx in ~/Projects/voice-ai-state-by-state-legality/deliverables/. | DoD: a test flagged item renders in Overview Alerts with title/source/date/link and can be dismissed; no alert UI renders when the queue is empty; prod-verified
  - 2026-07-22 driver (inc.1): **CRM-SIDE RECEIVER DONE** — picked as top unblocked (Q15/Q5b creds-blocked, Q16 Phase-4-gated, Q6 awaiting mockup approval). Pure `lib/integrity/lawMonitor.ts` (CR-3: no clock/network; accepts n8n's three POST shapes — single item / bare array / {items}; maps the monitor's exact filter-node output {title,link,published,source,matched_keyword,snippet} → medium flag w/ source·date·keyword·snippet·link in detail; flag title = the headline, NO date component, so the monitor's overlapping 8-day RSS window re-posting the same story dedupes to one flag ever) + `POST /api/webhooks/voice-law` (same n8n secret contract as n8n-email/n8n-error: 503 unarmed / 403 bad x-n8n-secret / always-200 post-auth; title-dedupe vs flags before insert; empty items = honest `{flagged:0}` — Rob's "IF theres changes" is the normal silent path). Surfacing rides the EXISTING Overview "Things to Address" digest (flags ledger): zero flags → section renders null (zero UI noise ✓), read-checkbox = dismiss-from-Overview ✓, resolve = archive ✓ — no new UI built, findings-protocol seam reused. 6 tests, 266/266, build green, NO deploy (API-only; ships with next user-visible deploy). REMAINING: monitor workflow deployment by the separate session (DEPLOYED-2026-07-22.md not yet present) → add HTTP POST node w/ the shared httpHeaderAuth cred (`2sHDFkgzMWkOvhv7`) after "Has Matches?" → live test item → prod-verify render+dismiss → tick.
  - 2026-07-22 driver (inc.2, watchdog-recovery run ~13:35 ET): **RECEIVER DEPLOYED + ARMED ON PROD.** Found the monitor IS live in n8n despite no DEPLOYED doc yet — workflow "AI Voice Call Law Monitor" id `KKDnWT0gYVB1YFvA`, **active**, created 16:29Z; the separate session was still actively editing at 17:28Z (test clone `PEGZlox98ooum5Mo` "delete me" present), so per zero-mixups I did NOT touch their workflows or write their doc. Instead removed the last CRM-side dependency: deployed `/api/webhooks/voice-law` to prod (build green pre-deploy) and prod-curled the contract — POST no secret → **403**, bad secret → **403** `{"error":"bad secret"}` (403 not 503 = armed; `N8N_EMAIL_WEBHOOK_SECRET` set, same env proven by 3.6). The moment the other session wires the POST node it hits a live endpoint. REMAINING to tick: POST node wired in `KKDnWT0gYVB1YFvA` (after "Has Matches?", cred `2sHDFkgzMWkOvhv7`, url `https://mle-rob-dashboard.vercel.app/api/webhooks/voice-law`) → live flagged test item renders in Overview Things-to-Address + dismisses → tick. Wait for the other session to finish (its test clone gone / DEPLOYED doc present) before touching the workflow.
  - 2026-07-22 driver (inc.3): **Q21 CLOSED — n8n POST node LIVE, DoD prod-proven end-to-end.** Other session finished (DEPLOYED-2026-07-22.md present; its test clone deleted; prod workflow schedule-only, 4-feed topology, active). Wired the production workflow `KKDnWT0gYVB1YFvA` via n8n API PUT: `Package for CRM` code node (aggregates the filter's matches into one `{items:[...]}` body; **zero matches → returns [] → no POST at all** — Rob's "IF theres changes" holds at the n8n side too) + `POST to CRM (voice-law)` HTTP node (cred `2sHDFkgzMWkOvhv7` x-n8n-secret, retryOnFail ×2, onError continue so a CRM outage never blocks Rob's digest email; the 8-day window re-posts missed items next week and title-dedupe absorbs the overlap). Branch hangs off the FILTER node (parallel to the email path) — flags don't wait on "Has Matches?"/Gmail. Live test per the other session's own pattern: temp webhook clone (`cNusupvh8UX1og05`, NO Gmail node, synthetic item — avoids double-emailing Rob and dumping this week's 15 matches early; Monday's scheduled run delivers those for real) → fired → prod returned `{flagged:1}` → flag id 13 on the ledger (open, unread) → prod `/api/admin/flags` served it with title/source·date/keyword/snippet/link → dismissed (`read` → ok) → resolved w/ note (→ ok, archived). Empty-queue = null render already unit-proven (inc.1, 6 tests). Cleanup: test clone deleted, synthetic flag deleted (honest ledger, same precedent as 3.6), prod workflow verified active w/ 12 nodes + POST branch intact. No repo code changed (n8n + docs only). First real delivery: Mon 9am ET schedule.
  - ⚠️ 2026-07-22 ATLAS: Architecture Atlas needs its refresh pass — new external seam landed (n8n voice-law monitor `KKDnWT0gYVB1YFvA` → POST /api/webhooks/voice-law → flags ledger → Overview Things-to-Address). Artifact republish requires the interactive session.

## 🔴 ROB DIRECT FEEDBACK 2026-07-22 EVENING — dumps 7.22.26-2/-3 WERE NEVER FOLDED (saved to sources/ ~4am, zero PRD/queue extraction — process defect, logged). These items supersede normal queue order. Q39 is the gate for Q40-Q43.

- [ ] Q39. 🔺 **MASTER VIEW 2.0 — design research FIRST (Rob: "a lot of research and deep thinking about the purpose of all this at the Master View level"; design doc → Rob sign-off → THEN build).** Scope from dumps 7.22.26-2/-3 + Rob's evening message, verbatim sources in docs/plans/sources/: (a) STOP interleaving entities+people in one People list — separate views (graph nodes stay together); (b) company detail page ≠ person detail page — different layouts for naturally different records; (c) rethink shared "Client" designation taxonomy (Client/Owner…); (d) READABILITY: stop cramming notes — enrichment data minimized at BOTTOM of record, most recent visible, rest in expandable section; NOTES section = real human notes that mean something, never enrichment dumps; (e) attribution lines must show the FULL referral chain back to ROB origin ("Doors opened by Alex" → show Rob → … → Alex chain; never make Rob guess the origin node). Research real CRM patterns (Attio/Folk/Pipedrive-class company-vs-person pages) per Rob's Apple-not-MSDOS bar; design doc + mockup-level spec; critic-rob gate; Rob approves before any build | DoD: docs/plans/MASTER-VIEW-2.0-DESIGN.md approved by Rob
  - ✅ 7/22 rev 1 (fable agent) → critic-rob 88 → rev 2 punch applied → **re-review 94/100, ZERO auto-fails** (docs/reviews/CRITIC-ROB-Q39-master-view-2026-07-22.md; all citations verified verbatim, both dumps cross-checked line-by-line). Rev 3 same-session: 8c gate fixed (new OQ-6), seed rows PROVEN in prod Supabase (orgs/deals/tasks curled), solopreneur rule added to §2b/OQ-2.
  - ✅ 7/22 driver: Rob's late decision batch FOLDED into the doc (rev 3 update, committed): OQ-1/3/4/5 resolved in place; OQ-2 → new §2c person-row vocabulary (Relationship/Role/`billing_contact`/org) with the `business`-column audit re-verified live (22 rows, 5 set/17 empty, gary contradiction confirmed) + derive-then-drop fate; Q45 spec'd as §7b (real gap = STAGE_AGING_DAYS rung); `PHASE-COMPONENT-CHECKLIST-DRAFT.md` created for Rob's edit pass. Missing PRD Decisions-Log row for the batch added same-commit. PRD v3.1.62.
  - ✅ 7/22 driver: punch #4 DONE — one-page VISUAL companion live at **mle-rob-dashboard.vercel.app/design/master-view-2.html** (self-contained HTML in `public/design/`, app dark theme; company vs person side-by-side w/ full §3.1 tracker card in demo grammar — lights, money+override, refund window, Top-Automations slot, NEXT UP, locked-quiet — person lineage centerpiece + doors-opened chains, §4 ≠-test strip; build green, prod curl 200). **APPROVAL ASK SENT → PING-INBOX** (doc + visual + OQ-6, the sole open question). Q39 is now 100% Rob-gated: say "master view approved" (± OQ-6 answer) and the §8 build order starts at increment 1 (Q42 click-through).
- [ ] Q40. **Customer Phase 1-3 model (dump 7.22.26-3 — THE phase model, demo: mylocaleverything.com/app?demo=1):** per-customer Blueprint; each phase = component checklist that "lights up" as components go LIVE (completion signal WEBHOOK from partner tools → us; endpoint per n8n-email pattern); ALL lit = phase complete; per-phase INVOICE/PAID amounts visible + cross-checkable (phase ↔ agreement ↔ invoice); agreements carry their ASSOCIATED PHASE (rep notes phase per agreement); one-phase-at-a-time is the norm; between P1→P2 show "Top Automations we recommend" (aim-for-next slot); P1 = 30-day full refund, clock starts at website-live-with-AEO-SEO, VOIDED if customer advances to P2 early (track + surface this); P2 = 3-month ROI guarantee (calcs forthcoming from Rob); reps see phase progress on the company page | DoD: phase tracker on company record (master + rep views), per-phase paid/owed, refund-window state machine as CODE, signal webhook armed
- [ ] Q41. **Phase 4 spinoff/equity registry (dump 7.22.26-2 + evening msg):** first-class OWNERS-ONLY records (Rob+Will; hidden from reps until ACCESS rollout enforces roles): spinoff entity, equity split terms, agreed-vs-SIGNED state, paperwork follow-up task auto-created. Seeded today by Max: `deal-gulf-coast-equity-phase4` (30% Gulf Coast RE, PROBABLE, unsigned) + existing `spinoff-homeclonevault` org (40/60 Alex, agreed unsigned) + tasks `task-gulf-coast-equity-signoff` / `task-homeclonevault-equity-signoff` (due 7/29, overdue-watcher covered). Also: surface FUTURE Phase-4 opportunities highlighted out of notes/meetings/emails (Caleb's CRM precedent) | DoD: registry view for Rob; equity fields structured (not notes-crammed); rep-hidden verified once ACCESS lands
- [x] Q42. **Rep pipeline click-through (Rob: "you cant even click into them in the pipeline") — BUILDABLE NOW, small:** /rep pipeline cards must open the account workspace / open-Lead view they represent | DoD: every pipeline card navigates to its lead detail; prod-verified click path
  - ✅ 7/23 driver: SHIPPED per Master View §7 spec (= §8 increment 1, gate A / Q46 R1 — the one Rob-approved buildable-now item; rest of queue gated on creds/Phase-4/master-view sign-off). Whole card on `/rep` is now the click target: name `<Link>` stretched over the card (`after:absolute after:inset-0`), Call/Email raised `z-10` so one-tap contact still works, money pill click-through (`pointer-events-none`) — zero dead zones, no nested anchors, CallButton untouched. Build green, deployed (mle-rob-dashboard-aktxlratw), prod-verified: 12/12 queue cards carry the overlay + `/rep/accounts/[id]` href, destination 200, health ok/70ms. Honest scope note: design doc's "same treatment for DealsBoard cards in a rep context" has no target yet — no rep-scoped board exists until Q46 R3; the click-vs-drag threshold lands there. (Also: vercel CLI upgraded 37.4.0→56.5.0 mid-increment — old endpoint refused deploys; stale `/usr/local/bin/vercel` shadows the new binary, use `~/.npm-global/bin/vercel`.)
- [x] Q43. **Notes/enrichment layout retrofit (quick win ahead of Q39 full redesign, per Rob's evening msg):** on person/company records: NOTES = human notes only, prominent; enrichment/provenance data collapsed at bottom (most recent line visible + "show all" expander); no crammed walls of text | DoD: record pages render notes-first with collapsed enrichment; critic-rob readability pass
  - 7/23 driver (inc.1): **BUILD SHIPPED — remaining for tick: critic-rob readability pass.** Pure `lib/notes.ts` (CR-3: deterministic split — human words above the first machine-marker line stay Notes; `ENRICHED…`/`Enrichment hunt…`/`Sources:` paragraphs from there on are enrichment blocks, explicit marker list not vibes; `composeNotes` is the loss-proof inverse). Record page (`app/people/[id]`): Notes box now shows + edits ONLY Rob's words (new `serialize` hook on `InlineTextarea` recomposes untouched enrichment on every save — an edit can never wipe provenance ⚠️ **corrected 7/23 inc.2 per critic punch #3 — true for anything on screen, but the recomposition is CLIENT-side, so enrichment written server-side after the tab loaded IS lost on the next save; "NEVER" holds only once the recompose moves into the API route**, round-trip test-pinned); new `EnrichmentSection` at the very bottom, "machine-gathered" labeled, most-recent block visible + "show all (N)" expander (§3.5 Attio precedent). 13 new tests on real prod note shapes (rob-acheson/michael/gary/daniella/david-cates) — **now 18 in `lib/__tests__/notes.test.ts` after the fixture correction + `lintNotes` work (corrected 7/23 inc.2, critic punch #6)** — 510/510 vitest at the time, 598/598 today, build green. Deployed from a CLEAN worktree at the commit (working tree carries Q47 overnight WIP — not shipped). Prod-verified: gary's record renders 2 collapsed ENRICHED blocks bottom, Notes empty-honest; daniella Notes = her human line only. NOTE: gate ambiguity resolved on the record — Q43 was queued as pre-approval quick win (bc8bdcd) and 7/23 driver log listed it buildable; design-doc §8 line 385's blanket "unlocks Q40–Q43" reads stricter — flagging in log, trivially revertable if Rob meant to hold it.
  - 7/23 driver (inc.2): **CRITIC-ROB READABILITY PASS RUN — REVISE 78/100, so Q43 does NOT tick.** Full review: `docs/reviews/CRITIC-ROB-Q43-notes-enrichment-2026-07-23.md` (gates: Fidelity 88 · UX 85 · Truth 80 · Engineering 78 · Recording 90 · Effort 92; overall = min gate). The core design PASSED on live prod — gary/daniella/michael/david-cates/rob-acheson all render notes-first with quiet collapsed enrichment, and the **company variant is genuinely covered** (company rows anchor on `/people/[id]`; verified on miga), so the inc.1 claim was not overstated there. Closed THIS increment: **punch #1** — `orgs.miga-food-manufacturing.notes` opened with a stray `| ` separator, so a live company record was showing `| Rob 2026-07-17: ownership resolved…` as its human note; pure prefix strip against prod (body byte-identical, pre-image captured), the one shape `notes.test.ts:108-112` already names — and **punch #6**, the recording drift (the note said "13 new tests"; the file actually carries **18** after the later fixture-correction + `lintNotes` work landed in aca8325 without its own Q43 line — this line is that log). STILL OPEN, and they are the tick: **#2** `lintNotes` is dead code (zero consumers — a guarantee in prose, not code; CR-3 — wire it into ThingsToAddress and miga would have flagged itself); **#3** the inc.1 claim "an edit can NEVER wipe provenance" is stronger than the code — recomposition is a CLIENT closure over render-time enrichment feeding a blind PATCH, so enrichment appended server-side after Rob's tab loaded (overnight agent runs — exactly his pattern) is lost on his next notes save; the fix is to recompose in the API route against the stored row, and until then the NEVER wording is softened here; **#4** two shipped writers still cram machine tags mid-line into human notes (`lib/leads/recycle.ts:60-63` via the recycle cron, `lib/csvMapping.ts:168`) using prefixes no marker or lint regex recognizes — Q43's discipline must bind every notes writer, not just the editor; **#5** the "machine-gathered" badge sits over daniella's Rob-confirmed ALIAS line (correct splitter behavior, wrong word — soften to "auto-appended"). **#7** (mobile column order puts enrichment above EstimatePanel) deliberately DEFERRED to Q39's redesign rather than churned now. Critic's own path to ship: fix 1-4 → re-scores ≥95. **Next increment = punch #2 + #5 (small), then #3 + #4.**
  - 7/23 driver (inc.3): **punches #2 + #5 CLOSED. Still open (and still the tick): #3 + #4.** #2 — `lintNotes` has a consumer now, so the guarantee is code (CR-3): new pure `lib/integrity/notes.ts` (`findNoteShapeIssues`, `noteShapeFlagTitle`, `noteShapeFlagDetail`) runs inside the nightly `/api/cron/integrity` pass over every `people` + `orgs` note and raises a **per-record** Things-to-Address flag (`entity_id` = the record, so it lands on that record's own page, unlike the orphan/credential flags on the "CRM integrity" pseudo-entity); idempotency keyed on (entity_id, title) so re-runs never duplicate; severity `low`. #5 — the `EnrichmentSection` badge now reads **"auto-appended"**, not "machine-gathered" (the splitter parks Rob-confirmed addenda like daniella's ALIAS line in the last block by design, so the badge describes HOW text arrived, not authorship). Found + fixed while proving it: `LEADING_SEPARATOR` in `lib/notes.ts` matched `-`, and a read-only scan of live prod flagged gulf-coast's real bullet `- Replace Boomtown` — `-` dropped from the class, bullet-list fixture pinned, because a watchdog that flags Rob's own bullets is noise. Post-narrowing live scan: **0 bad shapes on prod** (miga's was inc.2's fix), i.e. this is a regression net, not a backlog. 606/606 vitest (7 new in `lib/integrity/__tests__/notes.test.ts`), build green, deployed (badge visible). **Next increment = punch #3 (move the notes recompose into the PATCH route so server-appended enrichment can't be lost) then #4 (bind `lib/leads/recycle.ts:60-63` + `lib/csvMapping.ts:168` to the marker vocabulary).**
  - 7/23 driver (inc.4): **punch #3 CLOSED — the recompose is SERVER-side now, and the "an edit can never wipe provenance" claim is finally true as written.** The Notes box sends ONLY Rob's human draft as a virtual field `notesHuman` (deliberately NOT in `FIELD_MAP` — it is not a column); `PATCH /api/admin/people` re-reads the target row's stored `notes` at save time and calls new pure `applyHumanNotesEdit(stored, draft)` in `lib/notes.ts` to put the enrichment blocks back. Same `isOrgId` people/orgs routing as every other field; non-string `notesHuman` → 400, no matching row → 404 (never a false "saved"). Removed rather than left as a shadow path: `InlineTextarea`'s `serialize` hook + `PersonEditor`'s client `composeNotes` import (`EnrichmentSection` reads the split independently). **DoD PROVEN ON LIVE PROD:** `gary-waskivich` pre-image captured → a new `ENRICHED` block written straight to Supabase (the overnight append) → real PATCH sent carrying the human text *as it looked before that block existed* → race block SURVIVED, both original ENRICHED blocks intact, human edit landed; row restored **byte-identical** to the pre-image (verified). That proof also confirms the deploy carried the new route — the old one would have 400'd `notesHuman`. 610/610 vitest (4 new), build green, deployed. **Next increment = punch #4** (bind `lib/leads/recycle.ts:60-63` + `lib/csvMapping.ts:168` to the marker vocabulary), then the critic re-score.
  - 7/23 driver (inc.5): **punch #4 CLOSED — all four blocking punches (#1–#4) are now shut; the ONLY thing left for the tick is the critic-rob re-score.** Both stragglers wrote by string-gluing onto stored text (`withRecycleTag`: `` `${notes} ${tag}` ``; `planRealImport`: `` `${p.notes} ${stamp}` ``), landing `[recycle_candidate …]` / `[import: …]` at the end of Rob's own sentence where the line-anchored splitter can't see them — so they rendered inside the human Notes box. Fixed as ONE path, not two patched call sites: new pure `appendMachineNote(notes, block)` in `lib/notes.ts` (own blank-line-separated block always; empty block = no-op), both writers call it. Both tag shapes added to `MARKER` (splitter files them as enrichment) AND `MID_LINE_MARKER` (lint → nightly integrity flag catches any future writer/hand-edit that regresses). Honest scope: this governs NEW writes, not history — read-only prod scan FIRST (22 people + 19 orgs) found **zero** rows carrying either tag in any position, so no cleanup backlog and no flag-flood from widening the lint; regression net, not debt. `hasRecycleTag` is a substring match → cron idempotency unaffected. 615/615 vitest (5 new; the 2 existing tag-format assertions updated to the new shape, not deleted), build green, lint clean. No user-visible surface → no deploy. **Next increment = critic-rob re-score of Q43** (his path to ship: fix 1–4 → ≥95); #7 stays deferred to Q39. ATLAS: Architecture Atlas needs its refresh pass (dated 2026-07-23).
  - 7/24 driver (inc.6): **✅ Q43 TICKED — critic-rob re-score = SHIP 95/100, zero auto-fails** (`docs/reviews/CRITIC-ROB-Q43-notes-enrichment-RESCORE-2026-07-23.md`; Fidelity 97 · UX 95 · Truth 98 · Engineering 95 · Recording 95 · Effort 97, overall = min gate). His stated path ("fix #1–#4 → ≥95") was met, and he re-proved each one from source rather than from this queue: the `lintNotes` cron consumer read in `app/api/cron/integrity/route.ts` + confirmed scheduled in `vercel.json`; the server-side recompose read in the PATCH route with a repo-wide grep showing **zero** surviving client `composeNotes`/`serialize`/`field="notes"` paths; both machine writers confirmed on the shared `appendMachineNote`; miga's leading pipe confirmed gone by reading live prod. His independent numbers matched ours exactly (615/615 vitest / 58 files, 22 people + 19 orgs, zero `[recycle_candidate`/`[import:` rows, zero lint findings across all 41 live rows, gary's 4 ENRICHED blocks) — no drift left in the record. **#7 deferral JUDGED LEGITIMATE**, because Q39 scope (d) READABILITY already owns "enrichment minimized at BOTTOM", so Q39 cannot close without it. This increment also cleared his three explicitly non-blocking hygiene punches so nothing dangles past the tick: (1) `notes` DELETED from `FIELD_MAP` (`lib/adminEdit.ts`) — no client sends it, so it was purely an open door for a future client to bypass `applyHumanNotesEdit` and re-create the provenance wipe punch #3 killed; pinned by a test asserting `FIELD_MAP.notes` is undefined AND that a raw `notes` patch produces an empty row (**616/616 vitest**, build green); (2) the orphaned `PRD` "Updated: 2026-07-24" date bump now belongs to a real revision row (3.1.102) instead of drifting with no content — the stale-input pattern he named; (3) punch #7 written into `docs/plans/MASTER-VIEW-2.0-DESIGN.md` §3.5 as an explicit DoD condition (does not close while enrichment can render above live record content at ANY breakpoint), so Q39's builder meets it in the design doc, not buried in a Q43 sub-line. No user-visible surface changed → no deploy. ATLAS: Architecture Atlas needs its refresh pass (dated 2026-07-24) — Q43 closed out the notes/enrichment write path (`notesHuman` virtual field, server-side recompose, single `appendMachineNote` writer, nightly note-shape lint), which the Atlas should reflect.

- [ ] Q44. **Customer Blueprint portal (dump 7.22.26-3, Rob: "Now this is very important....Every Customer will have their own login showing their own Blueprint") — OUT of Master View 2.0 scope, tracked here so it can't be lost.** Third audience beyond master/rep (MASTER-VIEW-2.0-DESIGN.md §1): each customer logs in and sees THEIR OWN Blueprint as a portal variant of the Q40 phase tracker — component lights + phase names + NEXT UP, demo grammar (mylocaleverything.com/app?demo=1), NO financials, no internal notes, no other customers. GATED ON: Q40 tracker shipped (increments 8a-c) + ACCESS rollout (customer logins provisioned from Rob's admin portal, per 7.21.26-1) | DoD: customer login renders their own Blueprint lights; zero financial fields in the customer-facing payload

## ✅ ROB DECISIONS 2026-07-22 late (answered via question batch — these unblock Q40/Q41 back-half):
- **Manual phase toggles: YES** — acceptable interim until Will's tools send phase-signal webhooks (contract already drafted).
- **Phase pricing: standard list price per phase + per-customer override.**
- **Phase-4 registry: route-hidden pre-ACCESS is approved** — ship at unlinked owners route now; real auth locks it at ACCESS rollout.
- **Phase lights: Max drafts the P1/P2/P3 component checklists, Rob edits/approves** (P1 confirmed includes website-live-w/-AEO-SEO — it starts the refund clock).
- **Labels: Option A approved WITH AMENDMENT** — relationship labels (Owner/Champion/Connector) are NOT enough for people; Rob: someone may be "a VP, or a Bookkeeper that we need to send the Invoice to" → people also need a functional ROLE designation (job title + invoice-recipient flag). ALSO: Rob spotted the mostly-empty `business` field on people rows in Supabase — design must resolve that field's fate (legacy vs orgs-split redundancy vs repurpose); "figure out the language we want to use."

- [ ] Q45. **Meeting Booked pipeline stage (Rob 2026-07-22 direct ask):** add `meeting_booked` to the deal stage ladder (between `contacted` and `meeting_held`) — pipeline column on /deals AND apparent on the company lead view (master + rep variants); scoring ladder + stage-aging thresholds updated to include it; existing deals unaffected | DoD: stage in ladder/board/company view, drag+audit trail work through it, todayRules/scoring account for it, prod-verified **7/23 inc.1 (driver): the UNGATED slice — `STAGE_AGING_DAYS` two-tier `meeting_booked` rung SHIPPED as pure code (design §7b, §8 row 11 leg 1). `lib/tasks/todayRules.ts`: fallback rung `meeting_booked: 7` days-in-stage + new exported `MEETING_BOOKED_GRACE_DAYS = 2` primary tier clocking off the linked meeting activity's own datetime (`stageAgingItems` takes an OPTIONAL `activities` arg — existing callers unchanged; `whoDoITouchToday` passes them). A meeting booked a week out yields a negative age, so it can't false-positive — test-pinned, which was the whole reason for two tiers. NA-4 verified unaffected (`followupLagItems` filters to `contacted` only, so the new rung can't leak into Rob's ledger). 5 new tests incl. the DoD ladder-order gate (`contacted < meeting_booked < meeting_held`); 562/562 vitest, build green. NO DEPLOY — zero user-visible surface (only consumer is `/api/tasks/today`; the rep Today band that renders it is Q46 R2, Rob-gated), Q52/Q53 precedent. Honest remainder for the tick: stage chip on the company lead view (master + rep) + drag/audit-trail pass + prod verification — all Q39-approval-gated, NOT buildable now.**

- [x] Q46. **Task MC.16 first slice — `/api/health` + uptime watch (picked 2026-07-22 late as top UNBLOCKED work: Q39–Q45 Rob-gated on the master-view approval ask, Q15/Q5b creds-blocked, Q16/Q6 Phase-4-gated; zero conflict with the Q39-frozen surfaces):** health endpoint probing the real dependency + an external monitor that alerts the flags ledger when the site/DB is down | DoD (this slice, not all of MC.16): unauth prod curl to /api/health returns ok+latency w/ data-free payload; DB-down reads 503; n8n uptime watch fires on schedule and a forced failure lands a flag via the existing error-alarm path
  - 2026-07-22 driver (inc.1): **ENDPOINT LIVE ON PROD.** Pure `lib/integrity/health.ts` (CR-3: response shaping only, no clock/network; payload structurally data-free — ok/store/db/latencyMs/error key-whitelist test-pinned, never counts/names/secrets) + `GET /api/health` (real one-head-count Supabase probe, not a vanity 200; STORAGE_SOURCE=file → honest `db:"n/a"`; DB error → 503 so a bare status-code monitor is a complete check) + proxy `isPublicPath` exemption (EXACT match — `/api/health/extra` stays gated, test-pinned). 5 new tests, 431/431 vitest, build green, deployed (`dpl_7ePmVybSzkMA7BFVSCxMkpZHvAgQ`, aliased) + prod-curled: `{ok:true,store:"supabase",db:"ok",latencyMs:156}` 200; `/api/network` 200 regression. REMAINING (inc.2): n8n uptime watch — schedule (~15 min) → HTTP GET /api/health w/ `errorWorkflow` → existing alarm `VoOFOPGqObGWe5Jr` → flags ledger (Q23 pattern; no new receiver needed — non-200 throws in the HTTP node, alarm dedupes); forced-failure DoD proof; then MC.16's backup/secrets remainder stays open on the PRD.
  - 2026-07-22 driver (inc.2): **UPTIME WATCH LIVE — Q46 TICKED.** n8n `MLE CRM — Uptime Watch /api/health` (`Dqpm48FtsjiwyUMO`, published/activated, every 10 min): probe w/ neverError+fullResponse → `Healthy?` (200 AND body.ok) → done; else fetch open flags → dedupe on title → POST high-severity flag to `/api/admin/flags`. Design deviation from inc.1 sketch, on purpose: failure handling is INLINE (deterministic branch, CR-3, provable by manual exec) instead of relying solely on `errorWorkflow` — which is still wired (`errorWorkflow → VoOFOPGqObGWe5Jr`) as backstop for n8n-internal crashes; same ledger either way. PROOF, all live execs: #269 healthy → no flag; forced 404 URL → #270 filed flag id 16 `{ok:true}`; #271 re-run → "Open flag exists — skip" (dedupe held, no spam); URL restored, flag 16 self-resolved w/ note (archive keeps proof), final prod curl `{ok:true,db:ok,67ms}`. Honest limits, on the record: (a) total-site/DB outage can't file a flag into the same down stack — flags-ledger alerting is in-stack by design (same accepted tradeoff as Q23); off-stack alerting (email/SMS) would ride Q15/Twilio creds; (b) scheduled firings begin from activation (manual execs proved the full path — same schedule primitive as proven Q24/Q38 watchers). Bearer-free: /api/health is public by design, so no rotation coupling. MC.16 remainder (backup+verification, outage-fallback, secrets grep, bundle check) stays open on the PRD.
  - ⚠️ ATLAS REFRESH NEEDED (2026-07-22): Q46 added a new infra component (n8n uptime watch → health endpoint → flags ledger). Architecture Atlas republish rides the next interactive session, per Rob's zero-mixups standing order.

- [x] Q47. **Task MC.16 second slice — nightly backup + verification (picked 2026-07-23 as top UNBLOCKED work, same gate map as Q46: Q39–Q45 Rob-gated, Q15/Q5b creds-blocked, Q16/Q6 Phase-4-gated):** bearer-gated `/api/cron/backup` that snapshots every CRM table to the private `backups` storage bucket and VERIFIES the stored copy before certifying | DoD: prod run uploads dated object + `latest.json`, re-downloads the stored copy and verifies it against INDEPENDENT live head-counts (truncation/corruption caught, never a "file landed" vanity check); wrong/absent bearer → 401; verification failure files ONE deduped high flag to Things to Address; n8n nightly schedule fires it (Q24/Q38 pattern) and a forced failure proves the flag path
  - 2026-07-23 driver (inc.1, watchdog recovery): **ROUTE LIVE + FIRST VERIFIED BACKUP ON PROD.** Prior headless run was killed at the 10-min timeout (rc=142) with this slice's code stranded uncommitted — recovered, verified, landed. Pure `lib/integrity/backup.ts` (CR-3: snapshot shaping + verifier, no clock/network; 12-table registry w/ per-table order keys — `dedup_review` pages on its `pair_key` PK; verifier refuses an empty-people "backup" even when counts agree) + `GET /api/cron/backup` (same bearer contract as recycle/dedup crons: no CRON_SECRET → 503 inert, wrong bearer → 401; paged row fetch → upload dated object + `latest.json` upsert → re-download the STORED copy → verify vs independent head-counts; failure files one high flag deduped on open title). 13 new tests (444/444), build green, deployed + prod proof: bearer curl `{ok:true, verified:true, objectName:"crm-backup-2026-07-23.json"}` w/ live counts (22 people/18 orgs/47 edges/43 dev_chat), unauth + wrong-bearer both 401, bucket listing shows both objects at 223KB in private `backups`. REMAINING (inc.2): n8n nightly schedule (~04:00 ET → bearer GET, Q24/Q38 clone) + forced-failure DoD proof (e.g. temp-break bucket name → flag files → restore → flag self-resolves); then MC.16 remainder (secrets grep, bundle check, outage-fallback proof) stays open on the PRD.
  - 2026-07-23 driver (inc.2): **✅ Q47 TICKED — n8n nightly schedule LIVE + forced-failure flag path proven on live execs.** Workflow `f99xNvSpKIy95k4Q` "MLE CRM — Nightly Backup /api/cron/backup" (published/activated, version 82eef9de): Schedule daily 03:33 → HTTP GET w/ Authorization bearer (120s timeout, neverError+fullResponse) → `Verified?` (200 ∧ `"ok":true` ∧ `"verified":true`) → done; else fetch flags → dedupe on open title → POST high flag; `errorWorkflow → VoOFOPGqObGWe5Jr` backstop (Q46 inline+backstop design). PROOF, live execs: #288 healthy → `Verified — done` (`verified:true`, 12 live counts); #284 forced-404 URL → flag #24 filed `{ok:true}`; #285 re-run → "Open flag exists — skip" (dedupe held); URL restored, flag #24 self-resolved w/ note. TWO REAL BUGS caught & fixed during the proof: (a) `responseFormat:json` + HTML 404 page makes the HTTP node THROW before the inline branch (the Q46 uptime watch shares this latent gap — its own forced-404 flag #16 detail shows the same throw message; inline branch never ran there either, errorWorkflow carried it) → switched to text format; (b) n8n text-mode puts the payload under `$json.data`, JSON-mode under `$json.body` — branch + flag detail now read `body ?? data` (execs #286/#287 documented the miss: healthy runs walked the failure branch and only dedupe stopped a false flag). BONUS live proof of the ROUTE's own flag path: real flag #20 (04:37Z run during inc.1's buggy window — `dedup_review.id` ordering) — resolved w/ note, runs verify clean since e8a3e8e. ⚠️ registry: bearer hardcoded in the HTTP node (API can't create n8n creds) — CRON_SECRET rotation must update `ozMXSpftU2lIbhp2` + `EsDSJQoJwzLkJOhR` + `f99xNvSpKIy95k4Q` by hand; live session should swap all three to an httpBearerAuth cred. First scheduled firing: tonight 03:33 (manual execs proved the full path; same schedule primitive as Q24/Q38/Q46). No repo code changed (n8n + flags + docs increment). MC.16 remainder (secrets grep, bundle check, outage-fallback proof) stays open on the PRD.
  - ✅ Q46 FOLLOW-UP APPLIED (2026-07-23 driver): uptime watch `Dqpm48FtsjiwyUMO` patched with the Q47-proven fix — probe `responseFormat` json→text, `Healthy?` condition `$json.body.ok`→substring `"ok":true` on `body ?? data`, flag detail reads `body ?? data ?? error` (sliced 300). PROOF on live execs (workflow stayed active throughout, versionId 2ed41632): #291 healthy → `Healthy — done` (text-mode `data` string parsed correctly — the exact miss Q47's execs #286/#287 documented); #292 forced-404 (HTML error page) → **inline failure branch RAN, no throw** → flag #25 filed `{ok:true}` — this was the whole gap: previously the HTTP node threw before the branch and only errorWorkflow carried it; #293 re-run → `Open flag exists — skip` (dedupe held); URL restored, flag #25 self-resolved w/ note, final prod curl `{ok:true,db:ok,84ms}`. Both MC.16 watchers (uptime + nightly backup) now share the identical proven response-shape handling. No repo code changed (n8n + flags + docs increment).
  - 2026-07-23 driver (nudge inc.1): **NUDGE CRON WIRED — `GET /api/cron/esign-nudges` (code green, not yet scheduled).** First increment off the released claim. Bearer contract identical to backup/overdue/recycle (CRON_SECRET unset → 503 inert, wrong bearer → 401; `/api/cron/*` already proxy-exempt). Reads open `signature_requests` (+ document title embed) and every prior `nudge` event, runs the pure tested `planNudges`, executes: customer rungs → email via `deliverEsignEmail` (n8n sender), rep/Rob rungs → flags deduped on the OPEN title, never both. **Idempotency is the event, and the event is written ONLY after the action actually happened** — an unconfigured/failed sender writes no event and the rung retries next run (surfaced in `deferred[]`), so a dead mailer can never silently eat a nudge. `markStalled` is REPORTED (`stalledDocuments[]`), NOT executed — the deal ladder has no Stalled stage, so nothing is mutated (hard limit); the 14-day flag copy was corrected same-commit to stop claiming a board change that doesn't happen. 2 new pure mappers + 6 tests (PostgREST object-or-array embed; never fabricates a document title; rung-less junk events dropped so they can't suppress a real nudge — round-tripped against the planner both ways). 568/568 vitest, build green. NEXT: n8n hourly schedule (Q24/Q38/Q47-backup clone, text responseFormat + `body ?? data` per the proven fix) → forced-nudge live proof on a synthetic request → then countersign / org+deal DocumentsSection mounts / SMS (Q5b) / counsel reviews.
  - 2026-07-23 driver (nudge inc.2): **HOURLY SCHEDULE LIVE — the ladder now runs on a clock, proven end-to-end against prod.** inc.1's route was committed but never deployed (cron-only surface), so prod 404'd it and no schedule could have worked — deployed first (`vercel --prod`), then re-verified the bearer contract **on prod itself**: no bearer → 401, correct bearer → 200 `{"ok":true,"open":0,"planned":0,"emailed":0,"flagged":0}`. n8n workflow `CxFUrjo29NiYMofS` "MLE CRM — Hourly E-Sign Nudge Ladder": Schedule Trigger hourly at **:17** (offset from the overdue watcher's :33 so the two never collide) → bearer GET, 60s timeout, `retryOnFail` ×3 — same shape as the proven `ozMXSpftU2lIbhp2`; published/active and **executed live through n8n** (exec 425, success, node output = the same 200 ladder result). Hourly is right because the planner DEFERS out-of-hours customer rungs instead of skipping them. ⚠️ **rotation registry: CRON_SECRET now lives in FOUR n8n workflows** (`ozMXSpftU2lIbhp2`, `EsDSJQoJwzLkJOhR`, `f99xNvSpKIy95k4Q`, `CxFUrjo29NiYMofS`) + Vercel prod + `.env.local`. **Honest scope: this proves the wiring, NOT a delivered nudge** — prod has zero open signature requests today (E2E rows were cleaned), so `planned=0` is the correct output, not a test of the email leg. STILL OWED for the tick: forced-nudge proof on a real open request → countersign flow → org/deal DocumentsSection mounts (note: no `/orgs/[id]` or `/deals/[id]` record page exists yet — company rows anchor through `/people/[id]`, so the "org mount" is already served; the deal mount needs a deal record surface, which is Q39/Q40 territory) → SMS (Q5b creds) → counsel reviews.
  - 2026-07-23 driver (nudge inc.3): **schedule HARDENED + a duplicate-infra near-miss caught and cleaned, same session.** (a) `errorWorkflow → VoOFOPGqObGWe5Jr` (the flags-ledger alarm) is now set on `CxFUrjo29NiYMofS`, so a failing hourly nudge run files a deduped flag on Things to Address instead of dying silently in n8n — the backstop the Gmail capture, backup and uptime watchers already had, and the one thing inc.2 left off. (b) **Near-miss, recorded not buried:** inc.2 was written by a run that was killed (rc=142) before it could commit, so its BUILD-QUEUE/PRD annotations sat uncommitted in the working tree; this run read the queue item but acted before reading those uncommitted lines, re-deployed (harmless — deploy `dpl_8RQ6q8A79dXpu7LuxqPbvGrUGQk7`, prod re-verified: no bearer → 401, bad bearer → 401, correct bearer → 200 `{"ok":true,"open":0,...}`, `/api/network` + `/api/health` 200) and created a SECOND identical hourly workflow `jblVjEPCq9NOeqDz` — which would have double-fired the ladder every hour. Caught before commit; the duplicate was executed once (exec 428, 200) then **archived**, and `search_workflows` re-verified exactly ONE active nudge workflow remains (`CxFUrjo29NiYMofS`). Rotation registry unchanged at FOUR workflows. Permanent fix + root cause logged in `~/.claude/memory/INCIDENT-LEDGER.md` (rule: read the *uncommitted* working-tree annotations for the item before touching external infra). (c) **The forced-nudge proof was deliberately NOT faked:** seeding a synthetic open signature request would write `nudge`/`sent` rows into `signature_events`, which is **append-only enforced in the database** (`signature_events_no_update` fires `before update or delete`) — a fabricated rung could never be removed from the very audit chain built to win a court fight. So the proof waits for a REAL open request (or a scratch DB), and Q47 stays unticked on that leg.
  - 📐 ATLAS 2026-07-23 (Q47, inc.2): refresh pass must also add the **hourly e-sign nudge cron** leg — n8n `CxFUrjo29NiYMofS` (hourly :17) → bearer `GET /api/cron/esign-nudges` → pure `planNudges` ladder → customer rungs email via the agreement sender `EIR0mgUWcn26rsjD`, rep/Rob rungs → Things to Address flags, `nudge` events as the idempotency ledger. Four cron workflows now share CRON_SECRET; the Atlas should show that fan-out so a rotation can't miss one. Artifact republish requires the interactive session.
  - 📐 ATLAS 2026-07-23 (Q47): refresh pass (pending, see prior notes) should add: nightly n8n `f99xNvSpKIy95k4Q` → bearer `GET /api/cron/backup` → dated object + `latest.json` in private `backups` bucket → re-download verify → flags on failure (route-internal + n8n inline + errorWorkflow backstop). Artifact republish requires the interactive session.

- [x] Q48. **Task MC.16 third slice — secrets grep + no-secrets-in-client-bundle (picked 2026-07-23 as top UNBLOCKED work, same gate map as Q46/Q47: Q39–Q45 Rob-gated on master-view approval, Q15/Q5b creds-blocked, Q16/Q6 Phase-4-gated):** verify secrets are externalized (repo grep clean of literal credentials) and no secret value ships in the client bundle | DoD: sweep clean on both surfaces, and the sweep itself repeatable ✅ **DONE 2026-07-23 (one increment; not a Q4/Q6/Q9 milestone → no critic-rob gate)**
  - 2026-07-23 driver: shipped AS CODE per CR-3 — `scripts/secrets-sweep.mjs`, three gates: (1) `git ls-files` has zero `.env*` (gitignore line 34 covers); (2) zero literal secret VALUES in tracked files — JWT pairs `eyJ….eyJ…`, `sk-ant-`/`sk-proj-` keys, Twilio `SK`/`AC`+32hex — swept including docs/md, 0 hits; (3) zero `.env.local` values (≥8 chars, 10 checked) in any `.next/static` client chunk (34 files, fresh build). Detector PROVEN both ways: forced-leak test planted the service key in a static chunk → exit 1 naming the exact file; removed → CLEAN again (not a rubber stamp). Prod spot-check: 9 live chunks off mle-rob-dashboard.vercel.app grepped for `eyJhbGciOi|SERVICE_ROLE|CRON_SECRET|sk-ant` — clean. The DoD's `git grep -i password` read HONESTLY, not literally: 17 hits audited, every one is the `DASHBOARD_PASSWORD` env-var NAME, doc narrative, or comment — zero literal credential values (renaming the env var to zero the grep would be churn, not security; the sweep's literal-value gate is the real permanent enforcement). No app code changed, no deploy (nothing user-visible). **MC.16 remainder is now ONLY the outage-fallback proof** (simulated store outage → dashboard serves from last backup — base 1.6's folded DoD; natural next driver pick). PRD MC.16 progress + rev 3.1.70 same-commit.

- [x] Q49. **Task MC.16 final slice — outage-fallback proof (picked 2026-07-23 as top UNBLOCKED work, same gate map as Q46–Q48: Q39–Q45 Rob-gated on master-view approval, Q15/Q5b creds-blocked, Q16/Q6 Phase-4-gated):** simulated store outage → dashboard serves from last backup, with the backup→fallback restore path shipped as code | DoD: base 1.6's folded requirement proven end-to-end ✅ **DONE 2026-07-23 — closes MC.16 (one increment; not a Q4/Q6/Q9 milestone → no critic-rob gate). MC.16 TICKED on the PRD — all four DoD legs now proven.**
  - 2026-07-23 driver: **RESTORE PATH AS CODE (CR-3):** `scripts/regen-fallback.mjs --from-backup` — downloads `latest.json` from the private `backups` bucket (the Q47 VERIFIED snapshot), runs the same raw-row mappers as the live mode (zero drift — one mapper, two sources), refuses non-snapshot/empty-people payloads (mirrors the verifier's empty-guard), writes `data/network.json` stamped with the snapshot's `takenAt`. Ran it: fallback went stale-38 → **40 nodes (22 people + 18 companies), 47 edges, 8 verticals, 12 projects — exactly the verified backup's counts**, restoring 2 nodes the old live-regen (7/22) predated. **SIMULATED OUTAGE PROVEN locally on the prod build:** `STORAGE_SOURCE=supabase` + unreachable `SUPABASE_URL` → `/api/health` honestly **503**; `/api/network` served **34 nodes/47 edges from the backup-derived file — byte-count-identical to live prod's response** (route filters 6 org rows both ways); homepage rendered the amber truth banner "Supabase unreachable — serving fallback snapshot data. Edits are paused until it recovers."; server log shows the no-stall fallback engaging per read; writes untouched (fail loudly by design, index.ts — no silent fork). 444/444 vitest, build green, **deployed** (`mle-rob-dashboard-dm5cjma23`) so PROD's bundled fallback IS the last verified backup; prod regression: health ok/58ms, network 34/47. **Honest limits on the tick:** (a) fallback freshness = last `--from-backup` run committed+deployed (base 1.6's DoD predates the CRM tables; refresh is one command — a deploy-time auto-regen was considered and rejected: build must not depend on DB reachability, that's the no-stall rule eating itself); (b) `data/crm.json` doesn't exist, so deals/activities/tasks reads serve EMPTY during a total outage (network graph/ledger/records = the covered dashboard surface; CRM-row offline coverage would need the TS mappers exposed to the script — follow-up only if Rob wants it). PRD MC.16 [x] + rev 3.1.71 same-commit.
  - 📐 ATLAS 2026-07-23 (Q49): refresh pass (pending, see prior notes) should close the MC.16 hardening loop on the map: nightly verified backup (`f99xNvSpKIy95k4Q` → `/api/cron/backup` → `backups` bucket) → `regen-fallback.mjs --from-backup` → `data/network.json` → no-stall read fallback (`lib/storage/index.ts`) + amber banner — the full outage story in one chain. Artifact republish requires the interactive session.

- [x] Q56. **Task MC.6 — systems & webhook field inventory (picked 2026-07-23 as top UNBLOCKED work; gate map unchanged: Q39–Q45 Rob-gated on master-view approval, Q15/Q5b creds-blocked, Q16/Q6 Phase-4-gated, Q46 R2+ awaiting Rob's research nod; MC.1 Rob-gated on stage→probability, MC.5 explicitly last, MC.9 wants open-Q6 freshness-SLA answer + builds ON this inventory)** | DoD: per PRD MC.6 — consolidated field table per system (`clients/<slug>.json` schema, Documenso IDs + signed-PDF URLs, CRM adapter fields, Cal.com/Fathom/Documenso/Twilio/Retell webhook payloads), each with source URL
  - 2026-07-23 driver (inc.1): INTERNAL LEGS + CAL.COM ROW DONE — `docs/research/MC6-SYSTEMS-FIELD-INVENTORY-2026-07-23.md`. §1 `clients/<slug>.json` full field table from the ONE live instance (`contracts/clients/gulf_coast.json`; no written schema exists — inferred, n=1 stated honestly) w/ ingestion traps pinned: `fee` is a `$`-string not a number, empty-string-as-null dates, free-text `agreement_status.status` can outrun the date fields (Gulf Coast: `signed_date:""` yet status "effective — paid in full"), paid-state ALSO encoded in the invoice file path. §2 Documenso IDs/signed-PDF URLs: honest verdict = PLANNED NOT DEPLOYED, zero IDs exist anywhere, the one real agreement went via DocuSign → MC.9's Documenso leg starts clean (no backfill). §3 onboarding-PRD Phase-5 adapter field contract mapped onto our in-house CRM: exists (org/person/signed/stage) vs missing (AI number, Retell agent ID, Documenso envelope, Terms-Reviewed, 30-day expiry→Q40); noted `/api/leads` + a future `LEADS_KEY_ONBOARDING` product key supersedes the onboarding repo's Python-adapter-to-third-party-CRM plan. §4a Cal.com row closed via the MC.4 verdict evidence (zero rework). Docs-only — no code, no deploy, no vitest delta. REMAINING (inc.2): §4b–d Fathom/Documenso/Twilio webhook field rows w/ FETCHED doc URLs (Twilio has a local head start — fields already consumed by `lib/twilio.ts`); §4e Retell row is likely OBE (stack = Twilio+Vapi HYBRID per Rob #40, post-dating base 8.1) → flag to Rob via /api/admin/flags per findings protocol, suggest Vapi row replaces it, don't silently drop. PRD: MC.6 in-progress note + rev 3.1.82 same-commit.
  - 2026-07-23 driver (inc.2, the tick): **EXTERNAL WEBHOOK ROWS DONE — MC.6 COMPLETE, TICKED on the PRD** (research task, not a Q4/Q6/Q9 milestone → no critic-rob gate). §4b Fathom: ONE event ("New Meeting Content Ready"), payload sections opt-in at webhook creation (`include_transcript/summary/action_items/crm_matches` — MC.9 must create the webhook with what it wants), Svix-standard verification (`webhook-id`/`webhook-timestamp`/`webhook-signature`, HMAC-SHA256 over `id.timestamp.body`, `whsec_` secret) — all doc-URL'd; leaf field names honestly deferred (payload page JS-rendered, returns `paths:{}` headless — exact URL pinned for MC.9's in-browser read). §4c Documenso: lifecycle events (`DOCUMENT_<STATUS>`/`TEMPLATE_<ACTION>`), envelope `{event,payload,createdAt,webhookEndpoint}`, `payload.id` IS the planned `documenso_document_id` (§2 join closed); trap for MC.9: the webhook example shows NO signed-PDF URL → plan a REST follow-up fetch after `DOCUMENT_COMPLETED`, don't assume it rides the webhook. §4d Twilio: every recording-callback param source-URL'd to official docs (`RecordingStatus`/`RecordingStartTime`/`RecordingSource`/`RecordingTrack` added beyond what `lib/twilio.ts` consumes). §4e Retell = OBE (Twilio+Vapi HYBRID, Rob #40 post-dates base 8.1) → **flag POSTed to /api/admin/flags (low), verified `{ok:true}` on prod** per findings protocol — Rob strike-or-keep; provisional Vapi row in place (server message types + `end-of-call-report` `artifact.recording/transcript/messages` fields, doc URL; binds to Q15's activities seam). Docs-only — no code, no deploy, no vitest delta. PRD: MC.6 [x] + rev 3.1.83 same-commit.
- [x] Q55. **Task MC.4 — lead-source taxonomy + UTM convention + Cal.com passthrough spike (picked 2026-07-23 as top UNBLOCKED work; gate map unchanged: Q39–Q45 Rob-gated on master-view approval, Q15/Q5b creds-blocked, Q16/Q6 Phase-4-gated, Q46 R2+ awaiting Rob's research nod; MC.1 still skipped — needs Rob's stage→probability approval)** | DoD: taxonomy doc; UTM convention table; Cal.com hidden-field/UTM passthrough yes/no verdict with evidence (or workaround) ✅ **DONE 2026-07-23 (2 increments; definition+research task, not a Q4/Q6/Q9 milestone → no critic-rob gate). MC.4 TICKED on the PRD.**
  - 2026-07-23 driver (inc.2, the tick): **CAL.COM PASSTHROUGH VERDICT: YES — via hidden booking fields ONLY.** `docs/research/CALCOM-UTM-PASSTHROUGH-VERDICT-2026-07-23.md`, evidence URL per claim (help/docs pages + calcom GitHub issues fetched today). The trap documented: Cal.com auto-tracks our exact 5 `utm_*` params BUT that Tracking-table data is host-UI-only — NOT in webhook/Zapier payloads (open calcom#24759, the feature request proves the absence). Reliable channel: hidden Short-Text booking questions with identifiers = `UTM_CONVENTION` params + `campaign_ref`, auto-filled by plain `utm_*` query params on the booking link (prefill-by-identifier is documented core behavior), delivered in `BOOKING_CREATED`'s top-level `responses` object (`{label, value, isHidden}`). `metadata[...]` query-param carrier REJECTED on evidence (regression calcom#16140). MC.9 binding recorded on its PRD row: six hidden questions per event type, workflow reads `responses.<id>.value` → `classifyUtm` (inc.1's shipped ladder), DoD test event must assert all six keys arrive so per-event-type config drift fails loudly instead of flooding `direct_unknown`. Bonus: webhook `responses`/`metadata` structure + URLs double-serve MC.6's Cal.com row (head-start note added there). Docs-only increment — no code, no deploy. PRD: MC.4 [x] + MC.9/MC.6 cross-refs + rev 3.1.81 same-commit.
  - 2026-07-23 driver (inc.1): **TAXONOMY + UTM CONVENTION SHIPPED as code (CR-3, Q52/Q54 pattern)** — `lib/leads/sourceTaxonomy.ts`: exact 5-value base-PRD enum (deliberately NO invented "paid ads" sixth bucket — paid-vs-organic is a `utm_medium` dimension; widening the enum is a Rob call, on the record), `UTM_CONVENTION` (5 params w/ reserved values; `lm-` campaign prefix joins 1.15's `campaign_ref` so per-lead detail and channel attribution meet on one string), deterministic `classifyUtm` 5-rung ladder (order test-pinned), `classifyLeadSource` (UTM evidence beats intake-type default; zero evidence → honest `direct_unknown`, never guessed prettier), `parseLeadSource` normalizer (null when unconfident), `INTAKE_TYPE_DEFAULT_SOURCE` total over Task 1.15's types (completeness gate: a new intake type without a rung fails the suite), `TAXONOMY_WORKED_EXAMPLES` pinned to the classifier. 7 tests, 473/473 vitest, build green. No deploy (definitions only, zero user-visible surface). Consumers: MC.2 `source_close_rate` domain (the adapter MC.2 named), MC.9 ingestion-time classification, MC.12/MC.15 group-by. Narrating doc `docs/plans/LEAD-SOURCE-TAXONOMY-SPEC.md`. REMAINING (inc.2, the tick): Cal.com hidden-field/UTM passthrough spike — yes/no verdict w/ evidence URLs (docs/webhook payload fields) or documented workaround; rides the same research seam MC.6 needs, so evidence gathered there double-serves. PRD: MC.4 progress + rev 3.1.80 same-commit.

- [x] Q54. **Task MC.2 — 4 marketing KPIs (picked 2026-07-23 as top UNBLOCKED work; gate map unchanged: Q39–Q45 Rob-gated on master-view approval, Q15/Q5b creds-blocked, Q16/Q6 Phase-4-gated, Q46 R2+ awaiting Rob's research nod; MC.1 skipped for now — its DoD needs Rob's stage→probability approval, MC.2's doesn't)** | DoD: each KPI has formula, input-source table, and a worked example ✅ **DONE 2026-07-23 (one increment; definition task, not a Q4/Q6/Q9 milestone → no critic-rob gate). MC.2 TICKED on the PRD.**
  - 2026-07-23 driver: shipped AS CODE per CR-3 (Q52/MC.3 pattern) — `lib/kpis/marketingKpis.ts`: Cost per Booked Call, Lead-Magnet Conversion, Source→Close Rate, Booking Volume by Channel; each with `formula`, `inputs[]` naming its source system (Cal.com via MC.9, activities-lake web_form per Task 1.15, Vercel analytics, ad platforms = manual entry — no ads integration planned), a pure deterministic compute fn, and a worked example in `MARKETING_KPI_WORKED_EXAMPLES` test-pinned to the fn (formula/example drift fails the suite). HONEST COVERAGE à la MC.3: a pin asserts NOTHING claims computable_today — Source→Close Rate blocked on MC.4 (deals has NO source column; fn takes `(source, isClosedWon)` pairs so MC.4 changes the adapter, not the math), booking KPIs blocked on MC.9, spend/visitors manual. Zero denominator → `null`, never fake 0/Infinity (truth-gate: MC.12's panel renders "no data"). 9 tests, 466/466 vitest, build green. No deploy (definitions only, zero user-visible surface). Consumers registered: MC.12 KPI Summary, MC.15 weekly rollup. Narrating doc `docs/plans/MARKETING-KPI-SPEC.md` (code canonical). PRD: MC.2 [x] + rev 3.1.79 same-commit.

- [x] Q53. **Task MC.13 — "Needs Action Today" Rob/ops widget (picked 2026-07-23 as top UNBLOCKED work; gate map unchanged: Q39–Q45 Rob-gated on master-view approval, Q15/Q5b creds-blocked, Q16/Q6 Phase-4-gated, Q46 R2+ awaiting Rob's research nod)** | DoD: per PRD MC.13 — seeded fixture for each rule surfaces exactly the expected items; widget renders the evaluated items + honest blocked-coverage state for Rob ✅ **DONE 2026-07-23 (3 increments; not a Q4/Q6/Q9 milestone → no critic-rob gate). MC.13 TICKED on the PRD.**
  - 2026-07-23 driver (inc.3, the tick): **WIDGET LIVE ON PROD** — `components/NeedsActionPanel.tsx` renders `/api/admin/needs-action` in the Overview's "Daily priorities" placeholder slot (the seam MC.3 itself names: "feeds the daily-priorities panel"; dashed placeholder replaced, zero Q39-frozen layouts touched — ThingsToAddress/stats/graph cards untouched). Plain-language rule labels (Rob's no-jargon bar: "New lead untouched", "Proposal overdue", "Signed, not invoiced" — rule ids stay internal), amber-dot items with the evaluator's deterministic reason line, count badge, `/deals` link. HONESTY CONTRACT RENDERED: `blocked` list shows as "Not yet watched: Discovery reminder — needs booking data that isn't in the CRM yet" (NA-2/MC.9, plain words); fetch failure states its own inability instead of hiding; empty state says what IS inside SLA rather than a bare "all clear". 457/457 vitest, build green, **deployed + prod-verified**: homepage 200 w/ "Needs action today" in the HTML and the old placeholder gone; `/api/admin/needs-action` live `{count:0, blocked:[NA-2]}` — the honest answer today (all deals inside their SLA windows; items-list rendering exercised by the route-level per-rule fixtures + plain list mapping). Note: original placeholder text promised Phase 5.1 AI-ranked priorities — that stays a separate future layer; this panel is the MC.3 SLA feed and says only what it is. PRD: MC.13 [x] + rev 3.1.78 same-commit.
  - 📐 ATLAS 2026-07-23 (Q53): refresh pass (pending, see prior notes) should add the completed Needs-Action chain to the map: `needsActionRules.ts` (MC.3 table) → `needsActionEval.ts` (pure evaluators, NA-4 shared with todayRules' `STAGE_AGING_DAYS` — ONE threshold table feeding BOTH the rep queue and Rob's panel) → `/api/admin/needs-action` → `NeedsActionPanel` on the Overview. Artifact republish requires the interactive session.
  - 2026-07-23 driver (inc.1): **EVALUATORS SHIPPED as pure code (CR-3, mechanical against the MC.3 table)** — `lib/tasks/needsActionEval.ts`: NA-1 `newLeadUntouchedItems` (new_lead >24h, no deal-anchored activity — table's deliberately-narrow anchor honored), NA-3/NA-5 via shared `stageLagItems` on `stageEntryAt` (latest `status_change` per Task 4.7, `deals.updatedAt` proxy fallback — exactly the table's fieldsRead), NA-4 **re-surfaced from todayRules' `stageAgingItems` contacted rung, NOT re-implemented** (one threshold table, ONE evaluator, two consumers — the 2.6/MC.13 reconciliation executed), NA-2 reported in a `blocked` list (honest coverage — the widget must show "blocked on MC.9", never fake completeness). Clock injected (`today`+`now`, whoDoITouchToday convention); demo-* excluded; composite deterministically table-ordered. Gate test pins table↔evaluator pairing: a rule added to MC.3 that's neither evaluated nor blocked FAILS the suite (`needsActionEval.test.ts`, 8 tests — every rule's positive + negatives incl. status_change-entry override). 455/455 vitest, build green. No deploy (pure lib, zero user-visible surface). REMAINING (inc.2): the widget itself — surface decision deliberate: master view is Q39-frozen, so it lands as a Rob/ops panel (MC.12 seam, likely `/api/admin/needs-action` + admin-gated panel) without touching Q39-frozen layouts; DoD's per-rule fixture proof then rides a route-level test (2.6 precedent). PRD: MC.13 progress + rev 3.1.76 same-commit.
  - 2026-07-23 driver (inc.2): **ROUTE LIVE + PER-RULE FIXTURE PROOF** — `app/api/admin/needs-action/route.ts` (2.6 pattern: thin/read-only/`force-dynamic`; pure lib evaluates, route feeds store rows + `todayInET`; response `{today, count, items, blocked}` — `blocked` IS the widget's honest-coverage contract, NA-2 rides it until MC.9). DoD's route-level leg met: `lib/__tests__/needsActionRoute.test.ts` runs the REAL GET handler on a temp `CRM_DATA_PATH` file store — one positive + one negative fixture PER evaluated rule (NA-1 touched/fresh negatives, NA-3 fresh, NA-4 fresh-contact, NA-5 just-signed via `status_change` — the stage-entry override proven through the route, not just the lib), demo-* exclusion, exact 4-item table-ordered composite + NA-2-only blocked list asserted. 457/457 vitest, build green. **No deploy** (API-only, zero user-visible surface — the panel is inc.3 and deploys+prod-verifies both). REMAINING (inc.3, the tick): Rob/ops panel rendering items + blocked state via the MC.12 seam, avoiding Q39-frozen master-view layouts. PRD: MC.13 progress + rev 3.1.77 same-commit.

- [x] Q52. **Task MC.3 — "Needs Action Today" Rob/ops SLA rule set (picked 2026-07-23 as top UNBLOCKED work; gate map unchanged: Q39–Q45 Rob-gated on master-view approval, Q15/Q5b creds-blocked, Q16/Q6 Phase-4-gated, Q46 R2+ awaiting Rob's research nod)** | DoD: rule table with trigger, action owed, SLA hours, exact field each rule reads ✅ **DONE 2026-07-23 (one increment; definition task, not a Q4/Q6/Q9 milestone → no critic-rob gate).**
  - 2026-07-23 driver: **table IS code per CR-3** — `lib/tasks/needsActionRules.ts`: NA-1 `new_lead_untouched` (24h; fields exist, evaluator = MC.13), NA-2 `discovery_reminder_missing` (24h; **blocked on MC.9** — no Cal.com bookings data, `meeting_booked` stage carries no start time), NA-3 `proposal_lag` (48h on meeting_held; deliberately NOT added to `STAGE_AGING_DAYS` — growing the rep queue is a todayRules/Q46 call, anti-notification-fatigue guardrail respected), NA-4 `followup_lag` (SLA **derived** from `STAGE_AGING_DAYS.contacted` — the 1.7 reconciliation directive executed, one threshold table two consumers; calendar-day proxy for base's "business days" adopted + documented), NA-5 `signed_not_invoiced` (24h; per G3 the stage ladder itself encodes un-invoiced — stage-only evaluator buildable now, CSV cross-check rides MC.9). Narrating spec `docs/plans/NEEDS-ACTION-SPEC.md`; TODAY-RULES-SPEC cross-ref closed out; invariants pinned by `lib/__tests__/needsActionRules.test.ts` (incl. the derivation — a todayRules threshold edit can't silently orphan MC.3). 447/447 vitest, build green. Definitions only → no deploy (zero user-visible surface). PRD: MC.3 [x], MC.13 marked UNBLOCKED (evaluator build now mechanical), rev 3.1.75 same-commit.
- [x] Q51. **Task MC.11 — n8n workflow map (picked 2026-07-23 as top unblocked work; gate map unchanged: Q39–Q45 Rob-gated on master-view approval, Q15/Q5b creds-blocked, Q16/Q6 Phase-4-gated)** | DoD: Mermaid map (trigger/nodes/target per workflow) matches n8n workflow list 1:1 ✅ **DONE 2026-07-23 (one increment; not a Q4/Q6/Q9 milestone → no critic-rob gate).**
  - 2026-07-23 driver: **Map published, 1:1 with the LIVE instance** — node-level detail API-pulled from boostn8n for every workflow. `docs/ops/N8N-WORKFLOW-MAP.md` (canonical; Mermaid renders on GitHub) + **visual page https://mle-rob-dashboard.vercel.app/design/n8n-workflow-map.html** (mermaid.js dark theme, master-view-2 design language — Rob's visual-outputs bar). All 17 workflows accounted: 7 CRM-touching (uptime watch `Dqpm48FtsjiwyUMO`, nightly backup `f99xNvSpKIy95k4Q`, overdue watcher `ozMXSpftU2lIbhp2`, recycle tagger `EsDSJQoJwzLkJOhR`, Gmail capture `JnIJiCbOqSaK8uN2`, voice-law monitor `KKDnWT0gYVB1YFvA`, error-alarm backstop `VoOFOPGqObGWe5Jr`) mapped trigger→nodes→auth→target→failure-path incl. errorWorkflow edges; the other 10 explicitly tabled as non-CRM (verified: zero `mle-rob-dashboard` references). Bonus: BOTH secret-rotation couplings now registered in one doc (CRON_SECRET → 3 bearer workflows; N8N_EMAIL_WEBHOOK_SECRET → shared cred `2sHDFkgzMWkOvhv7` → Gmail capture + error alarm + voice-law). Refresh rule baked into doc+page: MC.9's future ingestion workflows extend the map same-commit (1:1 DoD is continuous) — PRD MC.9 carries the cross-ref. Static-page-only change → build green, deployed, prod-curled. PRD: MC.11 [x] + MC.9 cross-ref + rev 3.1.73 same-commit.
  - 📐 ATLAS 2026-07-23 (Q51): the refresh pass (pending, see prior notes) just got EASIER — `/design/n8n-workflow-map.html` + `docs/ops/N8N-WORKFLOW-MAP.md` are now the verified 1:1 n8n-seam inventory (all 7 CRM-touching workflows, auth + failure paths + rotation couplings); the atlas's automation section can lift from it instead of re-deriving. Artifact republish still requires the interactive session.
- [x] Q50. **Task MC.7 — GATE G3: invoicing/AR backing-store verdict (picked 2026-07-23 as top UNBLOCKED work; gate map unchanged: Q39–Q45 Rob-gated on master-view approval — incl. Q45 per the design-doc §8 footnote "Nothing in §3–§7b gets built before Rob signs off", Q15/Q5b creds-blocked, Q16/Q6 Phase-4-gated; MC.16 closed last increment)** | DoD: written verdict per store with evidence; determines what the AR view reads ✅ **DONE 2026-07-23 (one increment; research task → no critic-rob gate).**
  - 2026-07-23 driver: **VERDICT: `invoice-ledger.csv` (contracts repo) is the ONLY live invoicing/AR store — zero Postgres/Supabase tables.** Evidence per store in `docs/research/G3-INVOICING-STORE-VERDICT-2026-07-23.md`: Supabase's full 12-table registry (BACKUP_TABLES, Q47-current) has no invoice/AR table; REST probes invoices/invoice/ar/ledger/payments/billing all 404; `deals` is stage-level only (`invoiced`/`paid` stages + `value` — no invoice_number/due_date, probe 42703). CSV holds both real invoices: MLE-2026-100122 $10k issued (2×$5,000 split plan, first due 2026-07-24, owner Caleb) + MLE-2026-100123 $19k paid 7/16 — status is free-text prose, human ledger. **Determines the AR path** (recorded in the verdict doc + PRD): MC.8 = provenance-tagged CSV sync (CSV stays canonical), MC.9 invoicing leg = scheduled CSV diff (no event emitter in the contracts repo), MC.14 unpaid-alerts need structured due_date extraction at sync time, Q40's phase↔invoice cross-check rides the same seam — one sync, multiple consumers. **Findings protocol:** medium flag filed on CG Roofing Group (prod `{ok:true}`) — invoice 100122's first $5,000 split payment due 2026-07-24 (TOMORROW) with no watcher covering invoice due dates; surfaces on the org record + Things to Address. Contracts repo touched read-only. Docs+flag increment, no code, no deploy. PRD: MC.7 [x], G3 dependency row CLOSED, rev 3.1.72 same-commit.

## ✅ ROB RULINGS 2026-07-23 ~00:45 (applied to prod by Max; flags 17/19 resolved):
- **Naples Spine & Joint: 100% COMPED** — deal value set $5,000→$0 w/ COMPED note (Polk = owner + MLE rep, never charged). Render as COMPED badge, never a $0 invoice. Rob: "No need to bring that up again."
- **CG Roofing: CLIENT primary.** AND a taxonomy defect: Rob — "I still dont have any idea what Vertical Anchor actually means or the utility of it." Master View vocabulary must not ship jargon Rob can't use: rename/plain-language the anchor concept (e.g. "Flagship — Roofing") or demote to a secondary descriptor; design doc rev tweak owed.
- **Gary Waskovich: Owner of Miga (co-owner w/ Daniella) + Director at De Cecco** — org link moved to Miga, both affiliations in role text, legacy business field nulled; Daniella has NOTHING to do with De Cecco (verified — she was never linked). Multi-affiliation case for §2c: one person, two real company hats — org_memberships (0003) is the eventual home; role text carries it until then.

- [ ] Q46. **Rep cockpit wiring (from docs/research/REP-COCKPIT-RESEARCH-2026-07-23.md — Rob-requested research, awaiting his morning read):** headline finding = the gap is WIRING shipped engines into /rep, not new systems. Buildable-now increments in research §5 order once Rob nods (R1=Q42 is already Rob-approved and buildable tonight): R2 wire the /rep Today band to todayRules `/api/tasks/today` (finish-able queue, not dashboard) → R3 rep-scoped pipeline board w/ stage-aging tint → R5 stage chip on account workspace → R10 log-interaction form (Task 1.9 validation) → R6 templates + compose prefill → R7 collateral shelf (vertical × stage → link config + pinned Growth Scan) → R9 per-stage guidance lines (static, 1 line — anti-notification-fatigue per research). Guardrails from research adopted as LAW: rep-required fields never grow beyond Task 1.9's list; the queue is the ONLY notification channel (no push/badges); no auto-sequences. Phase-selection-required-at-agreement-send lands with the send drawer (R6/Documenso flow) | DoD: per-increment per research §5; each prod-verified
  - ✅ 7/23 driver: **R1 (=Q42) COMPLETE** — see Q42 tick above. R2 onward still awaiting Rob's research nod ("rep research approved").

- [ ] Q47. 🌙 **E-SIGN AGREEMENT FLOW — CORE SHIPPED ✅ critic-rob SHIP 96/100 zero auto-fails (2026-07-23 pm; rev-review after 92→punch-list→f7269f4+b4d1fb5; all claims independently re-verified incl. latch-recovery failure-injection tests, §7001c verbatim-certificate assertion, prod trigger + zero residue). CLAIM RELEASED — driver may now work the REMAINING increments: nudge cron wiring (lib/esign/nudges.ts is tested+ready), countersign flow, engine skill-packaging, DocumentsSection mounts on org/deal records, SMS channel (Q5b-gated). Rob-gated: counsel review of both consent language drafts (b2b + consumer-DRAFT), ESIGN_CONSUMER_ENABLED stays off until counsel. (orig: Rob GO 2026-07-23 ~01:00 "get it going") — was: CLAIMED by Max overnight session; driver: do NOT pick this up, work other items (files: supabase/migrations/*esign*, lib/esign/**, app/sign/**, app/api/esign/**).**
  - 2026-07-23 Max (inc.1): **0008_esign APPLIED TO PROD + verified.** `documents` (activities-style anchors, phase field per Rob, sha256_at_upload/sha256_signed, version+supersedes_id, draft→…→archived enum) · `signature_requests` (token HASH only at rest, expires_at, channel, signer meta, consent_at, sha256_at_sign, presend_answers jsonb) · `signature_events` APPEND-ONLY — enforced by trigger (service role incluced; RLS-no-policies can't bind service role), FK restrict so audit outlives its request. Pre-apply gate clean (14 tables, none of the 3 present) → Management-API apply, tracked `20260723090000/esign` → PostgREST round-trip: doc+request round-trips exact; anchorless 23514, bogus status 23514, dup token_hash 23505, event UPDATE/DELETE both P0001 "append-only", request-delete-with-events 23503. Test rows cleaned (0/0/0). **ENGINE DECISION recorded in ESIGN-BUILD-LOG: (b) Python stays the local generator, dashboard takes uploaded PDFs** — phase1_engine.py is ReportLab flowables (full text-layout engine); a faithful one-night TS port to layout-engine-less pdf-lib = half-port risk on a battle-tested legal doc. Next: bucket + lib/esign core.
  - 2026-07-23 Max (inc.2+3): **`agreements` bucket LIVE (private, verified) + lib/esign core DONE, 23 tests, suite 496/496 green.** `lib/esign/storage.ts` (pure `documentPath` `<anchor>/<doc>/v<N>[-signed].pdf` + service-role uploadPdf upsert:false/downloadPdf/signedUrlFor) · `token.ts` (base64url mint, sha256 HASH at rest, constant-time verify w/ injected `now` — tampered/expired-at-boundary/reused/voided all covered, tamper-beats-expiry so no liveness oracle) · `status.ts` (ladder as data; sent→signed race-legal; signed terminal; archiveOnNewVersion refuses signed docs, voids only open links) · `events.ts` (0008-enum builders + deterministic cert-chain formatter) · `consent.ts` (exact B2B §7001 language, versioned) · DDL drift gate parses 0008_esign.sql (lib/crm.ts precedent). Next: deps add → signer flow.
  - 2026-07-23 Max (inc.4 + Rob amendments): **SIGNER FLOW + SEND/UPLOAD/GENERATE LIVE in code (build+lint+530 vitest green) — and Rob's 3 mid-build amendments folded in.** (1) **ENGINE DECISION AMENDED by Rob → TS port SHIPPED**: `lib/esign/agreementPdf.ts`, faithful 408-line port (all §1–§13 clauses, intake gate w/ same refusal messages, scope grammar incl. agent bundles/label/social-dict, mini layout engine w/ justification) — pure JSON-in→PDF-bytes-out, no Next imports, skill-wrappable per Rob; **parity PROVEN vs python reference on the real CG config: 4/4 pages, 94.6% normalized similarity, zero substantive text diffs** (full parity log in ESIGN-BUILD-LOG); wired as POST /api/esign/generate (intake refusal = the 400 body). (2) **PEWC comms consent** on signer page: optional/unchecked/quiet, structurally cannot gate signing; `comms_consent` event + `people.comms_consent` (0009, never-overwrite) + "on file ✓" never-re-ask + certificate line; `[counsel review]` marked. (3) **Consumer §7001(c) seam built TO THE LANDED SPEC**: signer_type on request (0009), consumer sends 403-blocked behind ESIGN_CONSUMER_ENABLED, disclosure-before-consent w/ render-locked checkbox + render evidence required server-side + full disclosure reproduced on certificate; ~~all 7 checklist elements test-asserted~~ (critic-rob punch #2: was 6/7 — element 6 had no assertion; closed same-day, see punch-list note below — NOW all 7 asserted incl. certificate-verbatim via pdf.js extraction). Signer page public via proxy isPublicPath (/sign/ + /api/esign/sign ONLY — send/documents/generate stay Basic-gated, pinned in proxy.test.ts). Sign route: hash-mismatch hard 409, single-use latch race-safe, audit-cert page w/ event chain, timeline activities, copy emails (failure never loses a signature). Deps added: @cantoo/pdf-lib+signature_pad+react-pdf (MIT). 0009 applied+tracked+verified on prod. ALSO: CI was RED before this build (SearchBar set-state-in-effect vs new eslint-config-next) — root-cause-fixed, CI green again. Next: n8n sender workflow → DocumentsSection → deploy → prod E2E.
  - 2026-07-23 Max (inc.5+6): **n8n sender LIVE + DocumentsSection mounted.** Workflow "MLE — agreement link sender" `EIR0mgUWcn26rsjD` (webhook `/webhook/esign-send` → secret-header IF → Gmail cred `zafHNwGNRYD8V9aq` from rob@aivoicetech.io → 200/401; errorWorkflow backstop `VoOFOPGqObGWe5Jr`) — proven live both branches (wrong secret 401; right secret 200 + real email delivered); one real bug caught+fixed (shell quoting ate the IF expression's quotes → first 401 exposed it → PUT fix → re-proven). ⚠️ rotation registry: secret hardcoded in IF node + `.env.local` + Vercel prod (`ESIGN_SENDER_WEBHOOK_URL`/`ESIGN_SENDER_SECRET`) — rotate all three together. `components/esign/DocumentsSection.tsx` on `app/people/[id]` (company rows anchor as org): status chips, signed-URL View (`?view=` serves the signed copy when it exists), Upload PDF, Send/Resend → pre-send check popup w/ remembered presend_answers, consumer + SMS options visible-but-gated, server 409s surfaced verbatim, mailer-down manual-link path. Build+lint green. Next: deploy → prod E2E.
  - 2026-07-23 Max (inc.7+8, NIGHT DoD MET): **PROD E2E SIGNED AGREEMENT — full loop proven then cleaned + nudge ladder lib shipped.** E2E on prod: `/api/esign/generate` ran the TS engine ON VERCEL (4-page synthetic agreement, demo-anchored) → send → link recovered FROM THE DELIVERED EMAIL (rob+esigntest@aivoicetech.io via the n8n sender — delivery leg proven, not assumed) → /sign viewed logged → signed w/ typed signature + ESIGN consent + PEWC comms opt-in → verified: token reuse 409, doc+request signed w/ full signer meta, event chain `created→sent→viewed→consent→comms_consent→signed→copy_delivered×2`, both timeline activities, people.comms_consent populated; signed PDF hash matches sha256_signed byte-exact, 5 pages, certificate carries signature/date/consent/digests-match/IP-UA/comms-block/event-chain. Cleanup total (rows+storage+comms reset, 0/0/0/0, bucket empty; [E2E TEST] emails left as delivery evidence). `lib/esign/nudges.ts` pure planner (full walkthrough ladder incl. Rob+Stalled@14d, max-3 customer, business-hours DEFER-not-skip, stop-on-terminal, DEMO exclusion, per-rung idempotency, deterministic flag titles) — 11 tests; cron wiring = morning increment by design. Suite 541/541, CI green on all pushes. ⚠️ **SECURITY FLAG #30 (high) filed: DASHBOARD_PASSWORD unset in ALL Vercel targets — prod Basic gate inert, whole dashboard open**; not fixed unilaterally (Rob must pick the password). Morning queue in ESIGN-BUILD-LOG: nudge cron, countersign, engine skill-packaging, org/deal mounts, Twilio SMS, counsel reviews (§3.5 + PEWC drafts), flag #30.
  - 2026-07-23 Max (critic-rob punch list, 92/100 REVISE → all 6 closed): (1) **post-latch failure recovery** — sign route's stamp/upload/doc-flip now a guarded critical section: failure REVERTS the latch to the pre-latch snapshot (link re-arms, retry works; atomic latch still prevents double-sign), files a high flag, honest 500; revert-failure sub-case flags "stuck consumed" + tells signer to get a fresh link; post-durability event misses file audit-gap flags instead of 500ing a completed signature; all pinned in `signRoute.test.ts` (stamp-fail/upload-fail/revert-fail/happy/consumer-gate). (2) element 6 + certificate-verbatim assertions added (pdf.js extraction of a stamped consumer cert proves the §3.4 DoD); log lines corrected honestly. (3) ★ parity claim corrected — both engines render AND extract it. (4) already-signed page only claims copy delivery when a `copy_delivered` event exists. (5) token.ts comment reworded (no upstream attribution). (6) render-lock now enforced by pure `lib/esign/signerGate.ts` + component source-drift guard + server consumer-400 test. Suite 557/557, lint+build green. Source decisions: docs/research/esign-mit-scout-2026-07-23.md + docs/plans/esign-flow-walkthrough-2026-07-23.html (ported tonight from the STALE Desktop clone the scout session wrote to — see pushback log). Build = scout Option 1, thin MIT flow in-dashboard: `documents` + `signature_requests` (single-use expiring token, consent_at, sha256_at_send/sign) + `signature_events` append-only audit (insert-only RLS) → Supabase Storage private bucket → public signer page /sign/[token] (react-pdf preview + ESIGN consent checkbox + signature_pad) → server stamps PDF (@cantoo/pdf-lib) + audit-certificate page + copy delivery → Documents section on the lead + timeline events. Then: pre-send check popup w/ remembered answers, one-click resend (void old link), nudge ladder (viewed+24h→rep · sent+2d/+5d/+10d→customer max 3, business hours · +14d→Rob + Stalled) riding the Task-3.4 watcher pattern. Constraints: legal elements per scout doc (intent, consent, association, retention; hash at send+sign; IP+timestamp audit; copy emails); SMS nudges deferred to Twilio creds (Q5b); consumer-facing docs (§7001c) flagged for counsel before any homeowner use; Python phase1_engine.py can't run on Vercel — agent decides TS-port vs local-generate+upload with evidence | DoD: end-to-end signed test agreement on prod (test payload, cleaned), audit chain verifiable, CI green
  - 2026-07-23 driver (nudge inc.3): hourly schedule hardened (`errorWorkflow → VoOFOPGqObGWe5Jr` on `CxFUrjo29NiYMofS`), prod re-verified, duplicate-cron near-miss caught + archived (INCIDENT-LEDGER #15). Forced-nudge proof deliberately NOT faked — `signature_events` is DB-enforced append-only.
  - 2026-07-23 driver (COUNTERSIGN inc.1): **the MLE side of the paper now exists in the schema and in the rules.** `supabase/migrations/0010_esign_countersign.sql` **APPLIED TO PROD + verified** — additive only: six nullable `documents` columns (`countersigned_at`, `countersigner_name/_title/_email`, `countersigned_path`, `sha256_countersigned`) + one constraint swap adding event type `countersigned`. Prod proof: pre-gate 0 counter-columns → post-apply all six present w/ correct types; `signature_events_type_check` = 11 types; **`documents` status CHECK verified UNCHANGED**; row counts 0/0/0 (nothing touched); registered `20260723210000/esign_countersign`. **Design decision (in the migration header + lib): `signed` stays TERMINAL — countersignature is NOT a sixth status.** `signed` = the counterparty is bound, true the instant they sign and never walked back by an internal step; widening the ladder would re-open the archiveOnNewVersion/transition invariants critic-rob already certified. So it's facts-on-the-row + one append-only event on the signer's request → chain reads created→sent→viewed→consent→signed→countersigned→copy_delivered; UI derives "signed · awaiting your countersignature" from `countersigned_at IS NULL`. `lib/esign/countersign.ts` = pure planner (no clock/network/Next imports): refuses rather than silently no-ops on not-yet-signed, already-countersigned (no re-dating an executed agreement), missing signed copy to stamp, blank printed name, blank authority title. 16 tests incl. a DDL gate that asserts the migration adds every column the planner patches AND does not widen the status enum. 582/582 vitest, build green. No deploy (nothing user-visible yet). **NEXT increment: the countersign stamp route (re-stamp `signed_path` → `countersigned_path` + digest) then the record-page button.** Correction for the queue record: "org/deal DocumentsSection mounts" is half-obsolete — the ORG mount is already live on `/people/[id]` for company rows; a DEAL mount has no home because there is no deal record page (only the `/deals` board), so that leg is really "build a deal record page" and should be scoped with Q39/Q45, not here.
  - 2026-07-23 driver (COUNTERSIGN inc.2): **the executor — `POST /api/esign/countersign` + the countersignature page.** The 0010 columns now get written. Order of operations copies the signer route: pure planner refuses FIRST (verbatim 409 for not-yet-signed / already-countersigned / no signed copy / blank name / blank authority) → **atomic claim** (`update … .is('countersigned_at', null)`) so a double-click can't produce two countersignatures or re-date an executed agreement → fallible PDF work → **claim REVERTED on failure** (retry safe; an un-revertable claim says so explicitly rather than leaving a row that lies). Integrity gate: the stored signed copy is re-digested and compared to `documents.sha256_signed` — mismatch REFUSES instead of countersigning an altered file. `lib/esign/countersignPdf.ts` appends exactly ONE `COUNTERSIGNATURE` page (name, authority title, server-stamped UTC, the counterparty's signature line, digest of the exact bytes countersigned) — nothing overlaid on their pages, same choice as `stamp.ts`. Executed copy lands BESIDE the signed copy (`v<N>-countersigned.pdf` via new `countersignedPath`) so the signer's emailed copy stays byte-identical; `documents.status` untouched. Post-durability misses (path/digest write, event, activity) log, never unwind. 5 new tests (page-count delta, pdf.js text of the appended page, 3 refusals, path guards) → **587/587 vitest, build green, lint clean**. No UI yet → no deploy. **NEXT increment: the record-page countersign button** (`countersignLabel` chip + name/title prompt on `/people/[id]` DocumentsSection) — that one deploys.
  - 2026-07-23 driver (COUNTERSIGN inc.3): **the button — countersigning is now a thing Rob can DO, from the record page.** `DocumentsSection` (live on `/people/[id]`, company rows anchor as org) grows a `Countersign` action on any `signed` document that has no `countersigned_at`, plus an honest state line: `awaiting your countersignature` in amber until it's executed, then `countersigned by <name>, <title> · <date>`. The chip reads **executed** once `countersigned_at` exists — derived in the UI, NOT a sixth status (the inc.1 decision holds: `signed` stays terminal in the DB). Modal takes printed name + authority title (+ optional email), remembers the last answers in localStorage as a convenience that can never gate — the server planner still refuses a blank name or blank authority — and surfaces planner refusals VERBATIM instead of a generic failure toast; the confirm button re-disables after success so a double-click can't attempt a second countersignature (belt to the route's atomic claim). `GET /api/esign/documents?view=` now hands back the MOST executed copy that exists (countersigned → signed → original), so `View` on a closed agreement opens the fully executed paper. 587/587 vitest, build green. **DEPLOYED to prod** (user-visible surface). Remaining for the Q47 tick: forced-nudge proof (blocked by design — append-only audit), deal-anchored mount (needs a deal record page → Q39/Q45), SMS (Q5b creds), counsel review of both consent drafts.
  - 2026-07-23 driver (SKILL-PACKAGING, last ungated Q47 leg): **the agreement engine now runs outside the dashboard — same engine, same gate.** `lib/esign/agreementPdf.ts` was built pure on purpose (Rob: "skill-wrappable"); this increment adds the two missing pieces. `scripts/esign/render-agreement.mjs` = CLI over the identical engine and the identical `checkIntake` gate — renders any `contracts/clients/*.json` to disk on Node's native type-stripping (no build step, no new dependency), prints path/pages/bytes/sha256, prints intake refusals **VERBATIM** and writes NOTHING on refusal, and refuses to overwrite an existing file without `--force` so a re-render can't clobber executed paper. Proven both ways: `gulf_coast.json` → 4 pages (the python-parity page count), `the_title_base.json` → refusal naming `[confirmed_by, date, entities_count, second_brains_total]`, zero bytes on disk — Q57's never-invent-scope rule now holds at a second surface. `skills/phase1-agreement/SKILL.md` carries the contract (which surface for which job, the five intake keys, the two cross-checks, the never-invent rule), and `lib/esign/__tests__/agreementSkill.test.ts` is a drift guard: it fails if a documented path disappears, if an engine-required intake key goes undocumented, if the never-invent/verbatim rules leave the page, or if the CLI's refusal path stops preceding its write. 593/593 vitest, build green, lint clean. **No deploy — nothing user-visible changed.** With this, every remaining Q47 leg is GATED, not homeless: forced-nudge proof (blocked by design — append-only audit chain), deal-anchored mount (needs a deal record page → Q39/Q45), SMS (Q5b creds), counsel review of both consent drafts.
  - 2026-07-23 ATLAS DUTY (2nd note): Atlas refresh pass now also needs the e-sign engine's OUT-OF-DASHBOARD surface — `scripts/esign/render-agreement.mjs` + `skills/phase1-agreement` alongside `/api/esign/generate` (live session republishes the artifact).
  - 2026-07-23 ATLAS DUTY: Architecture Atlas needs a refresh pass — 0010 adds countersign columns/event type to the e-sign data model (live session republishes the artifact).
