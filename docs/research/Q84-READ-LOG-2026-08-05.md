# Q84 read log — what the archive pages ACTUALLY say

**Started 2026-08-05 (inc.193).** One row per Notion archive page that has been *opened and read*,
in the order `npm run worklist:q84` schedules them (largest body first as of this increment).

**The rule this file exists to keep:** a row appears here only after `find_meeting.py --page …`
has actually run and its output is on disk. A block count is not a read; a page that has not
been opened gets no line here, not a hedged one. Nothing in this file is a summary of a meeting
whose body was not read.

Re-generate the queue with:

```
npm run --silent check:archive  -- --json                        > /tmp/q84-check.json
npm run --silent recount:q84    -- --input /tmp/q84-check.json --json > /tmp/q84-recount.json
npm run --silent worklist:q84   -- --input /tmp/q84-recount.json
```

---

## 1. Gulf Coast RE KICKOFF 2026-07-22 — READ ✅

- **Page:** `3a51de57-0199-802b-b9f8-f59fa153a013`
- **Read:** 2026-08-05, `find_meeting.py --page … --deep`, output 117,619 bytes.
- **Measured before the read:** `body-present`, 784 blocks / 101,488 chars (the read itself
  reports 114,354 chars — the re-count's depth-4 walk undercounts, which is the same cap
  inc.192 flagged; it is an undercount, never an overcount).

**Row identity (from properties, not the title):** Call Date 2026-07-22 · Meeting Type
"Phase 1 Kickoff Meeting" · MLE = YES · Company "Gulf Coast RE Group" · MLE attendees Rob
Acheson, Will DeVito · non-MLE attendees Alex Greenwood, Chris Acheson, Shasta · Sales Rep
Robert Acheson · Next Steps "Alex to Start EM Thread".

**What is on the page:** a `transcription` block holding an AI-generated Action Items list
(10 to-dos), structured `heading_3` sections — Competitor SEO Analysis, SEO & Website
Strategy — and, below the summary, the full verbatim call transcript (726 paragraph blocks).

**⚠️ The finding — four fields claim absence while the body holds 114k chars:**
`Call Recording`, `Meeting Summary`, `Google Doc Link` and `Export Status` are all empty on
this row. Any check that reads those fields concludes "no record of this meeting exists."
The record exists; it is in the page body. This is the Omega 7/28 defect, on a different row,
and it means **field-emptiness must never be treated as evidence of absence anywhere in this
system.** Flagged to the ledger.

**Named in the body and owed CRM records (NOT created by this increment — reads only):**
competitors profiled with hard numbers (Palm Paradise, Greater Fort Myers Team, Michael
Saunders, Dunkin Duo, Team 239, MVP Realty, The Villages) and one live opportunity —
**MVP Realty went bankrupt post-NAR-lawsuit leaving ~1,200 agents unserved**, called out on
the call as an audience to capture. Q85 owns turning this meeting into an activity on the
Gulf Coast org; `company-catcher` owns the competitor orgs.

---

## Still owed (⚠️ this hand-count was wrong — see the machine count at the bottom of this file)

> **2026-08-06 (inc.37):** every "still owed" line in this file was typed by hand, and by the time
> `parseReadLogPageIds` landed (inc.36) they disagreed with each other *and* with the archive: this
> heading said **12**, the last entry said **9**, and `npm run worklist:q84` measured **10**. The
> counts below are left in place as written — they are the record of what each increment believed —
> but **none of them is authoritative.** The only number to quote is the one the work-list prints,
> because it is derived from the live archive minus this log. Re-run the chain; do not read a total
> off this page.

Per today's work-list, next largest first: `Meeting 2026-07-28` (95,769 chars — the Omega
row), `Meeting 2026-07-30` (82,449), `will Devito 2025-12-20` (76,387), `GHL AI Agent
Knowledge Base Webinar` (58,534), `Matt Jackson and Ryan Groth` (41,303), `Gulf Coast Realty,
AI Alex meeting one` (39,704), then the remainder down to 874 chars. The 18 `container-only`
rows get `--page … --deep` before any cross-database sweep, and are excluded from the
unrecoverable ceiling entirely.

---

## READ 2026-08-05 (Q89 inc.5) — `Meeting 2026-07-28` · the Omega row · 531 blocks / 104,683 chars

`find_meeting.py --date 2026-07-28 --deep` → Notion page `3ab1de57-0199-80ef-bf9c-c2b98d7578ed`.

**The largest single correction this log has produced.** Rob has been told twice — in the
daily brief and in PING-INBOX — that this meeting was in person, that no notetaker was
present, and that what the Omega principals said *"exists ONLY in your head"*. **It was
recorded.** The page body holds a complete structured summary (Action Items, Meeting
Overview, Website/SEO, Everything Agent, Title Processing Automation, Software & Tools,
JV & Business Structure, Pricing & Phases, Future-Proofing) **and the full transcript**.

Four fields claim absence and all four are contradicted by the body: `Call Recording`,
`Meeting Summary`, `Google Doc Link`, `Export Status`. This is the same defect the Gulf
Coast read found, on the row it was originally named after — **field-emptiness is never
evidence of absence** — and here it cost real work: three separate mining passes over this
meeting, and two standing asks for Rob to dump from memory a call that was transcribed.

**Filed as a meeting activity, not just read:** `data/meetings/2026-07-28-omega.activity.json`
— `A-MTG-2026-07-28-OMEGA` on `C-2019`, carrying 18 intel entries across all four Q89 blocks.
Five pain points are **transcript paragraphs spoken by the Omega side**, each a literal
substring of the line it cites (`body ¶307/361/387/451/523`); summary bullets are used for
action items, talking points and the commercial read, never for pain.

**Held back on purpose and flagged instead:** the JV section states *"There is a desire to
remove the current JV partner, Scott, who is alleged to have committed fraud and stolen
money"*, with volume numbers attached. That collides with Rob's 2026-08-05 ruling that Scott
Dascani is CEO of the whole company and Mike Stiber holds the Alex JV. An unresolved
accusation against a named person does not go on a record Rob shows people — it went to the
flag ledger, and a test enforces its absence from the activity.

**Still owed:** 13 reads + 18 uncapped re-reads. Next largest: `Meeting 2026-07-30` (82,449),
`will Devito 2025-12-20` (76,387), `GHL AI Agent Knowledge Base Webinar` (58,534).

---

## READ 2026-08-05 (Q89 inc.8) — `Meeting 2026-07-30` · Martin Fierro Restaurant · 399 blocks / 89,170 chars

`find_meeting.py --page 3ad1de57-0199-80dd-b213-d09c387217e7 --deep` → 93,034 bytes of output at
`/tmp/q84-read-0730page.txt`. First read on this log that was **executed inside the increment that
consumed it**, rather than inherited from an earlier session.

**Three rows read, three rows lying.** `Call Recording`, `Meeting Summary`, `Google Doc Link` and
`Export Status` are all empty on this page too, over 89k chars of body — and the tool says so in
its own words: *"4 field(s) imply 'no record'. CONTRADICTED — the body below HAS the content.
Trust the body, not the field."* The Omega and Gulf Coast reads found the same four fields empty.
This is no longer a pattern to watch; it is the archive's normal state.

**The title lies by omission too.** `Meeting 2026-07-30` names no counterparty. The identity is
only in the properties — `Company Meeting with: Martin Fierro Restaurant`, `Company URL:
martinfierronaples.com`, `Non MLE Attendees: Dani, Michael`, `MLE Attendees: Rob Acheson, Will
DeVito` — which is exactly why the tool prints **TITLE (do not trust)**. Nothing in this archive
can be triaged by field or by title; only a body read settles it.

**Filed as a meeting activity:** `data/meetings/2026-07-30-martinfierro.activity.json` →
`A-MTG-2026-07-30-MARTINFIERRO` on the **existing** org `C-2005` (matched by name, not created),
19 intel entries across all four Q89 blocks. **Five pain points are transcript paragraphs spoken
by the restaurant side** (`body ¶14/¶21/¶26/¶90`), each a literal substring of the line it cites —
five where Gulf Coast yielded two, because these owners state their own problems plainly. The
count follows the evidence, never a target.

**Not written, deliberately:** the `Other Combined/Possible referral` field names SpotHopper,
Toast, Owner.com and *"Omega guys (Blake, Scott)"*. Vendor tools named in passing are not CRM
orgs, and the Omega line is a referral thread owned by `company-catcher` under Q87's gated rules.
Also withheld: our own 11-automation pitch and the case-study percentages — our sales claims are
not the customer's intel.

**Still owed:** 12 reads + 18 uncapped re-reads. Next largest: `will Devito 2025-12-20` (76,387),
`GHL AI Agent Knowledge Base Webinar` (58,534), `Matt Jackson and Ryan Groth` (41,303).

---

## READ 2026-08-05 (Q89 inc.9) — `will Devito 2025-12-20` · 49 blocks / 77,465 chars · **REFUSED FOR PUBLISH**

`find_meeting.py --page 2cf1de57-0199-8003-9e6d-fd921fbb8a59 --deep` → 79,838 bytes, archived at
`MLE Internal Meetings/archive-reads/2025-12-20-will-devito.deepread.txt`. Read first, believed
second — and this is the first row on this log that the read **stopped** rather than published.

**The row is two different meetings welded together.** The AI summary is a structured STG sales
call with **BBX Moving Company** — Quick Facts naming owner *Chai*, Vancouver, ~14 years in
business, ~$2.0–2.2M flat revenue, 60/40 commercial/residential, and a closing line that settles
it: *"Attendees (planned): Chai (BBX), Rob (STG), Ryan (STG)."* Directly beneath it sit **74,271
characters of a completely unrelated conversation** — an automation/AI walkthrough (three steps to
building an automation, image-generation prompts, campaign copy) with no counterparty named
anywhere in the text. Counted, not sampled: the transcript says **BBX 0 times, Chai 0, moving 0,
Vancouver 0**.

**Why this row is more dangerous than an empty one.** Every Q89 publish so far reads summary
bullets as action items / talking points / commercial read, and transcript paragraphs as the
verbatim pain. Run that recipe here and it creates an org for BBX Moving Company and attaches a
body BBX is never mentioned in — **a fabricated company record assembled entirely out of real,
individually-correct fragments.** Nothing in the pipeline would have objected: the summary is
well-formed, the transcript is long, both are genuine, and the four "no record" fields are empty
here exactly as on the three rows before it. The failure would have looked like a success.

**So the increment shipped a guard instead of a record:** `lib/meetings/bodyCoherence.ts` — name
the counterparty the summary asserts, then look for it in the body; if the summary claims a
company and 74k characters never say it, the row is `mismatch` and `safeToPublish: false`. It
never extracts the terms itself (an extractor guessing which words are the company is the same
guessing one level up), **silence is `unverifiable` and never `coherent`** (no asserted identity
or no body gets its own verdict — reporting "no disagreement" on a row nobody could check is the
`field empty ⇒ no record` inversion Q84 exists to kill), and a **partial** hit is not a pass.
Run against the real 74,271-char body: `verdict: mismatch · safeToPublish: false`. 10 tests.

**The title lied a fourth way.** Omega's title named no counterparty; Martin Fierro's named none;
this one names **`will Devito`** — an MLE person, matching the *transcript* while the summary is
about a moving company. Neither field, nor title, nor summary alone settles a row on this archive.

**Two things owed to Rob, flagged HIGH to the ledger (`ok:true`), not decided here:** (1) BBX is an
**STG-era prospect** from 2025-12-20, not an MLE company — whether STG-era archive rows belong in
this CRM at all is his call, and several more sit in the worklist (`Matt Jackson and Ryan Groth`,
`Matt, Ryan, & Rob | STG Presentation`); (2) `Call Recording`, `Meeting Summary`, `Google Doc Link`
and `Export Status` are empty here too — **four rows read, four rows lying.**

**Still owed:** 11 reads + 18 uncapped re-reads. Next largest: `GHL AI Agent Knowledge Base
Webinar` (58,534), `Matt Jackson and Ryan Groth` (41,303) — the second of which is STG-era and
is gated on Rob's answer to (1) above.

---

## READ 2026-08-05 (Q89 inc.10) — `GHL AI Agent Knowledge Base Webinar 2025-12-18` · 300 blocks / 65,104 chars · **NO RECORD OWED — INTERNAL**

`find_meeting.py --page 2cd1de57-0199-8029-8c87-ea7fdf6c9c53 --deep` → 67,849 bytes, archived at
`MLE Internal Meetings/archive-reads/2025-12-18-ghl-webinar.deepread.txt`.

**Five rows read, five rows lying.** `Call Recording`, `Meeting Summary`, `Google Doc Link` and
`Export Status` are all four empty here as well, over 65k chars of body, and the tool prints the
same contradiction it has printed on every row read so far. The archive's normal state is a page
whose fields deny the content sitting underneath them.

**This row is not a meeting with anyone, and that is a finding rather than a gap.** The body is a
single internally-consistent session: *"Robert facilitated a coaching call focused on knowledge
bases for AI agents in High Level. This is the fourth session in a series of coaching calls"*
(body ¶1), with its own action item reading **`@Robert Acheson to create feedback survey for
coaching calls`**. Rob ran it. The other side of the call appears throughout only as
*"Participant"* — no company, no named attendee, and both attendee fields empty. Counted, not
sampled: `High Level` 15, `GHL` 5, `Robert` 4, `STG` 0. There is no counterparty because there
was none.

**The guard already refused it — but for the wrong reason, and that is what this increment fixed.**
Run against the real 65,461-char body with no ruling supplied, `checkBodyCoherence` returned
`unverifiable · safeToPublish false`: correct on the refusal, wrong on the meaning. `unverifiable`
is the verdict for *nobody has checked this row*, so an internal webinar that is finished forever
was indistinguishable from a row still owed a read, and would have sat in the outstanding-reads
count permanently. That is Q73 inc.29's defect in a new column — *reviewed and cleared* and
*nobody ever looked* producing identical output, with every count reading the pair as one thing.

`CounterpartyReview` adds the third state. A human supplies the ruling with its reason and the
line it came from (the module still never infers it — rule 1 stands), and the verdict becomes
**`internal-no-counterparty`**, printed with the evidence so the ruling is checkable:

> *A human read this row and ruled it has no counterparty: a HighLevel AI-agent coaching call Rob
> himself facilitated; participants appear only as "Participant", no company is on the other side
> (body ¶1 'Robert facilitated a coaching call' + to-do '@Robert Acheson to create feedback survey
> for coaching calls'). It is an internal meeting, so it is not owed another read and it may not be
> published onto a company record — there is no company it belongs to.*

`safeToPublish` stays **false** in both states. The new verdict is a reason a row is *done*, never
a licence to publish. A test pins that the body still overrules the ruling: an "internal" ruling on
a row whose summary names a company the body never mentions is still `mismatch` — inc.9's welded-rows
defect wins over any human label.

**No org was created, and the one candidate was refused on Rob's standing rule.** The only company
named anywhere on this page is **HighLevel/GHL** itself, the vendor whose product the call is
about. A vendor named in passing is not a CRM org (inc.8's SpotHopper/Toast/Owner.com precedent),
and GHL specifically is a tool Rob has **no access to** — the STG-era key was revoked — so
manufacturing a HighLevel org record off a training webinar would be the fabrication class this
pipeline exists to stop, with a name Rob has explicitly ruled out.

**Owed to Rob, flagged HIGH, not decided here:** this is the second STG-era row in a row
(2025-12-18, one day before `will Devito`), and it sharpens the open question inc.9 raised — whether
STG-era archive rows belong in this CRM at all. It also raises a second, narrower one that is his
call and not the machine's: whether internal MLE meetings (training, stand-ups, coaching) should
land anywhere in the CRM at all, or stay in the archive. Until he rules, they are read, logged, and
left unpublished.

**Still owed:** 10 reads + 18 uncapped re-reads. Next largest: `Matt Jackson and Ryan Groth`
(41,303 — STG-era, gated on the question above), `Gulf Coast Realty, AI Alex meeting one` (39,704 —
MLE, ungated, and therefore the next publishable read).

---

## `Gulf Coast Realty, AI Alex meeting one` — 2026-06-16 — READ, PUBLISHED (Q89 inc.11)

**Page:** `3811de57-0199-8099-9137-ef10c8fd0efe` ·
**Read with:** `find_meeting.py --page … --deep` ·
**Measured:** 281 blocks / 44,547 chars ·
**Archived:** `MLE Internal Meetings/archive-reads/2026-06-16-gulfcoast-ai-alex-one.deepread.txt` (47,171 bytes)

**The four fields lie again — sixth row in a row.** `Call Recording`, `Meeting Summary`,
`Google Doc Link` and `Export Status` are all empty over 44,547 chars of body carrying the full
transcript and a four-item action list. At six consecutive confirmations this is no longer a
surprise worth re-discovering; it is a property of the Master Meetings Database, and it is flagged
to the ledger in those terms.

**Why this row and not the larger one.** `Matt Jackson and Ryan Groth` (41,303) is bigger but
STG-era and gated on Rob's open question. This one is MLE, ungated — and it is the **discovery call
five weeks before the 2026-07-22 Phase 1 kickoff already on C-2018**. The company record held the
decision and hid the conversation that produced it.

**Published:** `A-MTG-2026-06-16-GULFCOAST-AIALEX` on **C-2018**, 18 intel entries
(4 action · 5 talking · 4 pain verbatim · 5 benefits), same publisher, same gate, no new code.
Read back off prod `/companies/C-2018`, not asserted.

**Two pain candidates refused.** ¶202 and ¶224 are real concerns sitting inside a two-way riff
where the speaker cannot be established on an unattributed transcript, so neither was promoted into
"what the customer said". The four that shipped are each an ANSWER to a question Rob asked in the
paragraph before it — that is how the speaker was established.

**Four action items written `open`, on purpose.** Three are seven weeks old and both the 2026-06-17
in-person and the 2026-07-22 kickoff happened, so some are very likely done. "Very likely" is not a
status. A human closes them on the record.

**No org created.** Origin Vault, Origin Stamp, Field Pulse, Boomtown, Bold Trail, Upwork and Zillow
are all named; none is a CRM org (inc.8/inc.10 precedent). Origin Vault's revenue-share proposal is
on **Alex's** deal, not Rob's, and is written as a talking point.

**Still owed:** 9 reads + 18 uncapped re-reads.

---

## READ 2026-08-06 (Q84 inc.37) — `Matt Jackson and Ryan Groth 2025-12-17` · 198 blocks / 44,935 chars · **READ, NOT PUBLISHED**

**Page:** `2cc1de57-0199-8071-b1ac-c075faf9fdbd` ·
**Read with:** `find_meeting.py --page … --deep`, rc=0 ·
**Archived:** `MLE Internal Meetings/archive-reads/2025-12-17-matt-jackson-ryan-groth.deepread.txt` (47,497 bytes)

**The gate on this row was a PUBLISH gate, and six increments treated it as a READ gate.** It has
sat at the top of `read-page` since 2026-08-05 because inc.9/inc.11 recorded it as *"STG-era and
gated on Rob's open question"* and skipped past it to smaller MLE rows. Rob's open question is
whether STG-era rows belong **in this CRM** — that gates the write, and nothing about it gates
opening a page that is already in his own archive. The cheap half was blocked by the expensive
half's gate. This increment does the read and **writes nothing to the CRM**, which is what the gate
actually permits.

**Measured before the read:** `body-present`, 41,303 chars (depth-4). **The read found 44,935** —
the cap undercounts by 3,632 here, the third independent confirmation that the depth-4 walk is a
floor and never a ceiling.

**The four fields lie again — seventh row in a row.** `Call Recording`, `Meeting Summary`,
`Google Doc Link` and `Export Status` are all empty over a body carrying a full AI summary
(12 `heading_3` sections), 5 `to_do` action items, and the complete verbatim transcript.

**Row identity, from the page and nothing else.** Every property except `Call Date`
(2025-12-17), `Created`, `Last Edited` and the title is empty — no Company, no Sales Rep, no
attendees, no Meeting Type. So the identity here is what the **body** establishes and no more: an
STG sales-consulting call between Ryan Groth and a commercial roofing contractor, with **Rob
present as a third party** — he is introduced mid-call (*"this is Rob. He's in Fort Myers"*) and
thanked at the end. The counterparty's **company name is never spoken in the body**, and the
transcript carries no speaker labels, so no company is named here and none may be inferred from the
title.

**A discrepancy inside the page, recorded and not resolved.** The AI summary section says the
business operates *"Tampa/Fort Myers/Sarasota"*; the transcript body has a line reading *"Seattle
is a very big market"*. One of the two is wrong — most likely a transcription artifact — but on an
unattributed transcript there is nothing on this page that settles it. It is noted so that whoever
publishes this row does not quietly pick the one that suits them. **The summary is not evidence
about the transcript, and vice versa** — the fourth row where the two halves of the same page
disagree.

**Nothing was created.** No org, no person, no activity, no CRM write of any kind — Supreme, Best
Roofing, Advanced Roofing, Estimating Edge and Timberline are all named in the body and none is a
CRM org (inc.8/inc.10/inc.11 precedent), and the counterparty itself is precisely the STG-era
question that is Rob's.

**Owed to Rob — unchanged, not re-asked.** This is the third STG-era row read, and whether STG-era
archive rows belong in this CRM at all is still his call (flagged from inc.9). It is on the ledger;
the driver does not re-post to dev_chat.

**Still owed:** run `npm run worklist:q84` — as of this increment the machine says **9 `read-page`
+ 18 `deep-read-page`**, with **7 rows now carried as `already-read`**. That number is derived, not
typed; the hand-typed ones above it are why this line points at the command instead of repeating a
total.

---

## READ 2026-08-06 (Q84 inc.38) — `Chain & Rob | STG Sales & Recruiting Analysis 2025-12-18` · 196 blocks / 33,786 chars · **READ, NOT PUBLISHED**

- **Page:** `2cd1de57-0199-801b-a9d1-c09e11554992`
- **Read:** 2026-08-06, `find_meeting.py --page … --deep`, output 38,227 bytes, archived to
  `MLE Internal Meetings/archive-reads/2025-12-18-chain-rob-stg-sales-recruiting.deepread.txt`.
- **Measured before the read:** `body-present`, **29,757 chars** (depth-4 walk). **The read found 33,786** —
  the cap undercounts by 4,029 here. **Fourth independent confirmation** that the depth-4 walk is a floor
  and never a ceiling.

**This row breaks the pattern the last seven rows established, and that is the finding.** The previous
reads all had four "claims absence" fields sitting over a full body. Here only **two** do — `Call Recording`
and `Google Doc Link` — because `Meeting Summary` and `Export Status` are actually **populated**. So the
"four fields lie" line is a property of *those* rows, not of the database, and it is corrected here rather
than repeated. The archive is not uniformly empty-fielded; it is inconsistently filled, which is worse for
any check that assumes one shape.

**Row identity — from properties AND body, which agree.** Call Date 2025-12-18 · Contact Name **Chai** ·
Company (in the `Company URL` field) **BBX Moving Company** · Sales Rep Robert Acheson · Meeting Type
"2) Presentation + Pitch" · Tags `Recruiting VSL` / `Commercial` / `Moving` · Next Steps "Present VSL
Recruiting Funnel" · Show/No Show "Show". This is an **STG-era row**: Rob is selling STG's recruiting-VSL
service; Ryan (STG) is named as an attendee for the follow-up.

**⚠️ NAME DISCREPANCY — the title says `Chain`, everything else says `Chai`.** The Notion page title reads
*"Chain & Rob"*; the `Contact Name` property, the `Meeting Summary`, the pain-points field and **every one of
the ~20 mentions in the body** read *"Chai"*. On the evidence of the page the title is the outlier. It is
**not corrected here** — the title is a human-typed field and Q84's own DoD forbids this pass overwriting one
— it is filed to `/api/admin/flags` so a human decides. This is exactly the class of defect that reaches a
real send: a greeting addressed to "Chain".

**⚠️ FIELD-SHAPE DEFECT — `Company URL` holds a company NAME, not a URL.** The value is the literal string
`BBX Moving Company`. Any consumer treating that column as a URL gets a broken link; any consumer treating it
as a name only works by accident. Recorded, not fixed — the column's contract is Rob's to set.

**A discrepancy INSIDE the summary, recorded and not resolved.** The Company-Snapshot section states a
**~75% commercial close rate**, while the funnel-math bullet in the same summary says BBX closes
**"~60–70%"** of its ~10 quoting opportunities — and the transcript backs the lower figure verbatim
(*"out of that 10, we'll close like 60, 70% of those"*). Two numbers for one metric, both AI-generated, on the
same page. Written down so whoever quotes BBX's close rate cannot silently pick the flattering one. **Fifth
row where two halves of the same page disagree** — but the first where the disagreement is summary-vs-summary
rather than summary-vs-transcript.

**Spot-checked summary against transcript on four independent claims, and they hold:** ~14 years in market,
flat ~$2.0–2.2M across 2023–2025, ~60% commercial mix, and the January 16 follow-up (transcript:
*"Friday the 16th"*, 10:30 ET / 7:30 PT). The summary is faithful on the facts; it is only the close rate
that is doubled.

**Nothing was created.** No org, no person, no activity, no CRM write. BBX Moving Company is not a CRM org and
this is an **STG-era row selling STG's service** — whether those belong in this CRM at all is Rob's standing
call (flagged from inc.9, on the ledger, deliberately not re-asked in dev_chat).

**Still owed:** run `npm run worklist:q84`. As of this increment the machine says **8 `read-page` +
18 `deep-read-page`**, with **8 rows carried as `already-read`**. Derived, not typed.

---

## READ 2026-08-03 pre-call · **RECOVERED, NOT NEWLY READ** (Q84 inc.39) — `2026-08-03T19:34` · 285 blocks / 28,869 chars

- **Page:** `3b11de57-0199-8020-9fb9-c3171150b472`
- **Archived:** `MLE Internal Meetings/archive-reads/2026-08-03-internal-precall.deepread.txt`, 31,417 bytes.
- **Read:** 2026-08-05 23:50 by a prior run. **Never logged, and archived OUTSIDE the repo** — into a
  `~/Projects/MyLocalEverything/MLE Internal Meetings/` directory that is a *sibling* of this repo, not
  the tracked one inside it. Untracked by git, invisible to the log parser. The file has been moved into
  the tracked archive and the stray directory removed; this entry is the write-up that was owed.
- **Measured before the read:** `body-present`, **24,112 chars** (depth-4 walk). The dump holds **28,869** —
  the cap undercounts by 4,757 here. **Fifth independent confirmation** that the depth-4 walk is a floor
  and never a ceiling.

**Why this row nearly cost a second full read.** It sat at the **top** of the work-list's READ queue —
largest unread body first — and the only reason it looked unread is that the single source of "already
read" was a hand-written log paragraph nobody wrote. Same shape as the six re-scheduled reads inc.37
fixed; that fix taught the list to consult the log, and this one is the log itself failing. **Closed in
code, not in prose:** `parseArchivedReadPageIds` reads the `id :` header out of every archived dump, and
the work-list now unions the two witnesses — the human log OR the machine artifact, either is sufficient,
neither trusted alone. The list also now prints, as a standing warning, any read proven by a dump but
missing from this log.

**All four "claims absence" fields are empty over a full 285-block body** — `Call Recording`,
`Meeting Summary`, `Export Status`, `Google Doc Link`. **This does not revive the "four fields always
lie" law killed in inc.38.** Inc.38's row had two of the four genuinely populated; this one has none.
Both readings stand, and together they say the thing that matters: the archive is **inconsistently
filled**, so no check may assume a fixed field shape. On this row *every* property is null — Call Date,
Contact Name, Sales Rep, Meeting Type, all of it. Only `Created` and `Last Edited` carry anything.

**Row identity — from the body alone, because the properties carry nothing.** 2026-08-03 evening
internal MLE pre-call: **Rob** and a remote collaborator, preparing for the next morning's client
meeting. **Will** is discussed throughout but is not on the call.

**⚠️ THE SECOND PARTICIPANT IS NOT IDENTIFIED, AND IS NOT GUESSED HERE.** The AI summary calls them
"a remote collaborator" and never names them. The transcript contains the single fragment *"Now, Chris."*
inside a passage of visibly garbled, overlapping audio — the same passage that produced *"Cheers. Y.
Willow's water."* One mangled token is not an identification, and this row is an MLE-internal call whose
participants become CRM people. Filed to `/api/admin/flags` for a human to name; **no person record
created, no name asserted.**

**Business content, and it corroborates the CRM's own attribution chain in Rob's voice.** He describes
the network-effect method verbatim — *"I write down all the nodes that I have where I know there's a
business owner I'm friends with, but I know he's also a part owner in another business"* — and then walks
the exact chain the CRM already holds: **Gulf Coast** real-estate agency owner → part owner of an **Omega
Title** location → the meeting where *"the big boss came down too"* → *"my partner, we have a holding
company and we have like … five, like $400 million businesses."* This is **first-party corroboration** of
the referral edges already in `network.local.json`, not a new claim; nothing was written from it.

**Operational content, unactioned by this pass.** Six to-dos, all internal: Will holds Cloudflare super
admin and the invite to **rob@aivoicetech.io** was greyed out for lack of permission (that address is the
correct AI VoiceTech identity — no crossover); Martin Fierro mobile SEO/LCP work was done on a **demo
environment, not the live site**; a 10am in-person at **Martin Fierro, North Naples** was set for the
next morning. **Martin Fierro is an existing CRM org** with a `2026-07-30` activity already on file, so
this row is the pre-call *for a known client*, not a new company.

**Lead for the re-read queue, recorded not acted on:** three `2026-08-04 Meeting` rows sit in the
`deep-read-page` queue as container-only. This call sets a 10am in-person for **2026-08-04**. One of those
three is plausibly that meeting — a lead for whoever takes them, not a match, and not resolved here.

**Nothing was created.** No org, no person, no activity, no CRM write, no title corrected.

**Still owed:** run `npm run worklist:q84`. As of this increment the machine says **7 `read-page` +
18 `deep-read-page`**, with **9 rows carried as `already-read`**. Derived, not typed.

---

## READ 2026-08-06 — `Calvary Roofing RAMP Call | Donny & Tyson &Robert 2025-12-15`

- **Page id:** `2ca1de57-0199-80aa-a323-fc9d3fc564a0`
- **Command:** `find_meeting.py --page … --deep` → rc=0
- **Result:** **155 blocks / 25,225 chars** (8-section AI summary + 4 `to_do`s + full verbatim transcript)
- **Archived to:** `MLE Internal Meetings/archive-reads/2025-12-15-calvary-roofing-ramp-call.deepread.txt` (27,802 bytes, tracked)
- **Published:** nothing. No org, no person, no activity, no CRM write, no title corrected.

**Why this row:** it was `read-page` #1 largest-body-first on the live chain this run
(`check:archive` → `recount:q84` → `worklist:q84`, all rc=0, printing 7 read / 9 already-read).

**⚠️ THE TITLE IS THE OUTLIER ON THE PAGE'S OWN EVIDENCE — filed, NOT fixed.** The row is titled
*"**Donny** & Tyson &Robert"*. The AI summary and the full transcript say *"**Donnie**"* — **23
occurrences to 3, and all 3 of the `Donny` spellings are the title and the URL derived from it.** This is
the `Chain`/`Chai` defect from the 2025-12-18 STG row in a second column, on a different row, from a
different quarter — so it is a **pattern in how these titles were typed**, not a one-off. Q84's DoD
forbids this pass overwriting a human-typed title, so it is filed to `/api/admin/flags` and left alone.
**No last name for Donnie appears anywhere on the page**, so this row cannot resolve the person by itself
and none was invented.

**The depth-4 cap undercounts a SIXTH consecutive time:** 22,497 measured vs 25,225 read. Six for six,
always low. The capped number is a **floor**; no row may be called small — let alone empty — on one.

**All four "claims absence" fields are empty here** (`Call Recording`, `Meeting Summary`,
`Google Doc Link`, `Export Status`) over a body holding a full summary and transcript. Read together with
inc.38 — where two of those four were *populated* — the archive is **inconsistently filled**, so no
consumer may key off a fixed field shape in either direction.

**Identity, from the page only.** Every property but `Call Date` and the timestamps is null. This is an
**STG-era RAMP consulting call (2025-12-15)**: Rob (Robert Acheson) + **Tyson** on the consulting side,
**Donnie**, founder of **Calvary Roofing** (Kansas City / Missouri), on the other. First venture as an
owner; ~5 years as a 1099 selling project manager; **$700k retail in a good year, sold through networking,
not door-knocking**; portfolio ~50% insurance / 50% retail. A **2-day onsite was set for January 14–15**
at Donnie's home. Four to-dos, all logistics (Tyson sends invites + Slack; Donnie sources KC barbecue; no
homework).

**A transcription garble recorded, not resolved:** the same passage that says *"Chester's maintenance
system. repair"* (the summary reads it as **Chester's Maintenance & Repair**, the business Donnie is
renaming into Calvary) also produces *"Calgary Mission Roofing"* once, where every other mention on the
page says **Calvary Roofing**. Two mangled tokens in one noisy stretch — written down so nobody later
treats either as a second company.

**STG-era publish question is Rob's and was not re-asked.** Reading a page already in his own archive was
never what that gate was about (inc.37); writing a record from it still is.

**Still owed:** run `npm run worklist:q84` — no total on this page is authoritative. As of this
increment the machine says **6 `read-page` + 18 `deep-read-page`**, with **10 rows carried as
`already-read`**. Derived, not typed.

---

## READ 2026-08-06 (Q84 inc.41) — `Ryan Groth Explaining ClickUp 2025-12-26` · 110 blocks / 21,102 chars · **READ, NOT PUBLISHED**

- **Page:** `2d51de57-0199-8091-905c-e0000e9aa309`
- **Read:** 2026-08-06, `find_meeting.py --page … --deep`, rc=0, 23,505 bytes of output,
  archived at `MLE Internal Meetings/archive-reads/2025-12-26-ryan-groth-clickup.deepread.txt`.
- **Measured before the read:** 19,050 chars. **Read:** 21,102 chars over 110 blocks
  (paragraph×63, bulleted_list_item×34, heading_3×10, to_do×2, transcription×1).
- **Scheduled by the machine, not chosen:** it was the top `read-page` row largest-body-first.

**⚠️ THE FINDING — this is an STG-era INTERNAL call, and it is sitting in the archive the CRM is
being built from.** Nothing on the row says so. `Contact Name` = **Ryan Groth** is the *only*
identity field populated; `Company Meeting with`, `MLE`, `MLE Attendees`, `Non MLE Attendees`,
`Sales Rep`, `Meeting Type`, `Parent Account` are all null. The body is Rob and Ryan Groth walking
**STG's ClickUp build** on 2025-12-26 — sales launch workshop, Sales Builder, playbook, onboarding
system, summit system. Filed to `/api/admin/flags` (`q84-read-2025-12-26-ryan-groth-clickup`, high).

**The MLE select is a CANDIDATE discriminator, NOT an adopted one.** It is `None` here on an
STG-internal row and `YES` on the Gulf Coast 2026-07-22 row — one data point each way. That is
consistent with the field working, and it is *not* proof that it can be used to filter STG-era rows
out; a filter adopted on two rows is how real MLE meetings get silently dropped. Recorded, not used.

**⚠️ SECOND FINDING — a tactic Rob objected to, on the record, inside material that will be mined.**
The transcript closes on a proposal to build **fake customer personas and post AI-generated
testimonials from them** on LinkedIn/TikTok, modelled on real customers: *"Like create a fake client
that talks about us."* Rob's own words on the page: ***"That's kind of sketchy, though, I'll be
honest."*** Any future feature that summarises "growth strategy" out of this archive will surface
that passage as a recommendation carried in Rob's own record. Flagged so it can never be silently
promoted. It is **STG's idea, and Rob pushed back on it** — the attribution is the point.

**The depth-4 cap undercounts a SEVENTH consecutive time:** 19,050 measured vs 21,102 read. Seven
for seven, always low, never once high. A capped count is a **floor** — no row may be called small,
thin, or empty on one.

**All four "claims absence" fields are empty here** (`Call Recording`, `Meeting Summary`,
`Google Doc Link`, `Export Status`) over a full 21k body. With inc.38 (two of the four *populated*)
this stands as settled: the archive is **inconsistently filled**, and no consumer may key off a
fixed field shape in either direction.

**Content, unpublished.** STG product/ops call. Ryan Groth walks the ClickUp build: sales launch
workshop deliverables (job descriptions, comp structures, workbooks); a sales vision deck template
for owners to present at kickoff; personalised action plans generated from OMG assessments +
baseline-selling playbook via ChatGPT prompts, rendered to PDF through Gamma; Sales Builder (A-player
recruitment with VSL bundled in, *not* sold separately — asked and answered on the call); the living
sales playbook; a four-phase sales-athlete onboarding system (weeks 1–11+, role-play checkpoints
gating advancement); the hiring VSL + recruiting tech stack; the sales summit system (agendas,
recognition categories, cost/ROI framework). **Two open items stated as unfinished:** leaderboarding
/ scoreboarding, and CRM setup guides for industry partners. Two `to_do`s on the page: finish the
leaderboarding component; prepare a presentation for a call with **Kyle** (~38 minutes out, which is
where the call ends).

**Nothing published:** no org, no person, no activity, no CRM write, no title corrected. **No last
name, company or contact detail was invented from this page.**

**STG-era publish question stays Rob's and was not re-asked.** Reading a page already in his own
archive was never what that gate was about (inc.37); writing a record from it still is — and this
row is the sharpest case for why the gate exists.

**RAN `npm run worklist:q84`** (full chain — `check:archive --json` → `recount:q84 --json` →
`worklist:q84 --input`; the bare command refuses to guess and prints the chain, which is correct).
The machine says **34 open archive rows: 5 `read-page` + 18 re-read-uncapped + 0 sweep + 0 identify
+ 0 re-measure**, with **11 rows carried as `already-read` (11 of 34)**. Derived, not typed.

**Those two counters are the only proof this read happened.** Last increment the machine said
**6 `read-page` / 10 `already-read`**; it now says **5 / 11**, and it names
`2025-12-26 Ryan Groth Explaining ClickUp` in its own `ALREADY READ` block. That is the machine
reporting the read, not this page asserting it — which is exactly the witness inc.39 established
the read log to be. A read that does not move these counters did not happen.

---

## inc.42 — `Meeting 2026-06-16T11:05:00.000-04:00` — READ (2026-08-07)

**Page:** `3811de57-0199-80d4-b8e5-c0a7c8465085` ·
`https://app.notion.com/p/Meeting-3811de57019980d4b8e5c0a7c8465085`
**Archive:** `MLE Internal Meetings/archive-reads/2026-06-16-rob-connor-ai-voice-agent-buildout.deepread.txt`
(21,844 bytes) · `find_meeting.py --deep` rc=0 → **128 blocks / 19,402 chars**.

**Why this row:** the machine's #1 `read-page` row, largest-body-first, on the live chain — not chosen
by hand. Q92 is still the top unchecked queue item and its only open half **(a)** (the
`[email redacted @golf.org]` address on `C-2018`) is expressly Rob's call and must not be actioned
headless.

### THE FINDING — this call carries a tactic that collides with a MANDATORY standing rule

The body is Rob and a vendor named only **Connor** configuring **outbound AI voice agents** to work a
real-estate lead pond (~**500 leads/month** off overflow, all described as opt-in, live-transfer to a
human ISA). Three lines on the page, quoted from the AI summary and the transcript:

- Knowledge base to be supplied by Rob must answer *"Are you an AI?"* — it is listed among the
  questions **"likely to stump the AI."**
- *"Connor confirmed the current voice quality is strong — **many users don't realize they're talking
  to AI**, and even those who do tend to still engage."*
- *"Short conversations (3–5 questions) **reduce the likelihood of the AI being detected** or making
  errors."*

`~/.claude/rules/ai-voice-legality.md` makes a state-by-state legality check **mandatory before any AI
voice agent work**, and this call is Florida outbound (the rapport question on the page is literally
*"Do you have any connection or history to this area of Florida?"*) into a stated **60+** demographic.
Whether the vendor's approach is lawful is **not** decided here and is not asserted — what is recorded
is that the archive the CRM is built from contains a **minimise-AI-detection** tactic, and Rob's own
products (AIDRE / the outbound caller) are in exactly this category. Any future feature that
summarises "what has worked" out of this archive would surface it as a recommendation carried in
Rob's own record. **Filed to `/api/admin/flags`, not fixed.**

**Rob's recorded position is narrower than that and is not overstated here:** the page says he
*"expressed prior skepticism about voice AI being clunky and recognizable"* — a quality objection,
**not** a disclosure objection. He is not recorded objecting to non-disclosure, and no objection was
invented for him. Contrast inc.41, where the pushback was verbatim.

### Measurements

- **Depth-4 cap undercounts an EIGHTH consecutive time:** 17,173 measured vs **19,402** read. Eight
  for eight, always low, never once high. A capped count is a **floor** — no row may be called small,
  thin or empty on one.
- **All four "claims absence" fields are empty** (`Call Recording`, `Meeting Summary`,
  `Google Doc Link`, `Export Status`) over a body carrying a full 6-section AI summary, 5 `to_do`s and
  the verbatim transcript. With inc.38 (two of the four *populated*) this stays settled: the archive is
  **inconsistently filled**, and no consumer may key off a fixed field shape in either direction.
- **Identity exists only in the body.** Every property except `Call Date` is null — `Contact Name`,
  `Company Meeting with`, `MLE`, `Sales Rep`, `Meeting Type`, `Parent Account`, all empty — and the
  title is the bare container string `Meeting 2026-06-16T…`. The row announces nothing about itself.

### Deliberately NOT resolved, recorded so nobody quietly picks a reading

- **Connor's company is never stated.** The example greeting on the page reads *"this is Connor with
  the authority group in Southwest"* — but the same live-transfer template is rendered three
  different ways in the same passage (*"Will's RE group"*, *"the health and prosperity group"*), which
  is ASR noise on one template, not three organisations. **No company was inferred and no last name
  invented.**
- *"Boontown's horrible"* — a vendor name garbled beyond recovery, spoken as a complaint about a third
  party. Written down so the token is not later mistaken for a real company.

**Nothing published:** no org, no person, no activity, no CRM write, no title corrected.

**RAN the full chain** (`check:archive --json` → `recount:q84 --json` → `worklist:q84 --input`).
Counter movement is the only proof this read happened (inc.39's witness rule) — see the entry below
this line once re-run; last increment the machine said **5 `read-page` / 11 `already-read`**.

---

## inc.43 — `Meeting 2026-08-05` — READ (2026-08-07)

- **Page:** `3b31de57-0199-80ea-9171-cd12c228e3d7` · https://app.notion.com/p/Meeting-3b31de57019980ea9171cd12c228e3d7
- **Read:** `find_meeting.py --page … --deep` → rc=0, **90 blocks / 11,462 chars**
  (paragraph×59, bulleted_list_item×21, heading_3×5, to_do×4, transcription×1)
- **Archived:** `MLE Internal Meetings/archive-reads/2026-08-05-internal-omega-platform-scott-drone.deepread.txt`
- **Nothing published:** no org, no person, no activity, no CRM write, no domain corrected, no title fixed.

### THE FINDING — the CRM asserts an Omega domain the archive it came from calls a coin-flip

The page's own enrichment field, **verbatim**:

> *"Likely related: Omega National Title Agency https://omeganationaltitle.com/ **and/or** Omega Title
> Holdings Group https://omegatitlegroup.com/"* … *"No exact company name provided in transcript;
> needs identification."*

Prod `orgs` **C-2019 `Omega Title (FL)`** carries `domain = omegatitlegroup.com`,
`website = https://omegatitlegroup.com` — settled, with **no record that a second candidate ever
existed**. The source says *and/or*; the CRM says *is*. That is the archive's uncertainty being
dropped on the way in, not resolved on the way in.

It compounds the Omega split already reported to Rob in dev-chat on 2026-08-06 (seven Omega people in
Supabase, **zero** on the file copy of C-2019 — "two books, different Omega"). This is a **third**
axis of the same identity problem: which legal entity.

**Why it is high and not a note:** the same page's first action item is *"Finalize and wrap up the
platform proposal/demo for the title company prospect."* A proposal is in flight against a record
whose identity its own source declines to settle. **Filed as a flag; no domain touched — Rob's call.**

### Second finding (medium) — the fee anchor is contradicted on its own page

Spoken on the call: *"the fucking title capture … I think the average is like $4,000 a month … I think
we can get some good fees on that."* The **same page** records *"public plans range ~199–699/mo
depending on tier"* — a **6×–20× gap**. It matters because the prospect's stated motivator is
recurring revenue he has never had (*"I've never had that a hundred percent… it excites me even if
it's just a little"*). **Neither figure is asserted as correct here** — the $4k is what was said, the
199–699 is what the page claims, and this read verified neither against titlecapture.com. No money,
quoted or paid field was touched; nothing in the CRM carries this number today.

### Measurements

- Work-list scheduled it at **9,849 chars**; uncapped read returned **11,462** — the depth-4 cap
  undercut by 1,613 chars (14%), the **ninth consecutive** undercount. A capped count is a **FLOOR**.
- **All four "claims absence" fields empty over a full body** (Google Doc Link, Meeting Summary,
  Export Status, Call Recording) — the four-fields-lie pattern holds again.
- Every property except `Created` / `Last Edited` is null. Title is the bare container string
  `Meeting 2026-08-05`, so the page name identifies nothing about the call.

### Deliberately NOT resolved, recorded so nobody quietly picks a reading

- **Which Omega.** `omeganationaltitle.com` was **not** evaluated, added, or dismissed. Choosing one
  is exactly the guess this log exists to prevent.
- **"Scott" is not resolved to a person record.** The drone-inspection service he describes
  (nationwide pilots, $2M+ homes, *"fortify your insurance"*) is **never given a company name** on the
  page. No org invented.
- **"Catherine and Paul"** — an in-person ops review is planned with them; no surname, no company
  stated on this page. Not matched to any existing person record.
- **Two claims left as claims, attributed to their speakers, not adopted:** *"You can turn any website
  into an API"* (said as the workaround for the prospect's restrictive title software, and the
  proposal action item sits directly under it), and Scott's *"99% of court systems have been presented
  with AI-based evidence"* — an unsourced legal-admissibility assertion, spoken alongside *"we're
  operating in the gray area here."* Neither is endorsed and neither is contradicted here.

**RAN the full chain** (`check:archive --json` → `recount:q84 --json` → `worklist:q84 --input`).
Counter movement is the proof this read happened (inc.39's witness rule): before this increment the
machine said **4 `read-page` / 12 `already-read`**.
